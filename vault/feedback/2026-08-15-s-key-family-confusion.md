# Reached for shift+S to invoke cluster search — the s-key family needs reconciling

- Submitted: 2026-08-15
- Priority: normal
- Area: keybindings (walked during S05)

Invoking cluster search I pressed **shift+S** first and failed — it is
`sort.clear` today. `ctrl+s` is search. Combined with the sort feedback
(`2026-08-15-sort-column-picker.md`): `s` is sort.column, `S` is sort.clear
(proposed to become the sort-column picker), and the walker has now reached for
`shift+S` for **search**. Three different meanings crowd one family.

The fold-in should reconcile the whole s-family in one pass: sort column /
sort direction / search need keys a user does not trip over — this is the same
"n-family" confusion from S02 (`2026-08-15-context-switch-key-inconsistent.md`),
now on the s-family.
