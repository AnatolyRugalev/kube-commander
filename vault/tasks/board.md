# Task Board

Live board for the kubecom rewrite. See [`README.md`](README.md) for workflow and
the item template. Status: `todo` · `in-progress` · `blocked` · `done`.

_Last updated: 2026-07-19 — M2-05b done: **resource-menu discovery reconcile** (`internal/tui/components/menu`) — `(*Model).Reconcile(kube.DiscoveryResult)` folds the async discovery result into the M2-05a static seed **without disturbing selection or scroll** (the M2 risk item). Merges in place, never rebuilds: a seed item with a discovered **twin** (exact GVR) takes its metadata (verbs/short-names/categories) and stays available (seed title + order kept); a seed item whose API **group** is in `Result.Failed` and has no twin is marked `Available = false` (muted, no-op on drill-in — group-level match catches version-pinned entries); resources the seed omits (CRDs/extra groups) are **appended after** the seed in discovery order. A **total failure** (`Result.Err`) is a no-op, and partial failures still leave a working menu (principle 3 — the fix for the original's whole-menu-blanking bug). Selection preserved via GVR→index re-resolve; scroll re-clamped. No new deps. D57. **Top-unblocked next: M2-06** (custom table: 06a snapshot / 06b live deltas / 06c hscroll). · Prev: M2-05a done: **static seed resource-menu sidebar** (`internal/tui/components/menu`) — the browse view's left pane and second `components/*` package. A static-seeded vertical list of core resource kinds (each a real `kube.Resource` GVK/GVR/scope) so browse renders + navigates before discovery (principle 4); consumes **keymap actions, not raw keys** (D11) — `Update(keymap.Action)` moves the highlight (up/down/top/bottom, clamped, with a scroll `offset` that keeps the cursor visible) and on `nav.drillIn` emits **`menu.ResourceSelectedMsg`** (unavailable item → no-op, for M2-05b). Title = `GVK.Kind`; pure `View` frames with `Pane`/`PaneFocus` + `Selection` highlight (D54), `""` until sized; no shared mutable state (principle 1). **Message ownership (D56):** the emitter's package owns the message (root imports the component → no back-import), so `ResourceSelectedMsg` lives in `menu` and the placeholder `tui.ResourceSelectedMsg` was removed from `msg.go`. No new deps. **Top-unblocked next: M2-05b** (discovery reconcile without disturbing selection/scroll). · Prev: M2-04 done: **status bar component** (`internal/tui/components/statusbar`) — kubecom's bottom bar and the first `components/*` package. One line: `context · namespace · [spinner] discovering…` left, keymap short-help hint right-aligned. **Pure render from props** (`SetContext`/`SetNamespace`/`SetShortHelp`/`SetWidth`) + an embedded `bubbles/spinner` on the `Styles.Spinner` accent role (D54); owns no shared state (principle 1). Short-help comes in **as a string** from `help.Model.ShortHelpView()` (D11) — the bar never matches a raw key. Spinner ticks **gated on discovering**: `StartDiscovery()` seeds `spinner.Tick`, `Update` forwards `spinner.TickMsg` only while discovering, so `StopDiscovery()` stops the animation with no timer to cancel. Width-aware right-align (`gap` spaces + `Width(w).MaxWidth(w)` clamp); tight width keeps the left segment and drops the hint; width 0 joins inline; empty pieces skipped. No new deps. D55. **Top-unblocked next: M2-05a** (static seed resource-menu sidebar). · Prev: M2-03 done: **lipgloss theme + style set** (`internal/tui/styles`) — the app's single source of visual truth. A `Theme` (named, semantic `color.Color` fields, no styling) → `New(Theme) Styles` (the `lipgloss.Style` roles components render through: App/Subtle/Selection/Header/Pane+PaneFocus/StatusBar/Error/Warn/Success/Spinner). One place a color becomes a style; components ask for a role, never `lipgloss.Color`/`NewStyle`, so re-theming (M4/D6) just swaps the Theme. `DefaultTheme()` dark truecolor + blue accent; `Default()`=`New(DefaultTheme())`. Pure/immutable/copyable, no global renderer, deterministic. Dep `charm.land/lipgloss/v2` **promoted indirect→direct** (require-block move only; no version change, `go.sum` untouched). `make check` green; ANSI sanity-checked. D54. **Top-unblocked next: M2-04** (status bar component). · Prev: M2-02 done: **TUI message types + channel→msg pumps** (`internal/tui/msg.go`) — the `kube`-channels → Bubble Tea `tea.Msg` boundary. `ResourceEventMsg`/`WatchClosedMsg`/`DiscoveryReadyMsg`/generic `ErrorMsg{Context,Err,Kind}` (+ `ResourceSelectedMsg`/`RowSelectedMsg`); `watchPump`/`discoveryPump` read one item per Cmd (delta→event, watch ERROR→ErrorMsg, closed→WatchClosedMsg; discovery keeps total failure in `Result.Err`). Zero shared mutable UI state (principle 1); fake-channel tested, `-race` clean, no new deps. D53. · Prev: M2-PLAN done: **M2 app-shell expanded into ordered, leg-sized Backlog slices** (M2-02 … M2-14) so the milestone has pickable, dependency-ordered work again (the M2-01 action-registry group being complete). Slices, top-down: M2-02 msg types + channel→msg pumps · M2-03 lipgloss theme/styles · M2-04 status bar · M2-05a/b resource menu (seed + discovery reconcile) · M2-06a/b/c custom table (snapshot / live deltas / hscroll) · M2-07a/b/c/d root app shell (keymap-routed skeleton / two-pane browse / live watch wiring / discovery reconcile) · M2-08 namespace picker · M2-09 filter · M2-10 confirm/prompt modal · M2-11 config menu persistence · M2-12 legacy migration · M2-13 column sort (#85) · M2-14 teatest coverage. Package layout per REWRITE_PLAN; zero shared mutable UI state throughout (principle 1). Board-only leg (no code; `make check` green). D52. **Top-unblocked next: M2-02** (pure msg types + fake-channel-tested pumps, no UI deps).  ·  Prev: M2-01e done: **keybindings doc generated from the registry + drift check** (`internal/tui/keymap/doc.go` → `docs/keybindings.md`) — the doc half of D11. `(*Keymap).Markdown()` renders the committed reference from the registry (banner + one table per action namespace, Action/Keys/Description, keys `/`-joined vim-first, all from `Actions()`/`Keys()`/`Describe()`, no literals). Golden `TestKeybindingsDoc` compares the file to `DefaultKeymap().Markdown()` and **fails `make check` on drift**; `make keys-doc` (`go test … -update`) regenerates — the test is the generator (no `cmd/`). Doc at new repo-root `docs/`. **Completes the M2-01 group (01a–01e).** D51. Active milestone: M2; next up the **M2 app shell** (root model routing input through the keymap/sequencer + the M2-01d help overlay, then two-pane browse + live table). M1-04b parked until the M2 menu exists.  ·  Prev: M2-01d done: **help generated from the keymap registry** (`internal/tui/keymap/help.go` + `internal/tui/help`) — the help side of D11's "zero hard-coded keys", all derived from the resolved keymap so it can't drift. `(*Keymap).Binding(a)` → a `bubbles/v2/key.Binding` (WithKeys = resolved tokens, WithHelp = `gg/home`-style display + `Describe()`; empty/unknown → **disabled** binding, no panic); `Bindings()` for the whole registry; `HelpKeyMap`/`HelpMap()` satisfies bubbles' `help.KeyMap` (`ShortHelp` = curated status-bar subset, enabled-only; `FullHelp` = enabled bindings grouped into columns by action namespace, disabled dropped). New `internal/tui/help` overlay `Model` wraps `bubbles/help.Model` (`ShowAll`) — `Toggle`/`SetVisible`/`Visible`/`SetWidth`/`View` (full when visible, `""` hidden) / `ShortHelpView` (always-on hint); matches no raw keys (root model resolves `app.help` via the keymap). Dep **pinned `charm.land/bubbles/v2` v2.0.0** (+ lipgloss v2.0.0 indirect): go directive 1.24.2, requires bubbletea v2.0.0 → MVS keeps our v2.0.2, no Go/bubbletea bump (v2.1.0 needs Go 1.25); D50. Rendered both help views to confirm. The M2-01 action-registry group is functionally complete bar the standalone doc file. Active milestone: M2; next up **M2-01e** (committed keybindings markdown doc from the registry + drift check — split out of M2-01d), then the **M2 app shell** (root model routing input through the keymap/sequencer, embedding this overlay, two-pane browse + live table). M1-04b parked until the M2 menu exists.  ·  Prev: M2-01c done: **config `keys:` wiring** (`internal/config` + `kubecom keys`) — the user config's first real field and the keymap's first config surface (D49). `internal/config` is now a real package: `Config.Keys map[string][]string` (`json:"keys"`), zero value runs on defaults; **no new YAML dep** (`sigs.k8s.io/yaml`, already direct, `UnmarshalStrict` rejects unknown top-level fields so typos fail loudly). `Load`/`LoadFile` (missing file → zero config), `Path()` = `os.UserConfigDir()/kubecom/config.yaml` (D20). `Config.Keymap()` casts string ids → `keymap.Action` and reuses M2-01a's `Merge` wholesale (unknown-action/bad-token/collision errors; nav-shadow warnings). `kubecom keys` prints the effective map in registry order (disabled → `(disabled)`); warnings → stderr, table → stdout, invalid keymap → exit 1. No new deps. Active milestone: M2; next up **M2-01d** (bubbles/key.Binding + help overlay generated from the registry, D11) — the last M2-01 slice, then the M2 app shell (root model, browse view, table). M1-04b parked until the M2 menu exists.  ·  Prev: M2-01b done: **multi-key sequences + timeout resolution** (`internal/tui/keymap`) — the vim `gg`→`nav.top` case (D10) on top of M2-01a's single-chord resolver. A binding is now a `seq` (`[]chord`, len≥1) so single chords and sequences share one model; `parseSequence` takes a lone chord / vim concat (`gg`) / space-separated (`g g`) forms and `seq.String()` round-trips. The `Keymap` stays immutable (adds `prefix`/`extends` indexes beside exact `bySeq`); a new stateful `Sequencer` holds the only mutable input state (the buffered prefix) and returns `ResultAction`/`ResultPending`/`ResultNone` — a non-continuing key abandons the buffer, `Timeout` fires a prefix only if it's itself a complete binding. **The keymap package runs no timer**: `ResultPending` → model schedules `tea.Tick(SequenceTimeout=500ms)` → `Timeout()`, so the package is pure/hermetic (principle 1). pgdn/pgup stay half-page only (full-page fallback would collide, per D47). No new deps; `-race` clean. D48. Active milestone: M2; next up **M2-01c** (YAML `keys:` config wiring + `kubecom keys`), then M2-01d (bubbles/key + help overlay).  ·  Prev: M2-01a done: **keymap core** (`internal/tui/keymap`) — first slice of the M2 action registry (D10/D11). One canonical `chord` normalises a configured token (`parseChord`) and a live keypress (`chordFromKey(tea.Key)`) to the same string, so `KeyMsg → Action` resolution is one reverse-index lookup; shift is folded into the rune (`G`), never a modifier. `DefaultKeymap` is a single vim-first data table, collision-free by construction; `Merge(overrides)` returns a new validated keymap + nav-shadow warnings (unknown-action/bad-token/collision are errors), replacing each action's binding wholesale (empty=disable). Registry primitives `Actions`/`Keys`/`Describe` ready for help gen; no raw-key matching leaks; no view wired yet. No new deps. D47. **M2 is now in-progress.** M2-01 split into slices — next up **M2-01b** (multi-key sequences: `gg`→`nav.top`), then M2-01c (YAML `keys:` config wiring) / M2-01d (bubbles/key.Binding + help overlay). Active milestone: M2.  ·  Prev: M1-09 done: **typed graceful errors** (`internal/kube/errors.go`) — `Classify(err) ErrorKind` walks the layer's wrapped-error chain to a small taxonomy (not-found/already-exists/conflict/forbidden/unauthorized/invalid/timeout/unreachable/bad-context/unknown) the M2 TUI switches on to degrade one feature instead of crashing (#86, principle 3). Classification over rewiring — no call site changes, underlying error still reachable via `errors.Is`/`As`. apierrors `Is*` predicates unwrap `%w`; transport failures → `Unreachable` via `*url.Error`/`net.Error`; `RESTConfig` tags kubeconfig/context failures with an `errBadContext` sentinel (clientcmd has no predicate for an override-context miss). No new deps. D46. **M1's "graceful, typed errors" exit clause met.** Active milestone: M1; **M1's actionable Backlog is now empty** — M1-04b parked until the M2 menu exists, two remaining exit criteria are envtest-gated integration tests. Next up **M2-01** (action registry + configurable keymap, D10/D11) — the foundational M2 input layer.  ·  Prev: M1-08 **background port-forward** (`internal/kube/portforward.go`) — `Clients.PortForward(ctx, ref, ports)` forwards local ports to a pod in the background over an SPDY dialer to the pod's `portforward` subresource (built from the retained `*rest.Config`), exactly what kubectl upgrades; typed-clientset REST client (pod-only, not the dynamic path). `portforward.PortForwarder` does the work on a goroutine; kubecom wraps it in a **channel-based `PortForward` handle** — `Ready`/`Done`/`Err`/`Ports`/`Stop` (idempotent). No mutex-guarded result (goroutine writes `err` before closing `doneCh`; `Err` reads after → happens-before, principle 1); ctx cancellation stops it à la Logs/Watch. Own `ForwardedPort{Local,Remote}` keeps the TUI boundary client-go-tooling-free. Testable via an injected `forwarderFactory` + `fakeForwarder`; `-race` clean; live forward is envtest territory. 3 indirect transitives added (gorilla/websocket, moby/spdystream, go-flowrate), no version drift (D45). **M1-08 exit criterion met.** Active milestone: M1; next up **M1-09** typed graceful errors (#86). (M1-04b lazy group detail parked until the M2 menu exists.)_

_Prev: 2026-07-19 — M1-07b done: **describe** (`internal/kube/describe.go`) — `Clients.Describe(r, ref)` renders `kubectl describe`-identical output in-process by reusing kubectl's own describe generators (`k8s.io/kubectl/pkg/describe`, pinned v0.31.4); built-in describer keyed by GroupKind with a generic-unstructured fallback for CRDs, `RESTMapping` built straight from the discovery `Resource` (D42). Second in-process viewer._

_Prev: 2026-07-19 — M1-07a done: **get object as YAML** (`internal/kube/yaml.go`) — `Clients.GetYAML` renders any resource as kubectl-identical `get -o yaml` through the dynamic client (managedFields stripped, `sigs.k8s.io/yaml`, D41). First in-process viewer; M1-07 split into 07a/07b/07c._

_Prev: 2026-07-19 — M1-06e-2 done: **drain eviction loop** (`internal/kube/drain.go`) — `Clients.Drain(ctx, nodeRes, node, opts)` completes drain: candidates (M1-06e-1) → cordon (M1-06c) → evict via the typed policy/v1 Eviction API (PDB-aware, 429-retry to the ctx deadline, NotFound=success) → evict-all-then-wait-all for deletion (poll until NotFound or UID change). Candidates-before-cordon: a refused drain leaves the node uncordoned. Timing knobs are package vars; hermetic reactor-based tests. D40. **M1-06 (action set) complete** — delete/scale/restart/cordon/suspend/drain all landed. Active milestone: M1; next up **M1-07** (logs/describe/YAML), then M1-08 port-forward, M1-09 typed errors. M1-04b (lazy group detail) parked until the M2 menu exists._

## In Progress

- [ ] **M2-06a** Table component: snapshot render (`internal/tui/components/table`)
      status: in-progress | owner: claude-opus | added: 2026-07-19 | claimed: 2026-07-20
      notes: First slice of M2-06 (custom table — bubbles/table too basic, M2 risk).
      Render a `kube.Table` snapshot with vertical scroll, selection, and Home/End
      via keymap actions; emit the table's own `RowSelectedMsg` on drill-in (moves
      out of `msg.go` per D56). 06b (live deltas) / 06c (hscroll) stay in Backlog.
      Depends on: M2-02, M2-03.

## Blocked

_(none)_

## Backlog

### M0 — Groundwork
_(none — M0 complete)_

### M1 — Kube layer
- [ ] **M1-04b** Lazy group detail on first open (fetch a group's full resource detail only when its menu is opened)
      status: todo | owner: — | added: 2026-07-18
      notes: split from M1-04 — M2-coupled; needs the menu open interaction. Do after M2 menu exists.

### M2 — Core TUI
M2-01 (action registry + configurable keymap, D10/D11) is **complete** (01a keymap
core / 01b sequences / 01c config wiring / 01d help overlay / 01e generated doc).
The rest of M2 is the **app shell** — expanded here into ordered, leg-sized slices
(D52). Take them top-down; each notes what it depends on. Package layout follows
[`../REWRITE_PLAN.md`](../REWRITE_PLAN.md): `internal/tui/{app.go,msg.go}`,
`internal/tui/styles`, `internal/tui/components/*`, `internal/tui/views/*`. Every
slice keeps **zero shared mutable UI state** (principle 1); goroutines only send
messages.

- [ ] **M2-06** Table component (`internal/tui/components/table`)
      status: todo | owner: — | added: 2026-07-19
      notes: Custom table (bubbles/table too basic — M2 risk). Split — **M2-06a**:
      render a `kube.Table` snapshot with vertical scroll, selection, Home/End via
      keymap actions. **M2-06b**: apply live watch deltas (RESET/ADDED/MODIFIED/
      DELETED) from the M2-02 msgs into rows, keyed by `ObjectRef`, preserving
      selection. **M2-06c**: horizontal scroll + wide-column handling. Depends on:
      M2-02, M2-03.

- [ ] **M2-07** Root app model / shell (`internal/tui/app.go`, replaces the M0
      `internal/tui/tui.go` placeholder)
      status: todo | owner: — | added: 2026-07-19
      notes: Split — **M2-07a**: root `tea.Model` skeleton owning the resolved
      keymap + `Sequencer`, routing `KeyMsg`→`Action` (schedule `tea.Tick` on
      `ResultPending`, D48), embedding the M2-01d help overlay (toggle `app.help`),
      window-size, and `app.quit` — no panes yet. **M2-07b**: compose the two-pane
      **browse** layout (menu | table) with focus switching between panes.
      **M2-07c**: wire the live table to `kube.Watch` via the M2-02 pump (select a
      resource in the menu → start/stop the watch → table updates live).
      **M2-07d**: kick off async discovery on Init and route `DiscoveryReadyMsg`
      into the menu (M2-05b) + status-bar spinner. Depends on: M2-02..06.

- [ ] **M2-08** Namespace picker (`internal/tui/components/picker`, bubbles/list)
      status: todo | owner: — | added: 2026-07-19
      notes: Namespace switcher over a `bubbles/list`; scaffolding reused later for
      context/container/port pickers. Filtering handled by the picker; selection
      emits a "namespace changed" msg that re-scopes the watch. Depends on: M2-07c.

- [ ] **M2-09** Table filter (`bubbles/textinput`)
      status: todo | owner: — | added: 2026-07-19
      notes: `/`-triggered filter (keymap `filter.open`) over the current table's
      rows; `n`/`N` next/prev match. Client-side over the row set. Depends on:
      M2-06, M2-07.

- [ ] **M2-10** Confirm/prompt modal (`internal/tui/components/modal`)
      status: todo | owner: — | added: 2026-07-19
      notes: Replaces the old racy tcell popup (REWRITE_PLAN motivation). A
      message-driven overlay: confirm (y/n) + text prompt, resolved through keymap
      actions, returns a result msg. No mutex, no shared popup state (the whole
      point). Depends on: M2-03, M2-07a.

- [ ] **M2-11** Config: menu customization persistence (`internal/config`)
      status: todo | owner: — | added: 2026-07-19
      notes: Extend the M2-01c `Config` struct with the menu/browse fields
      (customized resource list, order, last namespace) + `Save`/`SaveFile`
      (round-trips the `keys:` field). Wire the app to load on start and persist
      changes. `UnmarshalStrict` already rejects typos (D49). Depends on: M2-05,
      M2-07.

- [ ] **M2-12** Legacy config migration (`internal/config/migrate.go`)
      status: todo | owner: — | added: 2026-07-19
      notes: One-shot migration from the old `~/.kubecom.yaml` (protobuf-yaml theme
      config) to the new plain-YAML config on first start; malformed/legacy files
      degrade gracefully (principle 3), never block start. Inspect the `master`
      branch's `config/` for the old shape. Depends on: M2-11.

- [ ] **M2-13** Column sort (#85)
      status: todo | owner: — | added: 2026-07-19
      notes: Client-side sort by column on the table's row set (keymap `sort.*`),
      stable, type-aware where cheap. Closes #85. Depends on: M2-06.

- [ ] **M2-14** teatest coverage: update loop · menu reconcile · modal flow
      status: todo | owner: — | added: 2026-07-19
      notes: The M2 exit criterion for tested UI — drive the root model with
      teatest/v2 (M0-05 harness) over: key→action routing, a menu reconcile on a
      fake `DiscoveryReadyMsg` preserving selection, and a modal confirm flow.
      Depends on: M2-07, M2-10.

_Remaining M3–M5 items to be expanded when those milestones open. See milestone files for scope._

## Done

- [x] **M2-05b** Resource-menu sidebar: discovery reconcile (`internal/tui/components/menu`) — `(*Model).Reconcile(kube.DiscoveryResult)` folds the async discovery result into the M2-05a static seed **without disturbing selection or scroll** (the M2 risk item). Merges in place, never rebuilds: a seed item with a discovered **twin** (exact GVR match) takes the twin's discovery metadata (verbs/short-names/categories) and stays available, keeping the seed's curated title + order; a seed item whose API **group** is in `Result.Failed` **and** has no twin is marked `Available = false` (rendered muted, a no-op on drill-in — group-level match so a version-pinned seed entry is still caught); discovered resources the seed omits (CRDs, extra groups) are **appended after** the seed in discovery's stable sorted order. A **total failure** (`Result.Err != nil`) is a no-op so the seed stays fully navigable, and a partial failure still leaves a working menu (principle 3: degrade, don't blank — the fix for the original's whole-menu-blanking bug). Selection is preserved by resolving the highlighted GVR back to its post-merge index; the scroll offset is re-clamped so nothing jumps (append-only keeps the index stable regardless). No new deps (adds an `apimachinery/.../schema` import already used in `seed.go`). Hermetic white-box tests: twin-fill + CRD-append, failed-group→unavailable (untouched sibling stays available), selection+scroll preserved across an append, total-failure no-op. D57. **Top-unblocked next: M2-06** (custom table component — 06a snapshot render / 06b live watch deltas / 06c hscroll).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-05a** Static seed resource-menu sidebar (`internal/tui/components/menu`) — the browse view's left pane and the second `internal/tui/components/*` package. `Model` is a **static-seeded** vertical list of core resource kinds (`seedItems()`: namespaces/nodes/events · pods + apps/batch workloads · services/ingresses · configmaps/secrets/serviceaccounts · pvcs/pvs/storageclasses), each carrying a real `kube.Resource` (GVK/GVR/scope) so a selection hands the kube layer everything List/Watch needs; the seed lets the browse view render and be navigated **before** discovery completes (fast cold start, principle 4). Consumes **keymap actions, never raw keys** (D11): `Update(a keymap.Action) (Model, tea.Cmd)` moves the highlight (up/down/top/bottom, clamped) with a scroll `offset` that keeps the cursor visible on a short pane, and on `nav.drillIn` emits **`menu.ResourceSelectedMsg`** for the highlighted item (an unavailable item — the M2-05b marker — is a no-op on drill-in). Display title = `GVK.Kind` (canonical, correctly-cased, uniform for built-ins + CRDs). Pure value-receiver `View` frames the list with `Pane`/`PaneFocus` and highlights the cursor via the `Selection` style (D54); returns `""` until sized. Owns no shared mutable state (principle 1). **Message ownership (D56):** a component-emitted message is owned by the emitter's package — the root model imports the component, so the component can't import package `tui` back without a cycle — so `ResourceSelectedMsg` lives in `menu`, and the placeholder `tui.ResourceSelectedMsg` was **removed** from `msg.go` (which now keeps only kube-boundary messages + `ErrorMsg`; `RowSelectedMsg` moves to the table package in M2-06). No new deps. Hermetic white-box tests + an end-to-end render sanity-check. D56. **Top-unblocked next: M2-05b** (reconcile the seed with `DiscoveryReadyMsg` without disturbing selection/scroll).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-04** Status bar component (`internal/tui/components/statusbar`) — kubecom's bottom bar and the first `internal/tui/components/*` package. One line: `context · namespace · [spinner] discovering…` on the left, the keymap short-help hint right-aligned. **Pure render from props** (`SetContext`/`SetNamespace`/`SetShortHelp`/`SetWidth`) + its own `bubbles/spinner` (styled with the `Styles.Spinner` accent role, D54); owns no shared state (principle 1). The short-help is generated upstream from the effective keymap via `help.Model.ShortHelpView()` (D11) and handed in **as a string**, so the bar never matches a raw key. Spinner ticks are **gated on discovering**: `StartDiscovery()` sets the flag + returns the seed `spinner.Tick`; `Update` forwards a `spinner.TickMsg` only while discovering, so `StopDiscovery()` breaks the self-scheduling tick chain (no timer/goroutine to cancel). Width-aware layout right-aligns the hint (`gap` spaces + `Width(w).MaxWidth(w)` clamp); a tight width keeps the live left segment and drops the hint; width 0 (pre-`WindowSizeMsg`) joins inline; empty pieces skipped (no dangling separator). No new deps. Hermetic pure tests + an ANSI render sanity-check (removed). D55. **Top-unblocked next: M2-05a** (static seed resource-menu sidebar).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-03** Lipgloss theme + style set (`internal/tui/styles`) — the app's single source of visual truth: a `Theme` (a struct of named, semantic `color.Color` fields — Foreground/Subtle/Primary/Selection(+Fg)/Border(+Focus)/Header/StatusBar fg+bg/Error/Warn/Success, no styling) and a derived `Styles` (the `lipgloss.Style` values components render through: App/Subtle/Selection/Header/Pane+PaneFocus(rounded border, accented on focus)/StatusBar/Error/Warn/Success/Spinner). `New(Theme) Styles` is the **one place a color becomes a style**, so components ask for a role and never call `lipgloss.Color`/`NewStyle` — re-theming (M4/D6) just swaps the Theme. `DefaultTheme()` is a dark-friendly truecolor palette with a blue accent (nod to the original); `Default()` = `New(DefaultTheme())`. Pure, immutable, copyable data — no shared mutable UI state, no global renderer (v2 dropped it), deterministic (tested). Dep `charm.land/lipgloss/v2` v2.0.0 **promoted indirect→direct** (was transitive via bubbles, D50): require-block move only, no version change, `go.sum` untouched. `make check` green; ANSI output sanity-checked (truecolor fg+bg, rounded accent border). D54. **Top-unblocked next: M2-04** (status bar component — renders context · namespace · spinner · short-help hint through these styles).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-02** TUI message types + channel→msg pumps (`internal/tui/msg.go`) — the boundary between the concurrent `kube` layer (returns work on channels) and the single-threaded Bubble Tea loop (consumes `tea.Msg`). Msg types: `ResourceEventMsg` (a `kube.WatchEvent` data delta, carried verbatim), `WatchClosedMsg` (terminal watch-channel close), `DiscoveryReadyMsg` (the whole `kube.DiscoveryResult` reconcile signal), a generic classified `ErrorMsg{Context,Err,Kind}` (`NewErrorMsg` fixes `Kind = kube.Classify(Err)`), plus cross-component selection msgs (`ResourceSelectedMsg`, `RowSelectedMsg`). The **pumps** are `tea.Cmd` adapters reading **exactly one** item per invocation: `watchPump` (delta→`ResourceEventMsg`, watch ERROR→`ErrorMsg`, closed→`WatchClosedMsg` so the model stops re-pumping a closed channel) and `discoveryPump` (always `DiscoveryReadyMsg`, keeping a total failure inside `Result.Err`+isolated `Result.Failed` rather than a generic error; closed-empty→nil). One receive per Cmd → `Update` never blocks on more than one; the model re-issues to pull the next. Size uses Bubble Tea's own `tea.WindowSizeMsg`. No UI, no shared mutable state (principle 1); hermetic fake-channel tests, `-race` clean, no new deps. D53. **Top-unblocked next: M2-03** (lipgloss theme/styles — promote lipgloss v2 to a direct dep).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-PLAN** Expanded the M2 app-shell into concrete, ordered, leg-sized Backlog slices (M2-02 … M2-14, several pre-split into sub-slices), so the milestone is no longer a prose seed with nothing pickable. Turned the M2 milestone scope + exit criteria into a dependency-ordered task list: **M2-02** msg types + channel→msg pumps; **M2-03** lipgloss theme/styles; **M2-04** status bar; **M2-05a/b** resource-menu sidebar (static seed + discovery reconcile); **M2-06a/b/c** custom table (snapshot render / live watch deltas / horizontal scroll); **M2-07a/b/c/d** root app shell (keymap-routed skeleton / two-pane browse / live table wiring / discovery reconcile); **M2-08** namespace picker; **M2-09** filter; **M2-10** confirm/prompt modal; **M2-11** config menu persistence; **M2-12** legacy config migration; **M2-13** column sort (#85); **M2-14** teatest coverage. Each carries scope + dependency notes; package layout follows REWRITE_PLAN (`internal/tui/{app,msg}.go`, `styles`, `components/*`, `views/*`), zero shared mutable UI state throughout. Board-only leg, no code change (`make check` green). **Top-unblocked next: M2-02** (pure, no UI deps). D52.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-01e** Generated keybindings doc from the registry + drift check (`internal/tui/keymap/doc.go` + `docs/keybindings.md`) — the "generated keybindings doc" half of D11 (help overlay was the in-app half, D50). `(*Keymap).Markdown()` renders the committed reference straight from the registry: fixed banner + one markdown table per action **namespace** (reusing `groupOf` from help.go), columns Action / Keys / Description in registry order, keys quoted and `/`-joined vim-first (`docKeys`, no-keys → em dash). Nothing restates a literal key — all `Actions()`/`Keys()`/`Describe()`. **Drift-guarded** by golden test `TestKeybindingsDoc` (`doc_test.go`): compares `docs/keybindings.md` to `DefaultKeymap().Markdown()`, fails `make check` (via `go test ./...`) if stale; a `-update` flag rewrites it, exposed as **`make keys-doc`** — the test *is* the generator, no standalone `cmd/`. `TestMarkdownFromRegistry` asserts every id/key/description appears (derivation, not restatement). Committed doc at repo-root **`docs/`** (new, discoverable); resolved from the test as `../../../docs/keybindings.md` (go test cwd == package dir). Linked from `vault/knowledge/keybindings.md`. No new deps. Verified the guard both ways (tampered → fail with the `make keys-doc` hint; regenerated → green). **Completes the M2-01 action-registry group (01a–01e).** D51. Active milestone: M2; next up the **M2 app shell** (root model routing input through the keymap/sequencer + embedding the M2-01d help overlay, then two-pane browse + live table fed by the `kube` watch channel). M1-04b parked until the M2 menu exists.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-01d** Help generated from the keymap registry (`internal/tui/keymap/help.go` + new `internal/tui/help`) — the help side of D11 ("zero hard-coded keys"), all derived from the resolved keymap so it can't drift from actual bindings. `(*Keymap).Binding(a)` turns one action into a `bubbles/v2/key.Binding`: `WithKeys` = the resolved canonical tokens (vim key first), `WithHelp` = display text (tokens joined `/`, e.g. `gg/home`, `ctrl+d/pgdn`) + the registry `Describe()`; an action with **no bound keys** (disabled or unknown id) yields a **disabled** binding (never a panic) so renderers skip it via `Enabled()`. `Bindings()` = the whole registry in order. `HelpKeyMap` (from `HelpMap()`) satisfies bubbles' `help.KeyMap`: `ShortHelp()` = a curated status-bar subset (`shortHelpActions`, enabled-only), `FullHelp()` = every enabled binding grouped into columns by action **namespace** (id prefix before the first `.`), first-seen column order, registry order within, disabled dropped. New `internal/tui/help` package is the toggleable overlay: a `Model` wrapping `bubbles/help.Model` (`ShowAll=true`) + the `HelpKeyMap` — `Toggle`/`SetVisible`/`Visible`, `SetWidth` (width-based short-help elision), `View()` (full overlay when visible, `""` when hidden), `ShortHelpView()` (always-on status-bar hint). It **matches no raw keys** — the (future) root model resolves `app.help` through the keymap and calls `Toggle`; this component only chooses what help to show (no shared mutable state, principle 1). Dep **pinned `charm.land/bubbles/v2` v2.0.0** (+ transitive `charm.land/lipgloss/v2` v2.0.0): v2.0.0's go directive is 1.24.2 and it requires bubbletea v2.0.0 — MVS keeps our pinned v2.0.2 (no downgrade, no Go bump); v2.1.0 needs Go 1.25.0, v2.1.1 needs bubbletea v2.0.7. Held in lockstep with the bubbletea v2.0.2 / Go-1.24.2 floor (D26). Rendered both help views end-to-end to confirm output (two registry-derived columns; short-help ellipsis at narrow widths). Hermetic pure-logic tests. Standalone generated markdown doc + drift check split to **M2-01e**. D50.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-01c** Config `keys:` wiring (`internal/config` + `kubecom keys`) — the user config gets its first real field and the keymap its first config surface (on M2-01a's `Merge`, D49). `internal/config` becomes a real package (was a doc-only stub): `Config.Keys map[string][]string` (`json:"keys"`), the action-id → key-tokens override table; the zero value runs on defaults. **No new YAML dep** — `sigs.k8s.io/yaml` was already direct (M1-07a), decodes plain user YAML through JSON tags, and `UnmarshalStrict` **rejects unknown top-level fields** so a typo (or a not-yet-wired section written early) fails loudly. `Load(io.Reader)` + `LoadFile(path)` (a missing file → zero config, not an error; kubecom runs on defaults with no file); `Path()` = `os.UserConfigDir()/kubecom/config.yaml` (D20). **`Config.Keymap()`** casts each string id → `keymap.Action` and calls `DefaultKeymap().Merge(overrides)` — reuses M2-01a validation wholesale (unknown-action/bad-token/collision are errors; a nav-key shadow is a returned warning), no resolution logic duplicated; config imports keymap one-way. **`kubecom keys`** loads config (default path or `--config`), resolves, and prints the effective map in registry order (disabled actions → `(disabled)`); merge **warnings → stderr**, table → stdout, an invalid keymap fails the command (exit 1). `printKeys` split from cobra `RunE` for hermetic tests. No new deps; ran `kubecom keys` end-to-end (defaults / override+warning / invalid→exit 1). Remaining M2-01 slice: **M2-01d** (bubbles/key.Binding + help overlay from the registry).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-01b** Multi-key sequences + timeout resolution (`internal/tui/keymap`) — the vim `gg` → `nav.top` case (D10) and the general multi-key mechanism on top of M2-01a's single-chord resolver. A binding is now a **`seq` (`[]chord`, length ≥ 1)** so single chords and sequences share one model: `parseSequence` accepts a lone chord (`j`/`up`/`ctrl+d`), the vim concatenated form (`gg`→`g`,`g`), and a space-separated form (`g g`/`ctrl+w k`) for sequences mixing modified/special chords; a `+`-token that fails to parse stays a hard error (never re-read rune-by-rune), and `seq.String()` round-trips (`gg` stays `gg`) for help gen. `DefaultKeymap` binds `nav.top` to `{"gg","home"}` (vim sequence + single-key fallback). The **`Keymap` stays immutable** and gained two derived indexes — `prefix` (is this a prefix of a binding) and `extends` (does a longer binding extend it) — beside the exact `bySeq` map. A new **`Sequencer`** holds the *only* mutable input state (the buffered prefix); the model owns one and drives it key-by-key: `Input` → `ResultAction`/`ResultPending`/`ResultNone`, a non-continuing key abandons the buffer and is retried alone, `Timeout` fires a buffered prefix iff it's itself a complete binding (a `g`-that-is-also-`gg`-prefix pends then fires on timeout — vim semantics). **The keymap package runs no timer**: `ResultPending` tells the model to schedule `tea.Tick(SequenceTimeout)` (exported var, 500ms) and call `Timeout()` — pure/hermetic, no goroutines/clock (principle 1). `Keymap.Action(key)` stays as the single-key resolver for non-buffering views (modals/pickers); `nav.top` is still reachable through it via `home`. **pgdn/pgup stay half-page only** — the board's earlier "wire full-page pgdn/pgup fallbacks" note would collide (both half- and full-page in one flat context; the exact reason D47 chose half-page). No new deps. `-race` clean. D48. Remaining M2-01 slices: **M2-01c** (YAML `keys:` config wiring + `kubecom keys`), M2-01d (bubbles/key.Binding + help overlay from the registry).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M2-01a** Keymap core (`internal/tui/keymap`) — the first slice of the M2 action registry (D10/D11): the single package where keys exist. **Canonical `chord`** normalises both a configured token (`parseChord`) and a live keypress (`chordFromKey(tea.Key)`) to one string — modifiers (`ctrl`/`alt`, lowercased, fixed order) `+` a base that is a case-sensitive printable rune (`G`≠`g`) or a lowercased special name (`up`/`enter`/`pgdn`/…); resolution is a single reverse-index lookup. **Shift is never a modifier** — a shifted letter is its capital rune (matches keybindings.md); `chordFromKey` prefers `ShiftedCode`→`Text`→`Code` so shift+g reads `G` under Kitty and legacy terminals, and lowercases under ctrl (`ctrl+D`==`ctrl+d`). **`DefaultKeymap`** is one data table (vim-first D10), collision-free by construction (panics if malformed — caught by tests); `pgdn`/`pgup` fall back to the half-page actions only (full-page keeps `ctrl+f`/`ctrl+b`) to stay collision-free in a flat context. **`Merge(overrides)`** returns a new validated keymap + warnings, never mutates the receiver: each entry replaces an action's binding wholesale (empty=disable); errors on unknown action / bad token / a chord bound to two actions (collision names both); **warns** (not errors) when an override shadows a nav key (D10). Registry primitives `Actions()`/`Keys()`/`Describe()` ready for later help generation. No raw-key matching leaks to callers; no runtime surface wired yet. No new deps (`tea` already vendored); hermetic pure-logic tests (parse round-trip, default validity, resolution, merge override/collision/unknown/shadow/disable). D47. Splits M2-01 → M2-01b (multi-key `gg`), M2-01c (YAML `keys:` wiring), M2-01d (bubbles/key + help gen).
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-09** Typed graceful errors (`internal/kube/errors.go`) — `Classify(err error) ErrorKind` maps any error the kube layer returns (all wrapped `fmt.Errorf(... %w)`) to a small taxonomy the M2 TUI switches on to **degrade a single feature instead of panicking** (#86, principle 3): `KindNotFound`/`AlreadyExists`/`Conflict`/`Forbidden`/`Unauthorized`/`Invalid`/`Timeout`/`Unreachable`/`BadContext`/`Unknown`, each with a stable-token `String()`. **Classification over rewiring**: zero call sites change; the underlying error stays reachable via `errors.Is`/`As`. apierrors `Is*` predicates already unwrap `%w` to the embedded `*StatusError` (called directly, most-specific first); transport failures (dial refused / DNS / TLS — no HTTP status) arrive as `*url.Error`/`net.Error` → `Unreachable`, plus `IsServiceUnavailable` (503); `context.DeadlineExceeded` → `Timeout`. **Bad-context is layer-tagged, not string-matched**: an override context that doesn't exist is a plain `fmt.Errorf("context %q does not exist")` inside clientcmd that **no clientcmd predicate matches**, so `RESTConfig` wraps its errors with an unexported `errBadContext` sentinel (dual-`%w`) and `Classify` keys off `errors.Is`; the clientcmd predicates (`IsContextNotFound`/`IsEmptyConfig`/`IsConfigurationInvalid`, run per-chain-link via `chainMatches`) stay as a secondary net. No new deps. Hermetic tests: a `Classify` table over constructed apierrors/url/net errors, a dual-`%w`-wrapped case (chain-walking), a real `RESTConfig(unknown-context)`→`KindBadContext` (the #86 path end-to-end), empty-kubeconfig, and `String()`. **M1's "graceful, typed errors" exit clause met.** D46. Next: M2-01 (action registry + keymap) — M1-04b stays parked until the M2 menu exists; M1's two remaining exit criteria are envtest-gated integration tests.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-19
- [x] **M1-08** Background port-forward (`internal/kube/portforward.go`) — `Clients.PortForward(ctx, ref, ports)` forwards local ports to a pod in the background (in-process, no kubectl binary, D2) over an **SPDY dialer** to the pod's `portforward` subresource: `spdy.RoundTripperFor(c.Config)` (the transport the `Clients` doc reserved) + `spdy.NewDialer(..., "POST", RESTClient().Post().Resource("pods")…SubResource("portforward").URL())`, exactly what kubectl upgrades. Typed-clientset REST client, not the dynamic path (port-forward is pod-only; drain/logs precedent D39/D43). `portforward.PortForwarder` does the work (`go ForwardPorts()`); kubecom wraps it in a **channel-based `PortForward` handle** — `Ready()` (listeners up), `Done()` (forwarding ended), `Err()` (fatal error / nil on clean stop), `Ports()` (bound local:remote pairs, for OS-assigned `:<remote>` local ports), `Stop()` (idempotent via `sync.Once`). **No mutex-guarded result (principle 1):** the goroutine writes `err` before closing `doneCh`, `Err()` reads it only after `doneCh` is closed (happens-before → race-free). ctx cancellation stops the forward via a bridge goroutine, mirroring Logs/Watch. Own `ForwardedPort{Local,Remote uint16}` keeps the TUI boundary client-go-tooling-free (D33). Ports syntax is kubectl's (`8080:80` / `80` / `:80`); empty pod name / empty port list / malformed spec rejected before any dial (#86). Testable via an injected `forwarderFactory`: `newPortForward` split from the exported method, driven by a `fakeForwarder` (ready→ports→clean-stop, fatal-error→Err, ctx-cancel-stops, factory-error→nil-handle, idempotent Stop, wrapped Ports error); `-race` clean; a live forward is envtest territory. `go mod tidy` added 3 indirect transitives (gorilla/websocket, moby/spdystream, mxk/go-flowrate) within the pinned k8s v0.31.4 graph — no direct-dep/version change. **M1-08 exit criterion met** (port-forward runs in a background goroutine and can be stopped). Next up **M1-09** typed graceful errors. D45.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-19
- [x] **M1-07d** Reconnecting/resuming follow logs (`internal/kube/logs.go`) — a **Follow** stream now survives a transient transport drop, extending the M1-07c single-connection core into a reconnecting loop à la watch (D34). `runLogStream` dispatches on `follow`: the non-follow path is unchanged (single connection, verbatim); the follow path (`followLogStream`) reopens on a drop. **Resume mechanics:** logs have no resourceVersion, so Follow **forces `Timestamps` on the wire** (every raw line `"<RFC3339Nano> <content>"`), records the last-seen line's timestamp, and on reconnect sets `SinceTime` to it (`SinceSeconds`/`TailLines` cleared — initial-read only). Timestamps are **stripped before delivery unless `opts.Timestamps`**; an unparseable prefix degrades to verbatim, never dropped. **Dedup:** `SinceTime` is second-granular (metav1.Time → RFC3339 seconds), so resuming re-serves the whole last second; the `logResumer` holds the raw lines already delivered in that second and drops the re-serves, delivering only genuinely-new lines (incl. ones missed *during* the drop), then clears the window on the next second. **Stop vs reconnect:** `bufio.Scanner` reports nil at `io.EOF`, so a clean end (container's log ended, à la `kubectl -f` exiting) stops the follow; any other read error reconnects. **Errors:** first open failure is terminal (one `LogEvent{Err}`, like non-follow); a *reconnect* open/read failure is transient — silent backoff (`logRetryBackoff`, the watch twin, a package var so tests shrink it, D40) + retry bounded by ctx — because a `LogEvent{Err}` is the terminal event by contract. Hermetic: pure `parseLogTimestamp` + `logResumer.process` (strip/keep-ts, dedup-resumed-second, window-clear) tests, and `followLogStream` via a resumable fake opener + a `scriptReader` (ends a connection with a drop error or clean EOF) covering reconnect+dedup (asserts the 2nd open's SinceTime), clean-end-stops, first-open-terminal, retried-reconnect-open (silent), ctx-cancel-mid-backoff; `-race` clean. Live follow-across-a-real-drop is envtest territory. No new deps. Completes **M1-07** (07a YAML / 07b describe / 07c logs / 07d reconnect). D44.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-07c** Streaming pod logs (`internal/kube/logs.go`) — `Clients.Logs(ctx, ref, opts)` streams a pod container's logs onto a bounded `LogEvent{Line,Err}` channel (`logChanBuffer=256`) via the typed clientset's `pods/log` subresource (`CoreV1().Pods(ns).GetLogs(name, *corev1.PodLogOptions).Stream(ctx)`) — the only in-process route, since logs have no dynamic-client path (mirrors drain's deliberate typed-client use, D39). `LogOptions` mirrors `kubectl logs` flags (Container/Follow/Previous/Timestamps/TailLines/SinceSeconds/SinceTime/LimitBytes), mapped 1:1 by pure `podLogOptions` (`SinceTime *time.Time`→`*metav1.Time`). Stream opened **inside** the goroutine (like Watch) so `Logs` never blocks first paint; the goroutine owns every send + the close (principle 1). Lines forwarded verbatim (server formats per the Timestamps flag), trailing newline stripped; a >1 MiB line (`logScanMaxLine`) ends the stream with `bufio.ErrTooLong` as a terminal error event, never a silent truncation or panic (#86). **Single connection** — Follow keeps it open for live lines until the container ends or ctx is cancelled, but a transient drop ends the stream; reconnect/resume à la watch split to M1-07d. Third in-process viewer (D2). Empty pod name rejected. Hermetic: pure `podLogOptions` table test, `streamLogs` from byte streams (verbatim / no-trailing-newline / over-long / mid-stream ctx-cancel), `runLogStream` wiring via a fake `logStreamOpener` (success / open-error / already-cancelled); live stream is envtest territory. No new deps. D43.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-07b** Describe (`internal/kube/describe.go`) — `Clients.Describe(r, ref)` renders `kubectl describe`-identical output in-process by reusing kubectl's own describe generators (`k8s.io/kubectl/pkg/describe`). Selection mirrors `describe.NewDescriber`: the specialized built-in describer keyed by `GVK.GroupKind()` (`DescriberFor`) with a fallback to the generic unstructured describer (`GenericDescriberFor`) so CRDs/rarer built-ins are covered — kept as a small local `describerFor` since we already hold the `*rest.Config`. The generic describer's `meta.RESTMapping` is built directly from the discovery `Resource` (pure `restMappingFor`, only GVR/GVK/scope), not re-resolved through the RESTMapper. No context param — kubectl's describe package has no context-aware entry point (uses `context.TODO`); empty name rejected, errors wrapped (#86). Second in-process viewer (D2). Dep **pinned to k8s.io/kubectl v0.31.4** to match the k8s stack — a bare `go mod tidy` resolves it to v0.36 and drags the whole graph up; footprint is kubectl direct + ~12 transitive indirect (cli-runtime, kustomize api/kyaml, liggitt/tabwriter, treeprint, …). Hermetic tests cover describer selection (Pod→`*PodDescriber`, CRD→generic), the pure `restMappingFor`, and empty-name rejection; live describe output is envtest territory. D42.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-07a** Get object as **YAML** (`internal/kube/yaml.go`) — `Clients.GetYAML(ctx, r, ref)` fetches any resource (built-in or CRD) through the dynamic client via the shared `resourceInterface` helper (ns dropped for cluster-scoped), strips `metadata.managedFields` on a DeepCopy (kubectl hides them from `get -o yaml` by default since v1.21; pure `marshalYAML` so the caller's object is untouched + directly testable), and renders with `sigs.k8s.io/yaml` (JSON round-trip → deterministic key order + json-tag-honoring, byte-identical to kubectl). First in-process viewer (D2 — no pager, no kubectl binary). Empty name rejected; NotFound wrapped (#86). Split from M1-07 (→ 07b describe, 07c logs). `sigs.k8s.io/yaml` promoted indirect→direct; no new module. Hermetic fake-dynamic-client + pure `marshalYAML` tests. D41.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-06e-2** Drain: **eviction loop** (`internal/kube/drain.go`) — `Clients.Drain(ctx, nodeRes, node, opts)` completes drain: computes candidates via M1-06e-1's `DrainCandidates` (refuses upfront on any blocker), **then** cordons the node (reuse M1-06c, dynamic client) — candidates-before-cordon so a refused drain never leaves the node cordoned — then evicts every candidate through the **typed policy/v1 Eviction API** (`Clientset.PolicyV1().Evictions(ns).Evict`, the same PDB-honoring subresource `kubectl drain` posts to). **PDB-aware:** a `429 TooManyRequests` (budget momentarily exhausted) is retried after `evictionRetryInterval` until it clears or **ctx** expires (ctx deadline is the whole budget, à la `kubectl drain --timeout`); `NotFound` = already gone = success. Two-pass **evict-all-then-wait-all** so grace periods overlap; `waitPodDeleted` polls `drainPollInterval` until each pod is NotFound or its name resolves to a different UID (recreated ⇒ original gone, the D35 snapshot guard). Timing knobs are package vars (5s/2s) shrunk to 1ms in tests. Hermetic: eviction reactor injects 429/NotFound/success + deletes from the tracker; cordon via fake dynamic client, evict/wait via fake typed clientset; `TestDrainRefusesBeforeCordon` locks the ordering. No new deps. Completes M1-06 (delete/scale/restart/cordon/suspend/drain). D40.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-06e-1** Drain: pod **selection** (`internal/kube/drain.go`) — `Clients.DrainCandidates(ctx, node, opts)` lists a node's pods via the typed clientset (`spec.nodeName` field selector, all namespaces) and runs a pure `classifyDrainPods([]corev1.Pod, DrainOptions)` that mirrors `kubectl drain`. Skipped silently (not evicted, not blocking): mirror pods (annotation `kubernetes.io/config.mirror`, inlined — no k8s.io/kubernetes dep), already-terminated pods (Succeeded/Failed), DaemonSet-managed pods when `IgnoreDaemonSets`. Blocking (collected into one refusal error naming each pod; drain refuses as a whole → nil slice, never partial): standalone-w/o-`Force`, DaemonSet-w/o-`IgnoreDaemonSets`, emptyDir-w/o-`DeleteEmptyDirData`. Check order (mirror→terminated→controller→emptyDir) means each pod is reported once. DaemonSet by owner-ref Kind (no existence GET — simplification). Returns `[]ObjectRef` (apimachinery-free boundary, D33) — the exact input M1-06e-2's eviction loop consumes. Drain uses the **typed clientset** (pod/node-specific), a deliberate departure from 06a–06d's generic dynamic path. Empty node rejected; list error wrapped (#86). Hermetic: pure `classifyDrainPods` table tests + fake clientset for the wiring (fake ignores field selectors → filtering is envtest territory). `k8s.io/api` indirect→direct in go.mod; no new module. D39.
      status: done | owner: claude-opus | added: 2026-07-19 | done: 2026-07-19
- [x] **M1-06d** Actions: **cronjob suspend/resume** (`internal/kube/actions.go`) — `Clients.Suspend(ctx, r, ref)` / `Clients.Resume(ctx, r, ref)` merge-patch a CronJob's `spec.suspend` (true/false) through the dynamic client, exactly as `kubectl patch cronjob NAME -p '{"spec":{"suspend":...}}'` does. A near-exact mirror of M1-06c cordon/uncordon (D37): reuse the shared `resourceInterface` helper; CronJobs are namespaced so the ref's namespace is honored (unlike the cluster-scoped node). Suspend gates only *new* Jobs — already-running Jobs keep running (the parallel of "cordon stops only new pods"). Resume writes concrete `false` (not null, which would drop the key). Idempotent → no UID guard; empty name rejected; NotFound wrapped (#86). Shared `setSuspend` body; wire format in pure `suspendPatch`; `suspendNoun`/`suspendVerb` for error verbs; hermetic fake-dynamic-client tests. No new deps. D38.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-19
- [x] **M1-06c** Actions: **cordon/uncordon** (`internal/kube/actions.go`) — `Clients.Cordon(ctx, r, ref)` / `Clients.Uncordon(ctx, r, ref)` merge-patch a node's `spec.unschedulable` (true/false) through the dynamic client, exactly as `kubectl cordon`/`uncordon` do; reuse the shared `resourceInterface` helper (cluster-scoped → ref namespace dropped). Uncordon writes concrete `false` (not null, which would remove the key). Idempotent → no UID guard; empty name rejected; NotFound wrapped (#86). Shared `setUnschedulable` body; wire format in pure `unschedulablePatch`; hermetic fake-dynamic-client tests. **Narrowed** from "cordon/uncordon + drain" — drain split to M1-06e. D37.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-06b** Actions: **scale** + **rollout-restart** (`internal/kube/actions.go`) — `Clients.Scale(ctx, r Resource, ref ObjectRef, replicas int32)` sets a scalable workload's replica count by **merge-patching the `scale` subresource** (`{"spec":{"replicas":N}}` via `.Patch(..., "scale")`): generic over Deployment/ReplicaSet/StatefulSet/ReplicationController and any CRD with a scale subresource, since replicas live at `scale.spec.replicas` regardless of the object's own schema. `Clients.RolloutRestart(ctx, r, ref)` triggers a rolling restart by merge-patching `spec.template.metadata.annotations["kubectl.kubernetes.io/restartedAt"]` with a UTC RFC3339 timestamp — kubectl's exact key, so kubecom and `kubectl rollout restart` interoperate and the annotation doesn't proliferate. **Merge** (not strategic) patch keeps both schema-free → works on unstructured objects and CRDs; the annotation add leaves sibling annotations intact. Both reuse the shared `resourceInterface(r, ns)` helper; empty name rejected; negative replicas rejected locally; NotFound surfaced wrapped (#86). No UID precondition (unlike Delete): `PatchOptions` carries none and the actions are idempotent. Wire format proven via pure `scalePatch`/`restartPatch`; the fake dynamic client round-trips the merge patch (it applies to the whole tracked object, ignoring the subresource) to assert `spec.replicas`, restartedAt-without-clobber, and error paths. No new deps. D36.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-06a** Action: generic **delete** (`internal/kube/actions.go`) — `Clients.Delete(ctx, r Resource, ref ObjectRef, opts metav1.DeleteOptions)` deletes any resource (built-in or CRD) through the **dynamic client**, addressed by GVR + scope (from the discovery `Resource`) and namespace/name (from the row `ObjectRef`) via a shared `resourceInterface(r, ns)` helper (namespaces the client iff `r.Namespaced`; a namespace on a cluster-scoped ref is dropped). When `ObjectRef.UID` is present, a `metav1.Preconditions{UID}` is layered on (pure `withUIDPrecondition` helper) so acting on a table snapshot never deletes a recreated same-named object; a ref without a UID deletes by name, and caller-set preconditions are preserved. Empty name rejected; NotFound surfaced **wrapped** (`errors.Is`→`IsNotFound` holds, #86). First slice of M1-06 (split, cf. D33). Hermetic tests: pure `withUIDPrecondition` for the precondition contract (the fake dynamic client **discards DeleteOptions**), `dynamic/fake` for the round-trip (removal, ns routing, empty-name, wrapped NotFound). `go mod tidy` added indirect `json-patch.v4`. D35.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-05b** Server-side Table **Watch** → event channel: `internal/kube/watch.go` — `Clients.Watch(ctx, r, ns, opts) (<-chan WatchEvent, error)` starts a background reconnecting List→Watch goroutine streaming `WatchEvent{Type,Columns,Rows,Err}` deltas (`ADDED/MODIFIED/DELETED/RESET/ERROR`) on a bounded channel (`watchChanBuffer=64`). Opens the same `tableRequest` as List with `watch=true`+`allowWatchBookmarks=true` via `.Stream()`; `streamTableWatch` decodes the `metav1.WatchEvent` stream with a plain `json.Decoder`, reusing `decodeTableRV` (new — `decodeTable` now delegates; surfaces `ListMeta.ResourceVersion`). Every (re)connection begins with a List→`RESET` (full row set + columns); resumable drops/clean EOF reconnect from the last resourceVersion with no re-List; `410 Gone`/`Expired` (sentinel `*errExpired` from `watchStatusError`) forces a full re-List + fresh `RESET`. Columns cached per connection (server omits defs after the first chunk); bookmarks advance the resourceVersion. Backoff `2s`, ctx-owned lifecycle (goroutine owns all sends + `close`). No new deps; hermetic tests drive `streamTableWatch` from byte streams + the full `watchLoop` over a `rest/fake` transport. Completes M1-05; M1 List+watch exit criterion met. D34.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-05a** Server-side Table **List** → typed `Table{Columns,Rows}`: `internal/kube/table.go` — `Clients.List(ctx, Resource, ns, opts)` fetches any discovered resource as a server-printed Table (`Accept: application/json;as=Table;v=v1;g=meta.k8s.io,application/json`) so columns are kubectl-identical for built-ins and CRDs with zero hard-coding. The dynamic client can't set the Table Accept header per call, so List builds a per-GroupVersion `rest.Interface` (`restClientForGV`) and sets it on the raw request; `decodeTable` flattens the `metav1.Table` JSON into a TUI-facing `Table`/`Column`/`Row` (no apimachinery in the TUI), with per-row `ObjectRef{Namespace,Name,UID}` from each row's embedded `PartialObjectMetadata` (Table default `IncludeObject=Metadata`). Bad/missing row metadata degrades to a zero ObjectRef, row kept. No new deps (rest, rest/fake, kubernetes/scheme already vendored). Hermetic tests via `rest/fake.RESTClient` + pure `decodeTable`. First slice of M1-05; Watch is M1-05b. D33.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-04** On-disk discovery cache + invalidation: `internal/kube/cache.go` + rewired `NewClients` — discovery is now backed by kubectl's on-disk `discovery/cached/disk.CachedDiscoveryClient` (replaces the M1-02 in-memory memcache as the base of the deferred RESTMapper, so discovery+mapping share one on-disk cache). Cache dir `os.UserCacheDir()/kubecom/{discovery/<host-slug>,http}` — per host:port (distinct clusters must not cross-serve), disposable, separate from the D20 config dir; TTL 6h. `Clients.Invalidate()` forces a refetch (clears disk cache + Resets the retained deferred mapper); seed mapper untouched. Degrades to in-memory `memcache` when no cache dir resolves; still zero network I/O at construction (fast cold start holds). `Discovery` field widened to `CachedDiscoveryInterface`. New transitive deps: httpcache, diskv, btree. Lazy group detail → M1-04b. D32.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-03** Async full discovery → reconcile signal; per-group fault isolation: `internal/kube/discovery.go` — `discoverResources` runs one `ServerPreferredResources` pass and flattens it into a sorted `[]Resource` (GVK/GVR/scope/verbs/short-names/categories) of listable, non-subresource kinds. Partial `*discovery.ErrGroupDiscoveryFailed` keeps healthy resources and isolates each broken/denied group into `DiscoveryResult.Failed` (#87, #76); total failure → `.Err`, empty results. `StartDiscovery(ctx,d) <-chan DiscoveryResult` runs the pass in a goroutine, delivers once on a cap-1 buffered channel (the "discovery ready" reconcile signal), returns immediately — never blocks first paint. `(*Clients).StartDiscovery` convenience; zero TUI imports. Cache/re-discovery deferred to M1-04. D31.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-02** Seed-set core GVKs with static REST mapping for instant start: `internal/kube/seed.go` — a static `meta.DefaultRESTMapper` seeded with ~28 core GVKs (core/v1, apps/v1, batch/v1, networking/v1, rbac/v1, storage/v1), each with its exact plural/singular resource + scope via `AddSpecific` (irregular plurals kubectl-identical). `NewClients` composes it ahead of the D29 deferred discovery mapper via `meta.FirstHitRESTMapper` — seeded kinds resolve instantly with zero network I/O; unknown kinds fall through to discovery. Instant-start half of D8; M1-03 adds async full-discovery reconcile. D30.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-01** Client bootstrap: `internal/kube/client.go` — `ClientConfig{Kubeconfig,Context}` → `RESTConfig` (clientcmd default rules + context override, non-interactive, wrapped errors) → `NewClients` (clientset + dynamic + discovery + deferred discovery RESTMapper) + `Connect` convenience. No network I/O at construction; RESTMapper deferred/mem-cached so bootstrap never blocks first paint (D8). Hermetic tests (temp kubeconfig + dummy rest.Config, D18); no new deps. D29.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M1-00** envtest harness: `internal/kube/envtest_test.go` — opt-in behind `KUBECOM_TEST_ENVTEST=1` (`requireEnvtest(t)` skips by default so `make check` stays hermetic), smoke test starts a real control plane and GETs the `default` namespace. Added the first kube deps (client-go + apimachinery v0.31.4, controller-runtime v0.19.4) and `make test-envtest` (fetches binaries via `setup-envtest`). D28.
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-06** goreleaser skeleton (Linux+macOS): `.goreleaser.yml` reshaped to v2 syntax, single build × `goos:[linux,darwin]` × `goarch:[amd64,arm64]`; Windows dropped (D7), publishers (Homebrew/AUR/Docker) deferred to M5, `kubectl` brew dep removed (D2). `goreleaser check` + `--snapshot` verified (D27)
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-09** README references the vault + decision log: rewrite-in-progress banner atop `README.md` linking `vault/`, goals, milestones, board, `decisions.md`, journal, and `CLAUDE.md`. Closes the last open M0 exit criterion; full README rewrite deferred to M5.
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-05** Test harness: teatest (tui) smoke test — bubbletea v2 (`charm.land/bubbletea/v2` v2.0.2) + teatest/v2; placeholder root model; Go floor → 1.24.2 (D26)
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-08** Sweep remaining legacy files M0-07 missed: `Dockerfile`, `get.sh`, `ci/aur/` (incl. `id_rsa.enc`), `ci/terminalizer/`, empty `ci/`; `.goreleaser.yml` de-referenced from `ci/aur/` (D25)
      status: done | owner: claude-opus | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-04** GitHub Actions CI: `make check` (build/test/vet/golangci-lint) on Linux+macOS matrix (D24)
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-02** Toolchain: bump to Go 1.23 (go.mod directive), wire `cmd/kubecom` onto cobra v1.10.2; `ioutil` already gone via M0-07 (D23)
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **PROC-04** Cloud runs set repo-local git identity (maintainer) in routine bootstrap + skill step 0
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **PROC-03** Model split: routine session on Sonnet (orchestration), leg subagents pinned to Opus in `/do-rewrite-run`; cron corrected to UTC
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-07** Delete legacy trees from `v1`: `app/`, `cli/`, `commander/`, `config/`, `pb/`, `cmd/kube-commander/`, Windows sources, Travis/snap CI; prune `go.mod`; drop `.golangci.yml` path excludes. Absorbs M0-03. (D14, D22)
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **PROC-02** Scheduled runs self-prime: checkout `v1` + lint tooling in step 0; legs read the skill by file path (cloud clones start on `master`)
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
- [x] **PROC-01** `/do-rewrite-run` orchestrator skill: fresh subagent per leg, sequential, 4-leg/90-min budgets — for the scheduled routine
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18 (D21)
- [x] **REVIEW-01** Maintainer setup review applied: legacy deletion planned, journal split to per-entry files, claim-push, `make check` + CI-early, fakes-default tests, Bubble Tea v2, XDG config path
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18 (D14–D20)
- [x] **M0-03** Single `kubecom` binary — absorbed into M0-07 (D14)
      status: done (absorbed) | owner: — | added: 2026-07-18 | done: 2026-07-18
- [x] **M0-01** Scaffold new module layout (`cmd/kubecom`, `internal/{kube,tui,config,version}`) + `.golangci.yml` scoped to new code
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18 (D12, D13)
- [x] **BOOT-01** Bootstrap vault, goals, milestones, task board on `v1`
      status: done | owner: claude | added: 2026-07-18 | done: 2026-07-18
