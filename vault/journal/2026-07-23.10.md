# 2026-07-23 — M3-09: delete wired through the confirm modal

- Agent: claude-opus-4-8
- Milestone: M3
- Did: First confirm wiring (D88) — the delete action now routes through the M2-10
  modal instead of firing on a keypress. Added a `Deleter` seam (`WithDeleter`;
  `*kube.Clients` satisfies it, signature matches `kube.Delete` so the M1-06a/D35
  UID snapshot guard rides through) and gave the root model a single `modal.Model`
  (`modal.New(s)`, sized in `resize()`, composited first in `View`'s overlay switch,
  added to `overlayActive()`). `handleRowAction`'s new `rowActionDelete` case →
  `openDeleteConfirm`: it stashes the target (`deleteRes`/`deleteRef` — the confirm
  carries no payload, D88) and `ShowConfirm`s the modal titled `Delete` with a
  `Delete <Kind ns/name>?` message (reusing `viewerTitle`). The modal captures input
  in `handleAction` **before** the viewer check (`handleModalAction`): `nav.drillIn`
  accepts, `nav.back`/`app.quit` decline, everything else swallowed — actions only,
  no raw y/n (D11). `modal.ConfirmedMsg` (Kind-routed) → `handleModalConfirmed` hides
  the modal and `runDelete` issues `kube.Delete` off the update loop → `deleteDoneMsg`
  → status bar: a failure (NotFound/RBAC/UID Conflict) as a transient error toast
  (D74), a success as a neutral notice (D114's `surfaceNotice`). `modal.CancelledMsg`
  just hides. The deleted row leaves the table on its own via the live watch DELETED
  event (`ApplyEvent`) — no manual table mutation. With no `Deleter` wired the action
  is inert (the modal never opens). Launcher wires `WithDeleter(clients)`. → D115.
  This **unblocks M2-14b** (the modal-flow teatest now has a reachable confirm).
- Decisions: D115 — delete wired through the confirm modal; root owns one
  `modal.Model` captured in `handleAction` (topmost input surface), `ConfirmedMsg`
  routes by Kind, target stashed on the model (confirm carries no payload), the kube
  call runs off the loop and reports to the status bar (error toast / neutral notice),
  the affected row leaves via the watch stream not manual mutation, accept/decline are
  `nav.drillIn`/`nav.back` — no raw y/n. The pattern M3-10…12 reuse.
- Files: internal/tui/app.go (Deleter seam + WithDeleter, modal field + init + resize
  sizing, deleter/deleteRes/deleteRef fields, deleteModalKind, deleteDoneMsg,
  openDeleteConfirm, handleModalConfirmed, runDelete, handleDeleteDone,
  handleModalAction, rowActionDelete case, modal capture in handleAction, modal.
  ConfirmedMsg/CancelledMsg/deleteDoneMsg Update cases, overlayActive + View overlay),
  cmd/kubecom/run.go (WithDeleter wiring), internal/tui/tui_test.go (fakeDeleter +
  deleteTableModel/openDeleteModal helpers + 6 tests, modal import),
  vault/knowledge/decisions.md (D115), vault/tasks/board.md (M3-09 done, M2-14b
  unblocked), vault/journal/2026-07-23.10.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tui tests
  drive the whole path: the delete key opens the confirm modal over the selected row
  (naming the target) with no kube call yet; accept (enter) hides the modal and runs
  `kube.Delete` against that exact row (its UID rides through) → success surfaces a
  neutral notice (not an error); a decline (esc) closes without deleting; a delete
  error degrades to a status-bar toast; no deleter → the modal never opens (inert);
  the open modal swallows a nav key (the table selection stays put). Runtime surface:
  over a live row `x` now pops a `Delete <Kind ns/name>?` confirm; enter deletes and
  flashes `deleted <target>`, esc cancels. A human live-cluster confirm (a real
  destructive delete against a cluster, the row vanishing via the watch, an RBAC
  denial toast) is a dogfood follow-up (D68), not reproducible in the sandbox — no
  bare-launch regression (the modal opens only on the action). M3 milestone: the
  workload-action exit criterion stays unticked (delete done; scale/rollout/cordon/
  drain/suspend are M3-10…12); Status stays in-progress.
- Next: M2-14b — modal-flow teatest, now unblocked (D115 made the confirm reachable
  through the running program): drive `x` → enter (accept) / esc (decline) end-to-end
  with teatest/v2. Or M3-10 — scale (`ShowPrompt`) + rollout-restart (`ShowConfirm`)
  wired, the next mutating actions reusing the D115 modal pattern.
