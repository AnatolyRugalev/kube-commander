# 2026-07-23 — M2-14b: teatest coverage of the modal confirm flow

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Added full-program (teatest/v2, the M0-05 harness) coverage of the M3-09/D115
  confirm-modal flow, the half of M2-14 deferred until the modal was wired into the app
  shell. Two tests drive the delete confirm end to end through the **running bubbletea
  program** (not hand-threaded `Update` calls): `TestProgramModalConfirmAcceptRunsDelete`
  — `x` opens the modal (barrier: `WaitFor("Delete Pod pod-b?")`, whose foreground-only
  message line is byte-scannable), a nav `j` is swallowed (input capture), `enter`
  accepts → the modal closes and `kube.Delete` runs against the selected row (UID `b`
  riding through for the snapshot guard); and `TestProgramModalConfirmDeclineNoDelete` —
  same open, `j` swallowed, `esc` declines → no delete. The accept test syncs on a new
  `signalDeleter` whose `Delete` announces on a `called` channel, so the test blocks on
  the async KeyMsg->ConfirmedMsg->runDelete->Delete chain completing **before** `Quit`
  (D116) — otherwise Quit races the resolve and the delete never lands in the final
  model. Assertions read `FinalModel` for the deterministic facts (delete ran once
  against pod-b, modal hidden, selection unmoved) and the deleter for the call record
  (channel receive -> happens-before -> `-race` clean). Test-only leg: no production code
  changed. -> D116.
- Decisions: D116 — a full-program teatest of an async modal resolve must synchronise on
  the action's side-effect signal (a channel the seam records on), never on Quit-ordering;
  the modal message line is byte-scannable (assert the open via WaitFor), the async
  decline-close is not final-model-assertable (prove it via the accept path + the
  direct-Update decline test).
- Files: internal/tui/tui_test.go (signalDeleter type + TestProgramModalConfirmAcceptRunsDelete
  + TestProgramModalConfirmDeclineNoDelete), vault/knowledge/decisions.md (D116),
  vault/tasks/board.md, vault/milestones/M2-core-tui.md, vault/journal/2026-07-23.10.md
- Verify: build ok - test ok - vet ok - lint ok (`make check`, 0 issues). The two new
  tests pass under `-race -count=40` (deterministic, no flakiness). Test-only leg — no
  runtime surface changed, so no bare-launch regression risk (D68); the running binary is
  unaffected.
- Next: M3-10 — Scale + rollout-restart wired: scale via `ShowPrompt` (replicas) ->
  `kube.Scale`; rollout-restart via `ShowConfirm` -> `kube.RolloutRestart`; results to the
  status bar. Follows the D115 confirm pattern; its own teatest should reuse the D116
  side-effect-signal barrier.
