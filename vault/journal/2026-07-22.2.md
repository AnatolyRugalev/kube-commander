# 2026-07-22 — M2-11a: config write-back primitives (Save / SaveFile)

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Split the oversized M2-11 (config persistence) and took the first slice.
  Added `Config.Save(io.Writer)` (marshals the config via `sigs.k8s.io/yaml`, the
  write-back counterpart of Load — round-trips `keys:` today) and
  `Config.SaveFile(path)` (creates the parent dir 0o700, writes atomically via a
  temp file in the same dir + `os.Rename`, file 0o600 like a kubeconfig). Recorded
  D89: M2-11's "customized resource list/order" scope is already delivered by the
  per-context menu files (D83), so M2-11 narrows to write-back (this leg) +
  last-namespace persistence (M2-11b, which must not duplicate a resource list into
  `config.yaml`). Config-package only, no app wiring.
- Decisions: D89 — Save/SaveFile are the write-back primitives (atomic, 0o600);
  future persistence + M2-12 migration write through SaveFile; M2-11 narrowed per D83.
- Files: internal/config/config.go, internal/config/config_test.go,
  vault/knowledge/decisions.md, vault/tasks/board.md, vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic —
  new tests round-trip Save→Load and SaveFile→LoadFile (incl. empty-config,
  parent-dir creation, 0o600 perm, atomic overwrite leaving no leftover temp file).
  No runtime surface changed (no app wiring), so the running binary is unaffected.
- Next: M2-11b — wire config load-on-start into the app and persist the last-used
  namespace across restarts (decide per-context storage for it; record the choice).
