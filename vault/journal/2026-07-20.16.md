# 2026-07-20 — FB-go-install: fix broken `go install …@v1` in README

- Agent: claude-opus-4-8
- Milestone: M2 (feedback leg; preempts board per D69)
- Did: Addressed the oldest feedback inbox item (`2026-07-20-go-install-v1-tag-fails.md`).
  The README's install line `go install …/cmd/kubecom@v1` fails because `@v1` is a
  Go module *version query* — `v1` matches the semver major-version-1 prefix, so the
  toolchain looks for a `v1.x.x` tag (none exist) and never falls back to the branch
  named `v1`. Rewrote the Install section to make a local `v1` checkout + `go install
  ./cmd/kubecom` the primary path, added a "why not `@v1`" explainer with the working
  commit-SHA-pin remote alternative, deleted the feedback file, added an M5 scope note
  (tag a release / `v1`→`main` rename restores clean remote `go install`), and recorded
  D75 so a future README leg doesn't re-add the broken form.
- Decisions: D75 — README install is local-checkout-only until an M5 release tag; no `@v1` remote form.
- Files: README.md, vault/milestones/M5-release.md, vault/knowledge/decisions.md,
  vault/tasks/board.md, vault/feedback/2026-07-20-go-install-v1-tag-fails.md (deleted)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓  (docs-only change; full gate run and green)
- Next: **FB-welcome-page** or **FB-menu-structure** (two feedback items remain, both
  normal priority — drain the inbox before board work). Then board pick **M2-09**
  (table filter). Also noted but out of scope: the README status banner (~lines 15–18)
  is stale — it says the browse UI hasn't landed, contradicting the Usage section;
  worth a small follow-up leg.
