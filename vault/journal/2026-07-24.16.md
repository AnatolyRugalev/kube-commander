# 2026-07-24 — M3-15c: unify YAML view + edit (retire the read-only YAML viewer)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Completed the D135 unification begun in M3-15b. Retired the standalone read-only
  YAML viewer entirely — removed `openYAMLViewer`/`yamlLoadedMsg`/`handleYAMLLoaded`,
  the `viewerKindYAML` const (viewer seed now `viewerKindDescribe`), the `rowActionYAML`
  menu entry, the `ActionYAML`/`res.yaml` keymap binding + its actionMeta row, and the
  `ActionYAML` case in the browse key router. The unified **View/Edit YAML** action
  (`res.edit`/`e`) is now the only object-YAML surface: opening the object in `$EDITOR`,
  `:q` to just view (clean no-op), save to apply (M3-15b machinery). Two choices D135
  left open, decided and recorded as **D136**: (1) the surviving key is `e`; `y` is
  removed and left unbound in the browse context (no surprise-mutation on the old view
  key, minimal churn atop D133; `y` keeps its confirm-context `confirm.accept` meaning);
  (2) the action's applicability is **`canGet`, not `canEdit`** — it is viewer-first, so
  read-only (get-only) users/kinds keep YAML viewing and a save just degrades to a toast
  on the RBAC error (principle 3, like `kubectl edit`). Menu title "View / Edit YAML",
  kept in the mutating group (before delete). Removed the now-unused `canEdit` helper.
  Regenerated `docs/keybindings.md`; updated the design-intent table in
  `vault/knowledge/keybindings.md`. The `YAMLGetter` seam stays — the edit flow fetches
  through it.
- Decisions: D136 — M3-15c resolves D135: unified View/Edit YAML keeps `e`, gates on
  `canGet` (viewer-first, not write verbs), `y` retired/unbound.
- Files: internal/tui/keymap/keymap.go (drop ActionYAML const/meta/binding; comment
  refresh), internal/tui/keymap/keymap_test.go (drop `y`→ActionYAML assertion, add
  `y`-unbound-in-browse lock), internal/tui/rowaction.go (drop rowActionYAML row; edit
  row → "View / Edit YAML"/canGet; remove canEdit; comment refresh),
  internal/tui/app.go (remove yamlLoadedMsg/openYAMLViewer/handleYAMLLoaded +
  viewerKindYAML + the msg/dispatch/router cases; viewer seed → describe; YAMLGetter
  doc comments), internal/tui/tui_test.go (remove YAMLViewer* tests + helpers, keep
  fakeYAMLGetter for edit_test.go; repoint direct-key + logs.follow/secret.reveal/
  secret.copy "inert on non-X viewer" tests to the describe viewer; actions-menu test
  → "View / Edit YAML"), docs/keybindings.md (regenerated via `make keys-doc`),
  vault/knowledge/{decisions.md (D136),keybindings.md}, vault/tasks/board.md (M3-15c
  In Progress→Done + Last updated), vault/milestones/M3-actions-viewers.md (annotate the
  logs/describe exit criterion — YAML now via edit, not an in-TUI viewer),
  vault/journal/2026-07-24.16.md (new)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic: the
  removed read-only-viewer tests had no behavior left to cover (the feature is gone);
  the edit flow stays covered by edit_test.go. No bare-launch regression — the running
  binary keeps a full YAML view+edit path (now one key), describe/logs/secret viewers
  untouched. The live `$EDITOR` suspend still needs the human dogfood (advisory task
  `2026-07-24-edit-live-cluster-dogfood.md`, still open) — M3 "Edit round-trips through
  `$EDITOR`" exit criterion stays unticked.
- Next: **FB-pf-port-picker** — port-forward: offer a picker of the pod's/Service's
  declared ports instead of free-text remote entry (needs a pod-ports introspection seam
  + the modal picker; bigger than one leg — split when picked, first slice = picker over
  remote ports). It's the top unblocked M3 board item now that the M3-15 unify is
  complete. (The advisory Edit dogfood human-task blocks nothing.)
