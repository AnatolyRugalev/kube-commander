# 2026-08-21 — STORY-06i-1: the relation graph primitive

- Agent: claude-opus (do-rewrite-leg)
- Milestone: M5
- Did: Split STORY-06i on pickup (D52, primitive before surface) into 06i-1 (this),
  06i-2 (the popup) and 06i-3 (reverse-selector relations), and landed the graph:
  `kube.Relations(ctx, res, ref, kinds)` resolves one object into its navigable
  neighbours — `ownerReferences` parents (the reverse of the drill-down, the
  feedback's headline ask), the existing child scope, and the links a kind writes
  into its own spec (a pod's node, service account, PVC/config-map/secret volumes
  and image-pull secrets; a claim's volume and a volume's claim). A `Relation` is
  named (one object, `Ref()`) or set-shaped (`Scope()`, the `ChildScope` the shell's
  drill-in already consumes), so the surface reuses two paths it has rather than
  inventing a third. `Children` is now a fetch-then-`childScope` wrapper, which is
  what lets `Relations` derive the child direction off the object it already fetched.
- Decisions: D286 — a relation is a scope or a name, resolved in one Get, its target
  always a kind the caller discovered, never an edge that cannot be opened.
- Files: `internal/kube/relations.go` (new), `internal/kube/relations_test.go` (new),
  `internal/kube/children.go` (`childScope` extracted), board, decisions.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (kube-layer only, no runtime surface
  yet — nothing in the TUI calls `Relations` until 06i-2, so the binary is unchanged)
- Next: **STORY-06i-2** — the relations popup: the gesture (`R` is free), a modal list
  grouped by direction over a `Relater` seam on the `Cluster` bundle, each row opening
  its named object or its scope. That slice deletes
  `vault/feedback/2026-08-15-relations-navigation-popup.md` (D69) — it stays until the
  popup the feedback actually asked for exists.
