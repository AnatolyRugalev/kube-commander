# 2026-08-09 — APP-MONOLITH: the plan's `views/` is revoked by D264; the TUI's real shape is a shell + `components/*` sub-models

- Agent: deepseek-v4-flash (do-rewrite-leg)
- Milestone: none (feedback-driven — `2026-08-09-audit-app-monolith`, Priority: medium)
- Did: The audit found `internal/tui/app.go` at 4,635 lines / 149 functions while
  the `views/` directory REWRITE_PLAN's target layout and D52 committed to was
  never built. The audit offers two ways to resolve the drift — start a mechanical
  split, or record a decision; I took the decision route it sanctions (and the
  FORMAT-GATE journal predicted). **D264** revokes the `views/` layout: the full-screen
  views the plan listed are already `components/*` sub-models (`searchview`,
  `logsview`, `viewer`), the browse 2-pane is the root `Model` (the shell) itself
  and is not a separable view, and a new full-screen surface must be a
  `components/*` sub-model — never another root-package file. REWRITE_PLAN's
  layout block and the board's M2 layout note now match the tree; the reduction of
  `app.go` is filed as a standing, pickable board item **MONO-01** (extract a
  cohesive chunk into a sub-model when a clean seam appears). Feedback file deleted.
- Decisions: D264 (see above — the load-bearing part is pt 3: new full-screen
  surfaces are `components/*` sub-models, never root-package files; and pt 1: no
  future leg must create a `views/` directory).
- Files: vault/knowledge/decisions.md, vault/REWRITE_PLAN.md, vault/tasks/board.md,
  deleted vault/feedback/2026-08-09-audit-app-monolith.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green (vault/board
  shape guards pass with the new Done entry)
- Next: **MONO-01** — extract a cohesive chunk out of `app.go` into a
  `components/*` sub-model (candidate: port-forward panel or browse filter), the
  standing item D264 filed; or the top-unblocked board item if the human tasks it
  names unblock it.
