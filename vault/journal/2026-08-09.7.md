# 2026-08-09 — CTX-MEM-04: a remembered drill-in comes back as a drill-in, degrading legibly when the owner is gone

- Agent: deepseek-v4 (do-rewrite-leg)
- Milestone: M4 (CTX-MEM line, feedback-driven D240)
- Did: Closed the CTX-MEM-04 deferral (D240 pt 6) — the one open item on the board
  not gated on a human task. Pane memory now records the *owner* of a drill-in
  (`config.State.LastDrillOwner`) beside the remembered child kind, written by the
  same `watchResource`→`recordResource` point, so a drill-in records child kind +
  owner in one file state and a plain re-select clears the owner. `restoreLastResource`
  sees a drill owner and re-enters the scope by re-resolving the owner's child scope
  through the ordinary `ChildResolver.Children` path off the loop (never replaying the
  selector), generation-guarded like a live drill-in. A failed re-resolve — the owner
  is gone — lands on the plain child list **and** surfaces a notice naming the owner,
  which is the legibility D240 pt 6 gated the deferral on. Context switch carries
  `LastDrillOwner` through `ContextState` (D163). Docs updated (README drill-in
  paragraph + "Where you left off" bullet, `docs/configuration.md` "What kubecom
  remembers").
- Decisions: D255 (address rides the same seam; restore re-resolves never replays;
  owner-gone failure is legible and lands on the plain list)
- Files: internal/config/state.go, internal/config/state_test.go, internal/tui/
  panememory.go, internal/tui/panememory_test.go, internal/tui/app.go,
  internal/tui/context.go, cmd/kubecom/run.go, cmd/kubecom/run_test.go, README.md,
  docs/configuration.md, vault/knowledge/decisions.md, vault/tasks/board.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: the board has no other unblocked todo item — CTX-WARM-02/03/04 and M5-11 stay
  gated on their human tasks, CTX-MEM-04 is now done. Next unblocked work: a THEME
  values leg (e.g. a further palette port) or another feedback item if one lands.
