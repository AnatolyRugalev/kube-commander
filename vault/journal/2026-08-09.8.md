# 2026-08-09 — LOGS-08: a rejected previous-instance flip keeps the log view

- Agent: kimi-k3 (do-rewrite-leg)
- Milestone: none (feedback-driven LOGS line — `2026-08-09-logs-no-previous-keeps-view`)
- Did: The maintainer hit `ctrl+p` on a pod that has never restarted and got the
  apiserver's `previous terminated container … not found` **plus** the log view
  closing under him; the override: "Show error and exists log view. It shouldn't
  exit log view." `handleLogMsg`'s `ErrorMsg` branch now tells a rejected *flip*
  apart from a fresh open failure — empty view + the stashed `logReq.previous`,
  the emptiness being the toggle's own `Restream` — and instead of the D74 close
  it re-issues the stashed request with only `Previous` cleared: the running
  instance's stream resumes (a restream replaying the tail bound, so the buffer
  refills), the `[previous]` marker goes, and the server's wording rides the
  toast. The toggle stays fully re-armable (a second `ctrl+p` asks and is answered
  the same way). No string-matching on "not found" — any rejection of the
  optional toggle falls back. Rewrote
  `TestLogsPreviousWithNoTerminatedInstanceDegrades` to the new contract (view
  stays, fallback open has `Previous=false`, resumed line shows, toast names the
  reason, second press still rejects without closing). Feedback file deleted.
- Decisions: D257 (rejected flip falls back, never closes; one-bit reversal of
  D177 pt 2; no-pre-check stands) — partially supersedes D177 pt 4, annotated inline.
- Files: internal/tui/logs.go, internal/tui/logs_test.go,
  vault/knowledge/decisions.md, vault/tasks/board.md,
  deleted vault/feedback/2026-08-09-logs-no-previous-keeps-view.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: the feedback inbox still holds five items that preempt the board —
  `logs-view-palette-bindings` (palette `:` from the logs view + a better
  previous-logs binding than `ctrl+p`) pairs naturally with this one and with
  `context-switch-key-from-overlays` (both are keys-captured-by-a-surface bugs);
  taking them together as one keymap leg is an option, else oldest-first.
