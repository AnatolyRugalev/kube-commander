# 2026-07-22 — M2-13a: table column-sort primitive

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Split M2-13 (#85) primitive-first (the M2-11a/M2-12a rhythm) and took the
  first slice — the table sort mechanism, no keymap/app wiring. Added sort as a
  view over the authoritative `full` row set, exactly like the filter: `applyFilter`
  now derives `full → filter → sort → measure`, so a sort is re-applied on every
  watch delta (a new row lands in sorted position) and clearing it restores the
  order. Public API on the table Model: `SortBy(visibleCol)` (stable, type-aware;
  same column toggles asc↔desc, a new column starts ascending, out-of-range ignored,
  selection preserved by UID), `ClearSort`, and `SortColumn`/`SortDescending`
  accessors for the header indicator M2-13b will render. `sortCol` is a
  visible-column position or -1 (unsorted); `New` starts at -1 and `SetTable` resets
  it (a sort for one resource's columns must not carry to another). Type-aware only
  where cheap: integer/number compare numerically (9 < 10), everything else —
  including date columns, which print as kubectl ages — as case-insensitive text
  (D94). The unfiltered display path now copies rows into the display slice instead
  of aliasing `full.Rows`, so a re-sort never scrambles the authoritative set.
- Decisions: D94 — column sort is a view over the authoritative set (stable,
  type-aware only for integer/number, reset on SetTable, preserved across deltas);
  M2-13b must wire keymap→`SortBy`/`ClearSort` and not re-implement ordering or sort
  `full.Rows`.
- Files: internal/tui/components/table/table.go (sortCol/sortDesc fields, SortBy/
  ClearSort/SortColumn/SortDescending, sortRows in applyFilter, isNumericColumn/
  compareCells, New/SetTable resets), internal/tui/components/table/table_test.go
  (9 sort tests), vault/knowledge/decisions.md (D94), vault/tasks/board.md,
  vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Component-only,
  hermetic — new tests drive SortBy/ClearSort over in-memory tables (text + numeric
  columns, watch delta, SetTable reset, selection-by-UID, out-of-range, empty). No
  runtime surface changed (no keymap binding, no app call yet), so the running binary
  is unaffected — the sort is not reachable until M2-13b wires it.
- Next: M2-13b — register the `sort.*` keymap action(s), route to `table.SortBy` on
  the table's current/selected column (+ `ClearSort`), and render the header sort
  indicator from `SortColumn`/`SortDescending`; regenerate `docs/keybindings.md`.
  Then M2-13 closes #85. (M2-14b still blocked on M3 confirm wiring, D88.)
