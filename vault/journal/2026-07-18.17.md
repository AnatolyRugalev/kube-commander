# 2026-07-18 — M1-01: client-go bootstrap (clientset + dynamic + discovery + RESTMapper)

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Added `internal/kube/client.go` — the client-go handle the kube layer
  builds on. `ClientConfig{Kubeconfig, Context}` → `RESTConfig` (clientcmd default
  rules + context override, non-interactive, wrapped errors) → `NewClients`
  (clientset, dynamic, discovery, deferred discovery RESTMapper), plus a `Connect`
  convenience. No network I/O at construction; the RESTMapper is deferred +
  mem-cached so bootstrap never blocks first paint (D8). Hermetic tests via a
  two-context temp kubeconfig + a dummy rest.Config (D18); no new deps.
- Decisions: D29 (client bootstrap shape; deferred RESTMapper; construction is
  local/no server call).
- Files: `internal/kube/client.go`, `internal/kube/client_test.go`,
  `vault/knowledge/decisions.md`, board, milestone, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓  (envtest N/A — gate off)
- Next: **M1-02** — seed-set core GVKs with static REST mapping for instant start
  (a static mapper composed with the deferred discovery mapper, so core resources
  are mappable before full discovery finishes).
