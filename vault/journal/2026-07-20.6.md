# 2026-07-20 — M2-07c: wire the live table to kube.Watch

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Wired the menu's drill-in (`menu.ResourceSelectedMsg`) to a live
  `kube.Watch`, streaming its deltas into the table through the M2-02 watch pump
  (`internal/tui/app.go`). The root model gained a narrow `ResourceWatcher` seam
  (`*kube.Clients` satisfies it; nil → watch-inert) injected via a new functional
  option `WithWatcher` on `New`/`NewWithKeymap` (existing call sites unchanged).
  Selecting a resource cancels the previous watch, blanks the table
  (`SetTable(kube.Table{})`), opens a fresh `context.WithCancel`-scoped watch —
  whose first RESET repopulates the table (Watch lists internally, so no separate
  List) — and moves focus to the table. Pump messages are wrapped as
  `watchMsg{gen, msg}` and generation-guarded (`watchGen`) so a delta from a
  superseded watch (its channel still draining after cancel) is dropped rather than
  applied to the new resource or used to re-issue a pump onto the current channel.
  `ResourceEventMsg`→`table.ApplyEvent` + re-issue; watch `ErrorMsg`→re-issue (loop
  retries/re-lists on recovery); `WatchClosedMsg`→clear channel, end chain.
  `app.quit` now cancels the current watch before quitting.
- Decisions: D63 — ResourceWatcher seam + WithWatcher option; drill-in starts a
  live watch (first RESET repopulates, focus→table); generation-tagged watch-pump
  messages drop stale deltas (mirrors the D61 seqGen guard); ns "" until M2-08.
- Files: `internal/tui/app.go` (ResourceWatcher/Option/WithWatcher, watch fields,
  `watchMsg`, `selectResource`/`pumpWatch`/`handleWatchMsg`, quit-cancels-watch,
  doc), `internal/tui/tui_test.go` (fakeWatcher + 5 tests: start+RESET populates,
  inert-without-watcher, cancels-previous+stale-drop, closed-stops-chain,
  start-error surfaces), `vault/knowledge/decisions.md` (D63), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); `-race` clean
  on the tui package. Behavior driven end-to-end through the real Update loop with a
  fake watch channel: drill-in → Watch called for the right resource, focus→table,
  RESET event populates the table ("pod-a"); a second selection cancels the first
  ctx and a stale-gen delta is dropped while the current RESET populates; closed
  channel stops the chain; a Watch start error surfaces an `ErrorMsg`.
- Next: **M2-07d** — kick off async discovery on `Init`, route `DiscoveryReadyMsg`
  into the menu (M2-05b `Reconcile`) + drive the status-bar spinner (a
  `Discoverer` seam / option mirroring `WithWatcher`).
