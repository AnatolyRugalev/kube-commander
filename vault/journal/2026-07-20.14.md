# 2026-07-20 — M2-08c: wire the namespace picker into the app shell

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Wired the M2-08a/08b modal picker into the root shell as a working namespace
  switcher, completing M2-08. Registered a new `ns.switch` action (default `ctrl+n`,
  its own `ns` help column / doc section) in the keymap registry. Added a narrow
  `NamespaceLister` seam (`Namespaces(ctx) ([]string, error)`) injected with
  `WithNamespaceLister`, mirroring `WithWatcher`/`WithDiscoverer`; `*kube.Clients`
  satisfies it via a new `kube.Namespaces` (sorted CoreV1 list, graceful error).
  `ns.switch` opens the picker and kicks off the list **off the update loop**
  (`namespacesLoadedMsg`) so opening never blocks on the network; a list failure
  closes the picker and surfaces a classified `ErrorMsg` (principle 3). While the
  picker is open the shell captures **all** input before the sequencer and routes it
  per D72's split (text/edit keys → `UpdateFilter`, mapped no-text keys → picker
  `Action`). A `picker.SelectedMsg` closes the picker, sets `m.namespace` +
  `status.SetNamespace`, and re-scopes the live watch by re-selecting the tracked
  `current` resource (M2-07c's watch reads `m.namespace`); `CancelledMsg` just hides
  it. Nil lister → namespace-switch-inert (action no-op). Regenerated
  `docs/keybindings.md` (drift-guarded).
- Decisions: D73 — `ns.switch`/`ctrl+n` + `NamespaceLister` seam; async list;
  selection re-scopes the current watch; concrete realization of D72's filter-key
  routing; open picker captures all input.
- Files: `internal/tui/keymap/keymap.go` (action + binding), `internal/tui/app.go`
  (seam/option, picker field + current-resource tracking, open/load/select/cancel
  handlers, input routing, sizing, overlay render), `internal/kube/namespaces.go`
  (+ test), `cmd/kubecom/run.go` (wire `WithNamespaceLister`), `docs/keybindings.md`
  (regenerated), `internal/tui/tui_test.go` (fakeLister + 6 tests),
  `internal/tui/keymap/help_test.go` (3 namespaces now), `vault/knowledge/
  decisions.md` (D73), `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`,
  this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Drove the
  whole switch through the real Update loop: `ctrl+n` opens the picker + issues the
  list; the `namespacesLoadedMsg` seeds it; `/` opens the filter, typing "mon"
  narrows to one; enter selects "monitoring", closes the picker, sets `m.namespace`,
  and re-issues `Watch` with `namespace="monitoring"`; esc cancels without changing
  scope; the open picker captures nav (menu cursor stays put); no lister → inert;
  a list error closes the picker with an `ErrorMsg`. Live-cluster dogfood of the
  real namespace list + re-scoped rows still awaits a human pass (D68, sandbox has
  no cluster).
- Next: **M2-09** — table filter (`bubbles/textinput`): `/`-triggered
  (`app.filter`/`app.searchNext`/`app.searchPrev`) client-side filter over the
  current table's rows, `n`/`N` next/prev match. Depends on M2-06, M2-07 (done).
