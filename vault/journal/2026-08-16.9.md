# 2026-08-16 — STORY-06g-2b-2: the cross-kind unhealthy wiring (`U` → `kube.Scan` over the menu's kinds)

- Agent: opencode (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: The second half of STORY-06g-2b (D278 pt 3) — the wiring that makes the
  cross-kind unhealthy list reachable, closing the S02 miss (a failure that is
  not a pod — a stuck PVC — was invisible to a pod-first walk) with the gesture
  the board and the feedback asked for: **`U` (`app.unhealthyScan`) opens
  `components/unhealthyview` and runs exactly one `kube.Scan`** over the kinds the
  menu currently offers, with `table.UnhealthyRow` as the row predicate — the
  concrete seam D276 named, the same `classifyCell` the per-kind `H` filter uses,
  so the list and the filter cannot disagree about what is broken (D275 pt 1). A
  **`Scanner` seam mirrors `Searcher`** on the `Cluster` bundle (`WithScanner`,
  nil → scan-inert, so an unwired `U` opens nothing). The sweep runs once on open
  over every offered kind (no curated subset — the whole point is that the broken
  thing may be a kind the operator is not looking at) in the app's own namespace,
  capped at `scanHitLimit` = 200, over a **generation-guarded pump**
  (`scanGen`/`scanCancel`/`scanCh` mirror search's, D140 pt 3), and `stopScan`
  joins the `stopClusterAsync` inventory so it dies on quit and on a context
  switch. Each **drill-in closes the list and switches browse** to the hit's kind
  through `selectResource`, stashing the hit's row object as the *same* pending
  selection fields the search drill-in leaves behind (`searchTarget`/
  `hasSearchTarget`), consumed by `applyPendingSelect` when the fresh watch's
  first RESET lands — one mechanism, two drill-ins. The view **routes like the
  logs view with its grep closed** (it has no text field): keys resolve through
  the browse keymap and `handleAction` intercepts `unhealthyView.Active()`,
  `q` closes the view, and it owns a new `HelpUnhealthy` hint context (D143
  pt 1). The keymap's `app.unhealthyScan` takes `U` and the doc + README are
  updated; the S02 feedback `2026-08-15-pod-first-blinds-non-pod-failures.md` is
  deleted (D69).
- Decisions: **D279** — the sweep is wired as a one-shot scan on `U` over the
  menu's kinds (the `Scanner` seam mirrors `Searcher`; the pump mirrors search's
  generation guard; drill-in reuses the search pending-selection mechanism;
  the view routes like the logs view and owns a `HelpUnhealthy` hint context).
- Files: internal/tui/unhealthy.go (new, the wiring), internal/tui/unhealthy_test.go
  (new), internal/tui/app.go (fields, New, update cases, handleAction, hintContext,
  resize, View, overlayActive, stopClusterAsync), internal/tui/cluster.go +
  cluster_test.go (Scanner seam on Cluster), internal/tui/msg.go (ScanEventMsg/
  ScanClosedMsg/scanPump), internal/tui/keymap/keymap.go + help.go
  (`app.unhealthyScan` `U`, `HelpUnhealthy`), internal/tui/keymap/keymap_test.go +
  hint_test.go (reachability), docs/keybindings.md (regenerated), README.md,
  vault/feedback/2026-08-15-pod-first-blinds-non-pod-failures.md (deleted),
  vault/tasks/board.md, vault/milestones/M5-release.md, vault/knowledge/decisions.md,
  this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. New hermetic
  tests pin the wiring: scan-inert without a scanner (the key opens nothing);
  open runs exactly one sweep over the menu's kinds in the app's namespace under
  the cap, marking the view in-flight with the kind count and streaming the hits
  in (Pod *and* PVC); the pump delivers kind-done progress and the terminal
  ScanDone clears in-flight; a capped sweep reports the cap; an empty scope
  degrades to an idle list with no scan; drill-in closes the list, starts a watch
  for the hit's kind, focuses the table and arms the pending object selection;
  ClosedMsg dismisses and cancels the sweep; `q` closes the view without quitting;
  and `HelpUnhealthy` is reachable through the real gesture (hint test). **Live
  sanity**: the binary builds and launches (`kubecom --help`). The new surface is
  launch-and-navigate-only in `make check`; pressing `U` against a real cluster
  and drilling into a hit is a human's dogfood, noted in the journal rather than
  claimed (D79).
- Next: **STORY-06j** — the logs set: a painted follow indicator
  (`2026-08-15-logs-follow-visual-signal.md`, Priority high), scrolling past the
  last line re-arms follow (`2026-08-15-logs-scroll-past-end-resumes-follow.md`,
  Priority high), and the newest-first order weighed
  (`2026-08-15-logs-newest-first-order.md`); or **STORY-06h** (the rich describe
  panel, Priority high) — the two remaining high-priority fold-in items after
  this leg, with 06j's two fixes smallest first.
