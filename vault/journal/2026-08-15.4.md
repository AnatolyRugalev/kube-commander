# 2026-08-15 — STORY-04: the stories are written, and a story is now the way to argue for a UX change

- Agent: claude-opus-5 (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: Wrote the five stories as `stories/s01…s05-*.md`, one user path each — first contact
  on an unknown cluster (S01), five failures in `broken` with five different answers (S02,
  marked the **main story**), finding the failures in a chatty live stream (S03), scale /
  restart / hand-edit (S04), and finding an object when you only half-remember its name
  (S05). Each story is a goal a user has, not a feature tour: a metadata block (main-story
  flag, the fixture it assumes, the trace path), **The situation**, **Your goal**, and
  **When you're done** (what to report back). None of them names a key — that is the
  measurement, and a guard enforces it. New `internal/stories/story_test.go` keeps the
  shape honest: every story carries the three sections and names its fixture
  (`TestStoriesHaveTheirSections`), none of them leaks a key in its prose
  (`TestStoriesNameNoKeys` — patterns deliberately narrow: a backticked 1–2-char token, a
  named key, a press instruction; a guard that cries wolf gets deleted), and exactly one
  story is marked the main story (`TestExactlyOneMainStory`), so TAPE-01 and DOC-04 cannot
  each pick a different one. The maintainer's ask is folded into the same leg:
  `CONTRIBUTING.md` gains "Arguing for a UX change: write a story" — an opinion can only be
  agreed or disagreed with, a story can be *walked by somebody else*, and a story that
  stalls two people is a finding; the trace makes it evidence rather than an anecdote, a
  press resolving to no action is the dead-end signal, and the README's contribution
  paragraph now points there. `stories/README.md` grows a story table and points the trace
  at `KUBECOM_KEYLOG` (set once, survives relaunches — STORY-02's env var) rather than a
  per-invocation `--keylog`.
- Decisions: none — D268 pt 1 already fixes the shape (the goal in the user's words, the
  fixture state, what to observe, never the keys to press; one main story the tape and
  docs are cut against) and this leg implements it. The two guard-test choices above are
  implementation notes, not new constraints (D67).
- Files: stories/s01-first-contact.md, stories/s02-the-failing-pod.md,
  stories/s03-read-the-logs.md, stories/s04-change-something.md, stories/s05-half-a-name.md,
  stories/README.md, CONTRIBUTING.md, internal/stories/story_test.go, README.md,
  vault/tasks/board.md, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. The three new guards were
  driven red first where the shape let them be: a story without a section, a prose line
  naming a key in backticks, and two stories marked main — each produced the intended
  message. The story text itself is not verifiable by machine beyond the shape (D79), but
  the maintainer walks them as STORY-05 and the words are theirs to judge. Diff ~500 lines,
  over the ~300 guideline — 5 × ~30 lines of story prose and ~140 lines of guard; the
  stories are the deliverable and the guard is what keeps them honest, so it landed whole.
- Next: **STORY-03** — the trace analyzer (`kubecom keys analyze <trace.jsonl>`), now the
  only thing between the maintainer and walking the stories. Reading a few traces by hand
  is the way to learn what it should count, but the analyzer is what makes the loop
  repeatable.
