# 2026-07-19 — M1-07b: describe (kubectl/pkg/describe)

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Landed the second in-process viewer in a new `internal/kube/describe.go`.
  `Clients.Describe(r, ref)` renders `kubectl describe`-identical output by reusing
  kubectl's own describe generators (`k8s.io/kubectl/pkg/describe`). Selection
  mirrors `describe.NewDescriber`: the specialized built-in describer keyed by
  `GVK.GroupKind()` (`DescriberFor`), falling back to the generic unstructured
  describer (`GenericDescriberFor`) for CRDs/rarer built-ins — kept as a small local
  `describerFor` since we already hold the `*rest.Config`. The generic describer's
  `meta.RESTMapping` is built directly from the discovery `Resource` (pure
  `restMappingFor`), not re-resolved through the RESTMapper. No context param —
  kubectl's describe package has no context-aware entry point (uses `context.TODO`
  internally). Empty name rejected, errors wrapped (#86).
- Decisions: D42 — describe via kubectl/pkg/describe; dep pinned to v0.31.4 (a bare
  `go mod tidy` resolves to v0.36 and drags the whole k8s graph up); describer
  selection mirrors NewDescriber; RESTMapping built from the Resource; no ctx.
- Files: `internal/kube/describe.go`, `internal/kube/describe_test.go`,
  `go.mod`/`go.sum` (k8s.io/kubectl v0.31.4 direct + ~12 transitive indirect),
  `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Describer
  selection + mapping + empty-name are hermetic; live describe output is envtest
  territory (like the watch live-server exercise).
- Next: **M1-07c** — streaming pod logs (`Clients.Logs`): follow/tail via the typed
  clientset, reconnecting stream à la watch (D34), stream to a channel. Then M1-08
  background port-forward, M1-09 typed graceful errors. (M1-04b lazy group detail
  stays parked until the M2 menu exists.)
