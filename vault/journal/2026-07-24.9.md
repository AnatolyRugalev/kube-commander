# 2026-07-24 — HT-exec-dogfood: close exec live-cluster dogfood human-task

- Agent: claude-opus-4-8
- Milestone: M3
- Did: The exec live-cluster dogfood human-task
  (`2026-07-24-exec-live-cluster-dogfood.md`, raised by M3-14b-1) came back
  `Status: done` — the maintainer confirmed the Exec-shell action works end-to-end
  against a real cluster in a real terminal: the shell drops in and behaves, and the
  TUI restores cleanly on `exit`. Per the human-task-closure rhythm (D79), folded the
  `## Result` into the board/milestone and closed the file: ticked the M3 exec exit
  criterion (`Exec drops into a working shell and restores the TUI afterward`),
  recorded the Done board entry, and deleted the task file. No code changed — this is
  a bookkeeping/verification-closure leg. (The Result also noted the Delete action was
  confirmed working in the same pass; other dogfood findings — port-forward, keymap,
  viewers, logs — were filed separately in `vault/feedback/` and remain the next work.)
- Decisions: none (no new load-bearing constraint; D79 already governs closure).
- Files: vault/milestones/M3-actions-viewers.md (tick exec exit criterion),
  vault/tasks/board.md (In Progress→Done, Last updated), vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md (deleted),
  vault/journal/2026-07-24.9.md (new)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`) — docs-only leg, no
  runtime surface touched, binary unchanged (no dogfood regression).
- Next: drain the feedback inbox — 6 items dated 2026-07-24 preempt the board (D69).
  Oldest/highest-priority first; e.g. `confirm-modal-yn-keys`, `delete-default-key-d`,
  `unify-yaml-view-and-edit` (relates to M3-15b), `port-forward-picker-and-local-port`,
  `logs-dedicated-view-live-grep`, `cluster-search-multi-resource`. After the inbox is
  empty, M3-15b (Edit TUI suspend wire) is the top unblocked board item.
