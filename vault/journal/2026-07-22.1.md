# 2026-07-22 — M2-10: confirm/prompt modal component

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Added `internal/tui/components/modal` — a Kind-stamped, centered/bordered
  overlay replacing the original's racy tcell popup. Two modes on one Model:
  `ShowConfirm` (yes/no) and `ShowPrompt` (single-line text over an owned
  `textinput`). Driven only through resolved keymap actions (nav.drillIn accepts,
  nav.back declines — Enter/Esc, the modal convention); emits its own
  `ConfirmedMsg`/`CancelledMsg` (no root import, D56); no shared mutable state
  (principle 1). Sibling of the picker (D65): same geometry/overlay approach,
  `UpdatePrompt` as the single raw-key entry point gated by `Prompting()`.
  Component-only — app wiring lands with the M3 action that needs a confirm.
- Decisions: D88 — modal resolves accept/decline via nav.drillIn/nav.back, no
  dedicated y/n actions or raw-key matching; component-only until an M3 action wires it.
- Files: internal/tui/components/modal/modal.go, internal/tui/components/modal/modal_test.go,
  vault/knowledge/decisions.md, vault/tasks/board.md, vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: M2-11 (config: menu customization persistence) — extend the M2-01c Config
  struct with menu/browse fields + Save, wire load-on-start/persist. Or M2-13
  (column sort, #85) if a smaller slice is preferred.
