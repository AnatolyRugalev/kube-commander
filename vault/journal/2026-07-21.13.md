# 2026-07-21 — FB-menu-config-02: map & merge per-context menu extras into the seed

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Second slice of feedback `2026-07-21-02` (D83) — the component-level **merge**
  of per-context menu customizations. Slice-01 (FB-menu-config-01) added the config
  loader (`config.MenuConfig`/`MenuResource`, per-context file); this slice teaches the
  menu package to fold those entries in. New `menu.AddExtras([]config.MenuResource)`:
  `extraItem` maps one entry to a resource `Item` (GVK/GVR built from
  group/version/resource, `Namespaced`; Title falls back Title→Kind→Resource; Section
  defaults to the trailing `Custom Resources` bucket where discovered CRDs also land;
  `Available: true`, optimistic like a seed row). `insertExtra` places each survivor
  **after the last item of its section**, so the D77 one-header-per-section contiguity
  invariant `rows()` relies on still holds — an extra naming an existing section joins
  that run, an extra with a new section appends and starts its own contiguous run, and
  the non-resource namespace seam (Section "") is never split. Dedup is by GVR against
  seed rows, earlier extras in the same call, and — because extras land in `m.items`
  before the first discovery pass — any later discovered twin: Reconcile's existing
  seen-set (D57) already covers every current `ItemResource`, so a discovered resource
  matching an extra fills the extra's twin metadata rather than double-listing. The
  cursor is preserved by re-resolving its GVR/kind after the slice grows, mirroring
  Reconcile. No app wiring yet — loading the per-context file and calling `AddExtras`
  on start is FB-menu-config-03.
- Decisions: none. No new binding constraint — the merge follows the existing D57
  dedup/preserve pattern and the D83 contract (map `MenuResource`→row, config stays
  free of menu). The Title-fallback order, the default Custom Resources section, and
  the insert-after-last-in-section placement are local choices recorded here per D67.
  Note: `menu` now imports `config` (config→keymap only, so no cycle; D83's "config
  stays free of kube/menu" makes the reverse edge impossible).
- Files: `internal/tui/components/menu/menu.go` (add `config` import; `AddExtras`,
  `extraItem`, `insertExtra`; package-doc sentence), `internal/tui/components/menu/menu_test.go`
  (map-into-custom, title fallback, dedup-against-seed+itself, sections-stay-contiguous
  + one-header-per-section, preserves-selection, discovered-twin-no-duplicate,
  empty-noop; `assertSectionsContiguous`/`findGVR` helpers), board, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Runtime:
  hermetic — the tests drive `AddExtras` on the real model and assert the mapped rows,
  dedup counts, section contiguity, selection preservation, and the twin-no-duplicate
  round-trip through the real Reconcile. No product wiring changed, so the running
  binary is unaffected (extras are still not loaded until FB-menu-config-03).
- Next: **FB-menu-config-03** — resolve the active context name (`kube.ContextName`,
  D76), `config.LoadMenuFile(config.MenuPath(ctx))`, feed its `.Resources` to
  `menu.AddExtras` on start; missing/malformed file degrades to the default menu
  (toast, never block — principle 3); update README's config section.
