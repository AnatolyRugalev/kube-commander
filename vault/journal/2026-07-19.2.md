# 2026-07-19 — M1-06e-1: drain pod selection

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Split M1-06e (drain) into 06e-1 (pod selection) + 06e-2 (eviction loop)
  and landed the first slice in a new `internal/kube/drain.go`. `Clients.DrainCandidates(ctx, node, opts)`
  lists a node's pods by the `spec.nodeName` field selector via the typed clientset
  (drain is pod/node-specific, so it uses the typed client — not the generic
  dynamic path of 06a–06d) and runs a pure `classifyDrainPods` that mirrors
  `kubectl drain`: skip mirror/terminated/DaemonSet(w/ IgnoreDaemonSets) pods; block
  (as one aggregated refusal, nil slice) standalone-w/o-Force, DaemonSet-w/o-Ignore,
  and emptyDir-w/o-DeleteEmptyDirData pods; evict controller-managed pods. Returns
  `[]ObjectRef` — the apimachinery-free boundary the table layer keeps (D33) and the
  exact input 06e-2's eviction loop consumes. Empty node rejected, list error wrapped (#86).
- Decisions: D39 — drain selection via typed clientset + pure classifier; M1-06e
  split into 06e-1 selection + 06e-2 eviction; DaemonSet by owner-ref kind.
- Files: `internal/kube/drain.go`, `internal/kube/drain_test.go`, `go.mod`
  (k8s.io/api indirect→direct), `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues)
- Next: **M1-06e-2** — drain eviction loop: cordon first (reuse M1-06c), evict the
  DrainCandidates pods via the policy/v1 Eviction API, PDB-aware 429-retry with
  backoff, wait for deletion; public `Clients.Drain`. Then **M1-07** logs/describe/YAML.
