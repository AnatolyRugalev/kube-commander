# 2026-07-19 — M2-PLAN: expand M2 app-shell into leg-sized slices

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Board-expansion leg. With the M2-01 action-registry group complete
  (D47–D51), the rest of M2 was a single prose paragraph on the board — nothing
  pickable. Decomposed the M2 app-shell into ordered, dependency-noted, leg-sized
  Backlog slices **M2-02 … M2-14** (several pre-split): M2-02 msg types +
  channel→msg pumps · M2-03 lipgloss theme/styles · M2-04 status bar · M2-05a/b
  resource menu (seed + discovery reconcile) · M2-06a/b/c custom table (snapshot /
  live watch deltas / hscroll) · M2-07a/b/c/d root app shell (keymap-routed
  skeleton / two-pane browse / live watch wiring / discovery reconcile) · M2-08
  namespace picker · M2-09 filter · M2-10 confirm/prompt modal · M2-11 config menu
  persistence · M2-12 legacy `~/.kubecom.yaml` migration · M2-13 column sort (#85)
  · M2-14 teatest coverage. Package layout follows REWRITE_PLAN
  (`internal/tui/{app,msg}.go`, `styles`, `components/*`, `views/*`); zero shared
  mutable UI state throughout (principle 1). Ordering is a default, not a
  contract — a later agent may re-split any slice that proves too big.
- Decisions: D52 — M2 app-shell decomposed into ordered leg-sized slices;
  rationale, the slicing, and the bottom-up build order recorded.
- Files: `vault/tasks/board.md` (M2 Backlog rewritten; M2-PLAN claimed→done;
  header updated), `vault/knowledge/decisions.md` (D52),
  `vault/milestones/M2-core-tui.md` (breakdown note), this entry. **No code.**
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues — docs-only
  leg, ran as the green gate; nothing to sanity-check at runtime).
- Next: **M2-02** — TUI message types + channel→msg pumps (`internal/tui/msg.go`):
  typed `tea.Msg`s bridging `kube.WatchEvent`/`DiscoveryResult`/`ErrorKind` + the
  `tea.Cmd` adapters that read one channel item and return it as a msg. Pure, no UI
  deps, hermetically testable with fake channels — the first pickable slice, and
  everything downstream consumes these msgs.
