# 2026-07-20 — M2-RUN: bare `kubecom` launches the browse UI against a real cluster

- Agent: claude (Opus 4.8), `/do-rewrite-leg`
- Milestone: M2
- Did: Wired the existing app shell into a runnable program. The root cobra command
  now has a `RunE` (bare `kubecom`) plus `--kubeconfig`/`--context`/`-n`/`--config`
  flags; it resolves the config-merged keymap (as `kubecom keys` does), builds a live
  `*kube.Clients` via `kube.Connect`, and runs `tea.NewProgram(model).Run()` with the
  client injected through `WithWatcher`/`WithDiscoverer` and a new `WithNamespace`
  option. A bad/missing kubeconfig or context fails gracefully via `kube.Classify`
  (KindBadContext → clear message, no panic). Added `cmd/kubecom/logging.go`
  (`setupLogging`): slog + klog routed to a cache-dir log file, klog held fully off
  stderr. `version`/`keys` unchanged; root `Args: NoArgs` keeps unknown subcommands
  an error.
- Decisions: D70 (v2 full-screen is a `View.AltScreen` field set in `Model.View()`,
  not the nonexistent `tea.WithAltScreen()` the board sketch used); D71 (TUI logging
  to a file — `klog.LogToStderr(false)` is insufficient, must also raise
  `stderrthreshold` to FATAL or client-go's memcache ERROR lines corrupt the
  alt-screen).
- Files: `cmd/kubecom/main.go`, `cmd/kubecom/run.go` (new), `cmd/kubecom/logging.go`
  (new), `internal/tui/app.go` (WithNamespace + `View.AltScreen`), `README.md`,
  `vault/knowledge/decisions.md`, `vault/knowledge/stack.md`, `vault/milestones/M2-core-tui.md`,
  `vault/tasks/board.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` PASS). Manual smoke: no
  real cluster in the sandbox, so smoked against a syntactically-valid but offline
  kubeconfig under a sized PTY — the browse UI painted the full seed menu
  (Namespace…StorageClass) + the keymap-derived status bar, entered/exited the
  alt-screen cleanly, and quit on `q` with exit 0. Verified the client-go "couldn't
  get server API group list" error went to the **log file, not stderr** (alt-screen
  intact) and the app degraded gracefully on the unreachable cluster. Also checked
  the graceful path: a missing kubeconfig prints `cannot start: … (check your
  kubeconfig and --context)` and exits 1.
- Next: **M2-08b** — picker filtering (top-unblocked M2 component slice). A human
  dogfood pass of M2-RUN against a live cluster (drill into Pods/Nodes, see live
  rows) is still wanted per D68 — that path couldn't be exercised in the sandbox.
