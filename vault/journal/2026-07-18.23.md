# 2026-07-18 — M1-06a: generic delete action

- Agent: Claude Opus 4.8 (`/do-rewrite-leg`)
- Milestone: M1
- Did: Split M1-06 (6 actions — too big for one green leg, cf. D33) into 06a
  delete / 06b scale+rollout-restart / 06c cordon+drain / 06d cronjob-suspend,
  and landed **06a**: `internal/kube/actions.go` — `Clients.Delete(ctx, r
  Resource, ref ObjectRef, opts)` deletes any resource (built-in or CRD) through
  the dynamic client via a shared `resourceInterface(r, ns)` helper. When the row
  `ObjectRef` carries a UID, a `Preconditions{UID}` guard is layered on (pure
  `withUIDPrecondition`) so acting on a table snapshot never deletes a recreated
  same-named object; NotFound surfaces wrapped (#86). Establishes the in-process
  action layer the rest of M1-06 builds on.
- Decisions: D35 (dynamic-client generic delete; UID snapshot-race guard; M1-06
  split; fake-dynamic-client discards DeleteOptions → precondition logic tested as
  a pure helper).
- Files: `internal/kube/actions.go`, `internal/kube/actions_test.go`, `go.mod`
  (indirect `json-patch.v4` via `go mod tidy`), `vault/knowledge/{decisions,stack}.md`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` — 0 issues)
- Next: **M1-06b** — scale + rollout-restart (workload actions; reuse
  `resourceInterface`; scale via the `scale` subresource, rollout-restart via a
  strategic-merge patch of the pod-template restart annotation).
