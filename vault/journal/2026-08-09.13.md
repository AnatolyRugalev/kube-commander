# 2026-08-09 — M5-09b: screencast tape tuning — matching demo, reproducible reruns, more tour

- Agent: claude-opus (do-rewrite-leg)
- Milestone: M5 (screencast — feedback `2026-08-09-screencast-tape-tuning`)
- Did: The maintainer's three notes on the recorded GIF, all answered in the tape
  (recording stays theirs; the agent share is the edits that make a re-record
  right). (1) **Bad search example**: the cluster-search step typed `dns` and came
  back empty on the dogfood cluster. It now types `shop` — the same string the
  filter step already matched a pod with earlier in the tour — so the cluster-wide
  pass is guaranteed to surface the object already on screen, whatever the search's
  scope does. (2) **Reruns not reproducible**: kubecom's per-context state (CTX-MEM
  pane memory, pins, last namespace) persisted across launches, so a second run
  opened on the remembered pane. The tape now wipes `/tmp/kubecom-screencast` and
  redirects `XDG_CONFIG_HOME`/`XDG_CACHE_HOME` there before every launch (typed in
  the hidden setup section, so it never shows), making each run start from the same
  welcome screen by construction. (3) **Show more**: the tour grew two captioned
  steps — the theme preview (`T`, two `Down`s, `Escape` — showcases THEME-07's live
  repaint and the esc-restores behaviour) and the help overlay (`?`). Header's
  stale TUNE note updated to name the three cluster-specific strings (`shop`,
  `info`, the search query); `make screencast` target comment names the throwaway
  state dir. `vhs validate` clean; the three screencast guards pass unchanged (the
  new presses are annotated and bound).
- Decisions: D261 (a demo query must be one the tour proved matches; reruns start
  from a clean XDG state by tape contract; the tour grows and its keys stay
  annotated).
- Files: docs/screencast/screencast.tape, Makefile, vault/milestones/M5-release.md,
  vault/knowledge/decisions.md, vault/tasks/board.md, deleted
  vault/feedback/2026-08-09-screencast-tape-tuning.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green;
  `TestScreencastTapeMatchesTheKeymap`/`ShowsTheHeadlineActions`/
  `AssetAndReadmeAgree` all pass; `vhs validate docs/screencast/screencast.tape`
  clean
- Next: `vault/feedback/` is empty and the board's only remaining item is M5-11,
  blocked on the deferred release tag (human task `2026-07-30-first-release-tag`).
  All agent-performable work for v1 is done — the next review decides whether to
  tag, and the tape above is ready for a re-record whenever.
