# 2026-08-21 — STORY-06i-2: the relations popup

- Agent: claude-opus (do-rewrite-leg)
- Milestone: M5
- Did: Landed the surface over D286's graph, which is what the S04 feedback
  `2026-08-15-relations-navigation-popup.md` actually asked for, so the file is
  deleted with this leg (D69). `gr` (or the actions menu's new `Related resources`
  entry, gated on `get`) resolves the selected row through a new `Relater` seam on
  the `Cluster` bundle (nil → inert, like `Searcher`/`Scanner`) and opens a modal
  picker of the object's neighbours: rows stably sorted up → down → side with the
  direction's arrow in the picker's name column (a picker has no section headings),
  labelled by the target — `Kind/name`, or `Kind (selector)` for a scope — and
  namespace-qualified when it differs from the browsed scope. A pick opens through a
  path the shell already had: named → `selectResource` + the pending selection the
  search drill-in arms, set-shaped → `selectChildScope` with the popup's source
  object as the owner, so the status-bar scope names it and `nav.back` returns to it.
  A named target in another namespace re-scopes display state first (never
  persisted; an all-namespaces scope is left alone). No neighbours is a notice, a
  failed resolve a toast — neither opens a surface.
- Decisions: D287 — navigating a relation reuses the two paths the shell has, never a
  third; `gr` is the gesture and the popup is a row action; direction groups, the
  label is the pick's identity; empty is a notice.
- Files: `internal/tui/relations.go` (new), `internal/tui/relations_test.go` (new),
  `internal/tui/app.go` (picker + state + routing), `internal/tui/cluster.go`
  (`Relater` on the bundle), `internal/tui/rowaction.go`, `internal/tui/theme.go`,
  `internal/tui/keymap/keymap.go` (`res.relations` = `gr`), `docs/keybindings.md`
  (regenerated), `README.md`, board, decisions, journal.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`). Hermetic only — the
  popup is driven end to end through a fake `Relater` (gesture, grouping, both open
  paths, the re-scope, empty/error/inert), but it has not been exercised against a
  real cluster; it is additive and inert without the seam, so the binary is not
  regressed (D68). The maintainer's next dogfood is where the row set on a real pod
  gets judged.
- Next: **STORY-06i-3** — reverse-selector relations: which Services (and which
  workloads' selectors) match this pod, folded into `kube.Relations`. Unlike every
  06i-1 edge it needs a List of its own, so bound it to the namespace, keep it one
  List, and let a failure degrade that relation away rather than fail the set (D286
  pt 3). The popup needs no change to show them.
