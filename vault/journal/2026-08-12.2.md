# 2026-08-12 — BOARD-03: a Done entry's ID is unique, and now guarded

- Agent: claude-opus-5 (do-rewrite-leg)
- Milestone: — (board hygiene; the BOARD line, reopened)
- Did: Orient-found defect in the canonical record (bug fixes are never gated by an open
  human task, and all three open ones are deferred and gate only M5-11). Two distinct legs
  were indexed as **`DOC-02`**: the 2026-08-09 README prose pass (commits `9e8ef4d`,
  `16c39db`, `c4e6990`, journal `2026-08-09.1`) and yesterday's install-docs leg (`4ce2e3d`,
  `6a9b094`, journal `2026-08-12.1`, D266). D15 makes that id the join key between board,
  journal and commit subject, so `DOC-02` resolved to two unrelated pieces of work. The
  BOARD line's guards could not see it: `TestBoardDoneIndexIsComplete` collects ids into a
  `map[string]bool` and only asks whether a key is present, so a duplicate is
  indistinguishable from the entry it collides with — and one entry's presence silently
  satisfies completeness for *both* working-area lines. Fixed both halves. Data: the older
  leg is now **`DOC-01b`** — it is the prose half of the feedback DOC-01 was the outline half
  of, which is what the board's own a/b convention is for (M4-12a/12b, THEME-04a/04b,
  BOARD-02a/02b), and it was the cheaper side to move, referenced by its Done entry and its
  journal against the newer leg's D266 + M5 exit criterion + human-task update + `Last
  updated:` line. Its commits are *not* rewritten; the renamed entry and a note at the top of
  its journal both record that they say `DOC-02`, so the orphaned subject stays findable.
  Guard: `TestBoardDoneIDsAreUnique` in `internal/vault`, written red against the live
  collision first, and its doc comment states why it cannot be folded into the completeness
  check. Swept the whole index while there: `DOC-02` was the only duplicate.
- Decisions: D267 — an `**ID**` names exactly one leg and is now enforced; completeness
  cannot stand in for uniqueness; a pushed collision is resolved by renaming, never by
  rewriting history; rename the side with fewer references and record the id its commits
  carry, preferring an a/b suffix when the two are halves of one item.
- Files: internal/vault/board_test.go, vault/tasks/board.md, vault/journal/2026-08-09.1.md,
  vault/knowledge/decisions.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. The new guard was
  confirmed to fail on the collision before the rename and to pass after, so it is known to
  bite rather than merely to compile. No runtime surface: this leg touches a test and vault
  markdown only, so the binary is untouched and D68's dogfood clause is N/A.
- Next: nothing on the board is unblocked — M5-11 waits on the stable `v1.0.0` tag
  (`2026-07-30-first-release-tag`, step 2), and the Homebrew/AUR tasks are deferred to it. If
  the maintainer cuts `v1.0.0`, the next leg is that task's step 3: restore
  `go install …@latest` as the primary install line, drop the rc-specific framing D266 pt 3
  made temporary, tick the remaining M5 criteria and close the resolved GitHub issues.
  Otherwise the honest next leg is whatever a review or the feedback inbox surfaces.
