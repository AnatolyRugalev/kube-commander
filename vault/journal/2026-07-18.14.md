# 2026-07-18 — M0-09: README references the vault + decision log

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M0
- Did: Added a "🚧 `v1` is a ground-up rewrite in progress" banner at the top of
  `README.md` (right under the tagline) that links the whole vault — `vault/`,
  `goals.md`, `milestones/`, `tasks/board.md`, `knowledge/decisions.md` (the
  decision log), and `journal/` — plus `CLAUDE.md`, and states that the rest of
  the README still describes the 2020 `master` code until the rewrite reaches
  parity. This closes the last open **M0 exit criterion** ("Decision log + vault
  referenced from the repo README"). The full README rewrite stays deferred to M5
  (per D25); this leg only adds the pointer, keeping the diff docs-only and small.
- Decisions: none (docs pointer; README-rewrite deferral already implied by D25 +
  the M0 milestone).
- Files: `README.md`, `vault/milestones/M0-groundwork.md` (ticked criterion),
  `vault/tasks/board.md` (M0-09 → Done), this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` exit 0, 0 lint issues;
  docs-only change, full gate re-run on the current tree to confirm green).
- Note on the run: this session first tried M0-05, then M0-06 — both were taken by
  concurrent agents (M0-05 landed as `5e99f6e`; M0-06 claimed by `claude-opus`).
  Aborted each lost claim per the skill's race rule and took the next unblocked
  work. All M0 **exit criteria** are now ticked; M0's `Status:` stays
  `in-progress` because the M0-06 goreleaser board item is still in flight
  (its owner flips the milestone to `done`).
- Next: **M0-06** (goreleaser skeleton, Linux+macOS) is the only open M0 board
  item — currently owned by `claude-opus`; once it lands, flip M0 `Status: done`
  and open M1 (start at **M1-00** envtest harness / **M1-01** client bootstrap).
