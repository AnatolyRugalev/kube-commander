# 2026-07-18 — M0-08: sweep remaining legacy files; de-reference goreleaser

- Agent: claude-opus (Opus 4.8)
- Milestone: M0
- Did: Deleted the legacy files M0-07's tree-based deletion missed — `Dockerfile`
  (golang:1.15 + baked-in kubectl, contradicts D2), `get.sh`, `ci/aur/` (old
  binary names, PKGBUILD/.SRCINFO templates, `publish.sh`, and the encrypted
  deploy key `id_rsa.enc`), and `ci/terminalizer/`. The now-empty `ci/` went too,
  including its stale `ci/.gitignore` (`/snap.login`, snap CI already gone in
  M0-07). Patched `.goreleaser.yml` to drop the two blocks that *only* referenced
  the deleted `ci/aur/` (the `aur` archive + the `publishers:` section running
  `ci/aur/publish.sh`), keeping the config self-consistent per D22's precedent;
  the rest of the release matrix (Windows target, brews, kubectl dep, README AUR
  badge) is left for M0-06's Linux+macOS redesign.
  Note: originally assigned M0-04, but a concurrent agent landed M0-04 first
  (remote `a09bb25`, D24) and claimed M0-05; discarded my duplicate and took the
  next unblocked item (M0-08) per the skill's race rule.
- Decisions: D25 — legacy sweep + minimal goreleaser de-reference (scope-limited,
  redesign deferred to M0-06, mirroring D22).
- Files: deleted `Dockerfile`, `get.sh`, `ci/aur/*`, `ci/terminalizer/*`,
  `ci/.gitignore`; edited `.goreleaser.yml`, `vault/tasks/board.md`,
  `vault/knowledge/decisions.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` exit 0, 0 lint issues);
  `.goreleaser.yml` re-validated as well-formed YAML; grep confirms no remaining
  file references to the deleted paths.
- Next: **M0-06** — goreleaser skeleton reshaped to Linux+macOS only (drop the
  `kubecom-windows` build target per D7; revisit brews/AUR + README release docs).
