# 2026-07-20 — M2-14a: teatest coverage (update loop + menu reconcile)

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Split M2-14 (teatest: update loop · menu reconcile · modal flow) into
  **14a** (this leg) and **14b** (modal flow) because the modal slice depends on
  M2-10, which the open dogfood human-task gates. Landed 14a — two tests in
  `internal/tui/tui_test.go`. `TestProgramRoutesKeyToPicker` drives the whole
  update loop through the **real** bubbletea program (teatest/v2, the M0-05
  harness) instead of a direct `Update`: a live ctrl+n keypress routes through the
  keymap to `ns.switch`, the async namespace list seeds the picker, and the seeded
  namespace renders — a full key→action→command→msg→View round-trip through the
  running program (prior teatest use was smoke+quit only). `TestReconcilePreserves-
  Selection` closes a real gap: `TestDiscoveryReadyReconcilesMenu` appends a CRD but
  never moves the cursor first, so the D57 "reconcile preserves selection" guarantee
  (the M2 menu-reconcile risk item) was unasserted — the new test moves the menu
  selection off row 0, then a `DiscoveryReadyMsg` appending a CRD must leave the same
  resource selected (resolved by GVR). Test-only: no product code, binary unchanged.
- Decisions: none (both behaviors are existing — D57 reconcile preservation, D11
  action routing; per D67 a test-coverage leg adds no new constraint).
- Files: `internal/tui/tui_test.go` (+2 tests), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); the two new
  tests also pass under `-race`. (No runtime surface changed — nothing to dogfood;
  the modal half and live-cluster checks still ride the open human-task.)
- Next: **M2-14b** — teatest the modal confirm flow, but it's blocked by M2-10 (the
  modal itself), which the dogfood human-task gates. With M2-10…M2-13 all gated and
  M2-14a done, the M2 board has no unblocked feature work left: the loop is now
  **blocked on the open dogfood human-task** (`vault/human-tasks/2026-07-20-dogfood-
  first-usable-build.md`) — a human dogfood pass unblocks M2-10…M2-13. Absent that,
  only non-M2 backlog remains (e.g. M1-04b lazy group detail, now that the menu exists).
