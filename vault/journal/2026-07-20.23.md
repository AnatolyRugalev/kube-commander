# 2026-07-20 — M1-04b: retire lazy group-detail-on-open as obsolete

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1 (parked remainder) / cross-cutting disposition
- Did: Claimed the top unblocked backlog item — M1-04b "lazy group detail on first
  open" — and, on inspecting it against the realized architecture, **retired it
  won't-do** rather than building it (D81). Grounds: (1) the M2 menu is a *flat*
  Dashboard-sectioned list of resource kinds (D77, hardened after FB-menu-nesting) —
  drill-in starts a *watch*, there is no collapsible group node to "open," so the
  item's premise ("fetch a group's detail when its menu is opened") has no UI, and
  adding one would regress UX (discovered CRDs/extra kinds would disappear until
  their group is expanded). (2) The cold-start intent — non-blocking start without an
  eager blocking full-discovery fetch — is already delivered by the seed set (M1-02),
  async `StartDiscovery` (M1-03), and kubectl-style per-host on-disk discovery caching
  (M1-04, `diskcached`, zero network at construction, per-group detail already
  read/written lazily by the cache). Nothing left to defer without the unwanted
  group-open UI. Vault-only leg: no product code changed; the binary is unchanged.
  With M1-04b retired, M1 has no open feature work (only the deferred envtest item
  M1-INT remains), and M2-10…M2-13 stay gated by the open dogfood human-task.
- Decisions: D81 — M1-04b retired obsolete; do not reintroduce a collapsible-group
  menu / per-group lazy discovery for it without a fresh UX decision superseding D77.
- Files: `vault/knowledge/decisions.md` (D81), `vault/tasks/board.md` (In Progress
  claim → Done one-liner, M1 section note, Last updated), `vault/milestones/
  M1-kube-layer.md` (M1-04 criterion note), this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, docs/vault only — no
  code touched; nothing to dogfood, binary unchanged).
- Next: **No unblocked feature work remains** — the loop is **blocked on the open
  dogfood human-task** (`vault/human-tasks/2026-07-20-dogfood-first-usable-build.md`),
  which gates M2-10…M2-13 (and M2-14b via M2-10). A human dogfood pass against a real
  cluster unblocks the remaining M2 slices. Absent that, the only non-gated,
  non-human items are envtest (M1-INT, needs control-plane binaries / a human) — so
  there is no low-value leg to invent; report the blocker.
