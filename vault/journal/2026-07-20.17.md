# 2026-07-20 — FB-welcome-page: startup welcome page in the right pane

- Agent: claude-opus (Opus 4.8), autonomous feedback leg
- Milestone: M2
- Did: Addressed the human feedback `2026-07-20-welcome-page-on-startup.md` — a bare
  launch showed an empty table before any resource was picked. Added a new
  `welcome` component (`internal/tui/components/welcome`, the fifth `components/*`
  package) that renders app name/version, the `context · namespace` scope (empty ns
  → "all namespaces"), a "Pick a resource on the left to begin." hint, and the
  registry-generated key hints (reusing `help.ShortHelpView()`, so no raw key is
  matched — D11). The root model renders it in the right-pane slot while
  `!m.hasCurrent` and hands the slot to the live table once a resource is drilled
  into (D76). Wired `WithContext`/`WithVersion` options; `run.go` passes the
  resolved context via a new `kube.ContextName` (no network, `""` on failure) and
  `version.Version`. Bonus: the status bar now shows the context name, which was
  wired (`SetContext`) but never set. Content is width-clamped per line so the
  verbose key hints wrap inside the pane instead of overflowing the border.
  **Feedback file deleted this leg.** The second inbox item
  (`2026-07-20-menu-structure-nesting.md`) is left for the next leg: it is
  larger-than-a-leg (menu model + reconcile + nav + render restructuring) and
  warrants its own triage-into-slices leg. Both items are same-date normal-priority
  (no true "oldest"), so picking the fully-completable one keeps this leg clean and
  green.
- Decisions: D76 — right browse pane is a welcome-page/table slot gated by
  hasCurrent; context/version cosmetic via options; kube.ContextName no-network,
  blank-on-failure.
- Files: `internal/tui/components/welcome/welcome.go` (new),
  `internal/tui/components/welcome/welcome_test.go` (new),
  `internal/tui/app.go` (welcome field/options/resize/View wiring),
  `internal/kube/client.go` (ContextName), `cmd/kubecom/run.go` (WithContext/
  WithVersion), `vault/knowledge/decisions.md` (D76), `vault/tasks/board.md`,
  `vault/feedback/2026-07-20-welcome-page-on-startup.md` (deleted), this entry.
  (Milestone file untouched — no M2 exit criterion newly met.)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Rendered the
  welcome component and the full composed browse view end-to-end (menu + welcome +
  status bar): centered content inside the border, key hints wrapped within the
  pane, status bar showing the context. Live-cluster dogfood (real context name,
  drill-in handing the slot to the table) still awaits a human pass (D68) — not
  reproducible in the sandbox.
- Next: **FB-menu-structure-nesting** — triage the menu-nesting feedback into
  concrete board slices (grouping taxonomy: cluster vs namespace; sub-groups
  Workloads/Config/Network/Storage/Access; reference `master`'s grouping) and do
  the first slice; keep discovery reconcile and vim nav intact. With the inbox
  empty, the board fallback is **M2-09** (table filter).
