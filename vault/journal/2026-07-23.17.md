# 2026-07-23 — M3-13a: port-forward start + background lifecycle

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Split the M3-13 port-forward manager into M3-13a (start + lifecycle + stop-all-
  on-exit), M3-13b (listing panel + stop-individual), M3-13c (Service→pod resolution),
  and landed 13a. The Port-forward action now starts a real background forward: a
  `PortForwarder` seam (`WithPortForwarder`, nil → inert) wrapping M1-08's
  `kube.PortForward`, plus a small `ActiveForward` interface (`Ready`/`Done`/`Err`/
  `Ports`/`Stop`) the shell observes instead of the concrete `*kube.PortForward` handle
  (hermetic-fakeable, D18). Because `kube.Clients.PortForward` returns the concrete type,
  the launcher adapts it with a new `tui.PortForwarderFunc` — the first seam needing an
  adapter rather than `*kube.Clients` satisfying it directly. Flow: `rowActionPortForward`
  → `openPortForwardPrompt` opens the D117 prompt-mode modal (shared `mutateRes`/`mutateRef`
  stash) over the selected Pod → `ConfirmedMsg` → `runPortForward` parses the kubectl-syntax
  specs (`8080:80`/`80`/`:80`, space/comma-separated), starts the forward with its own
  `context.CancelFunc`, and tracks it in `m.forwards` (each stamped with a stable
  `forwardSeq` id). Lifecycle is **message-driven, not a pump** (a forward has two edges):
  `waitForward` `select`s on `Ready()`/`Done()` and reports the first; on `forwardReadyMsg`
  the shell reads bound `Ports()`, flashes `forwarding pod/x localhost:8080 → 80`, and arms
  `waitForwardDone` on `Done()`; `forwardDoneMsg` removes the entry (neutral notice on a
  clean stop, error toast on a transport failure, D74). **All forwards are cancelled on
  quit** via a new `stopForwards()` — the third teardown after `stopLogStream`/`stopDrain`.
  Narrowed the Port-forward action to `Pod` only (was `Pod`+`Service`; `kube.PortForward`
  posts to the pod subresource) — Service resolution is M3-13c. No keymap/doc change
  (Port-forward is a menu-only action, no direct key). → D122.
- Decisions: D122 — port-forward is a tracked background handle observed via two-edge
  messages (not a stream pump, not a one-shot); `ActiveForward` interface + `PortForwarderFunc`
  adapter; started behind a ports prompt; Pod-only for now; cancel-all-on-exit.
- Files: internal/tui/app.go (ActiveForward/PortForwarder/PortForwarderFunc seam,
  WithPortForwarder, portForwarder/forwards/forwardSeq fields, portForwardModalKind,
  forward struct + forwardReadyMsg/forwardDoneMsg, openPortForwardPrompt, runPortForward,
  waitForward/waitForwardDone, handleForwardReady/handleForwardDone, forwardByID/
  removeForward/stopForwards, forwardPortsLabel, Update dispatch + handleRowAction +
  handleModalConfirmed cases, quit teardown line),
  internal/tui/rowaction.go (Port-forward applies to Pod only),
  cmd/kubecom/run.go (WithPortForwarder(PortForwarderFunc{…}) wiring, "context" import),
  internal/tui/tui_test.go (fakeForward/fakePortForwarder + openPodTable variadic opts +
  6 tests: OpensPrompt, Lifecycle start→ready→done, BlankPromptDegrades,
  StartErrorDegrades, InertWithoutForwarder, StopsOnQuit),
  vault/knowledge/decisions.md (D122), vault/tasks/board.md,
  vault/milestones/M3-actions-viewers.md, vault/journal/2026-07-23.17.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tui tests
  drive the full path: the intent opens a ports prompt naming the Pod (no forward yet);
  submitting `8080:80` starts a forward addressed to the selected row with the parsed
  specs, tracked once; a Ready fills the bound ports and re-arms the Done wait; a Done
  drops the forward; a blank entry and a start error each degrade to a status-bar error
  and track nothing; no forwarder → inert (no modal, no cmd); quit cancels the running
  forward's context and clears the set. Runtime surface: the actions menu's "Port-forward"
  over a live Pod row now prompts for ports and starts a real background forward, flashing
  the bound `localhost:L → R` when ready. A human live-cluster confirm (an actual forward
  carrying traffic, an OS-assigned `:80` port, a dial/RBAC failure path) is a dogfood
  follow-up (D68), not reproducible in the sandbox — no bare-launch regression (the prompt
  opens only on the action; a nil forwarder stays inert). M3 milestone: the port-forward
  exit criterion stays **unticked** — background start + clean stop-on-exit are done, but
  "are listed" needs the M3-13b panel; Status stays in-progress.
- Next: M3-13b — Port-forward panel: an overlay listing the active forwards with their
  local:remote ports (reads `m.forwards`), stop the selected forward, stop-all gesture.
  Compose over the browse view (D95), no raw-key matching (D11). Ticks the port-forward
  exit criterion once forwards are listed + individually stoppable.
