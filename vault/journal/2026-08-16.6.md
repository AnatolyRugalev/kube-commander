# 2026-08-16 — STORY-06g-2a: the cross-kind unhealthy sweep primitive (`kube.Scan`)

- Agent: opencode (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: Landed the kube-layer half of the cross-kind unhealthy sweep, the other
  half of STORY-06g (D275 pt 3): the S02 miss — the fifth failure is a PVC and a
  pod-first walk never visits it — needs a sweep across kinds, and the sweep
  needs an engine before a surface. **`kube.Clients.Scan(ctx, resources,
  namespace, keep RowFilter, limit)`** fans out one-shot server-side Lists across
  the given kinds and streams `ScanEvent`s: `ScanMatch` per kept row, exactly one
  `ScanKindDone` per kind (a failed List degrades to `Failed`, never aborts —
  principle 3), and a terminal `ScanDone` reporting `Capped` — the same
  guarantees Search established (per-kind isolation D131 pt 3, concurrency bound
  D131 pt 2, progress/terminal events SEARCH-03), reusing the shared
  `searchConcurrency`/`searchChanBuffer` constants and the `rowLister` seam.
  **The predicate is a seam** (`RowFilter func(tbl *Table, row Row) bool`)
  because the M4-06 health classifier is a TUI concern (keyed by column name in
  the table component) and kube must not import tui. **A hit carries the row,
  cells included** (`ScanHit{Resource, Row}`), unlike a SearchHit which carries no
  cells because drilling re-lists: the unhealthy surface must *show* the reason —
  kind · name · namespace · the offending cell — without re-listing every kind.
  Generalized the shared `sendEvent` to a generic `sendEvent[T any]` so both
  fan-outs use one cancellation guard. STORY-06g-2 was split on pickup (D52's
  bottom-up rhythm): 2a (this primitive) lands here, 2b (the surface that feeds
  Scan the M4-06 row predicate and presents navigable hits) goes back to Backlog,
  and the feedback file `2026-08-15-pod-first-blinds-non-pod-failures.md` stays
  with the slice that completes the address (D69).
- Decisions: **D276** — the sweep is a kube primitive over a caller-supplied
  predicate (the classifier stays TUI-side; no second TUI fan-out), a hit carries
  the row cells, and 06g-2 is two slices (2a primitive / 2b surface).
- Files: internal/kube/scan.go (new), internal/kube/scan_test.go (new),
  internal/kube/search.go (generic sendEvent), vault/tasks/board.md,
  vault/milestones/M5-release.md, vault/knowledge/decisions.md, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. New hermetic
  tests pin the primitive across kinds (healthy rows stay out, the broken PVC
  surfaces beside the broken pod), per-kind failure isolation, the hit cap +
  `Capped`, exactly-one-KindDone progress (failed kinds marked), cluster-scoped
  namespace handling, cancellation-without-terminal, the shared concurrency bound
  (the gateLister probe — at most `searchConcurrency` lists in flight), and that
  the hit carries the offending cell. No TUI, install, launch or config surface
  touched, so D68's dogfood clause is limited to `make check` and the binary
  building; the README is unaffected.
- Next: **STORY-06g-2b** — the cross-kind unhealthy surface: feed `kube.Scan` the
  M4-06 row predicate (the `classifyCell` lifted to a row, the one source D275
  pt 1/D164 established) and present the hits — kind · name · namespace · the
  offending cell — navigable to the object, one gesture from anywhere; delete
  `2026-08-15-pod-first-blinds-non-pod-failures.md` when it lands (D69).
