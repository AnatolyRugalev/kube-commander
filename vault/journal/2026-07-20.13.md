# 2026-07-20 — M2-08b: incremental filtering for the modal picker

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Added picker-owned incremental filtering to the M2-08a modal picker
  (`internal/tui/components/picker`). The picker now holds its own
  `bubbles/textinput` over the unfiltered value set and narrows the visible list by
  **case-insensitive substring** — the list's native filter stays disabled (D65), so
  no raw list key leaks. `app.filter` (`/`) opens the field (focus → cursor blink,
  the field steals one row from the list); raw text flows through a new
  `UpdateFilter(tea.KeyPressMsg)` entry point that feeds the textinput and re-narrows;
  `nav.back` while filtering clears+closes the filter and restores the full list (a
  second `nav.back` cancels the picker); `Hide()` also closes the filter so it
  reopens clean. New `Filtering()` accessor exposes the state so the M2-08c app wiring
  can route control keys → actions first and only text keys → `UpdateFilter`. `View`
  renders the `/`-prompted filter line between the title and the list while filtering.
  Component-only slice; no app-shell wiring (that's M2-08c).
- Decisions: D72 — picker filters itself (own textinput, substring match); the
  control/text key split (actions first, text to `UpdateFilter`) as the sanctioned
  D11 exception; back clears-then-cancels; Hide closes the filter.
- Files: `internal/tui/components/picker/picker.go` (textinput field, all[]/applyFilter,
  syncListSize, ActionFilter/ActionBack handling, UpdateFilter, closeFilter, Filtering,
  innerSize/View filter line), `internal/tui/components/picker/picker_test.go`
  (7 new tests), `vault/knowledge/decisions.md` (D72), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry. No new deps (textinput already
  pulled in transitively by M2-08a's list).
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); `-race` clean
  on the picker package. Rendered the sized+seeded picker filtering by "kube": the
  "/ kube" line shows between the title and the three matching kube-* rows (top
  highlighted), with "default"/"monitoring" filtered out. Tests: open+narrow,
  case-insensitive substring, back clears-then-cancels, drill-in selects the filtered
  value, no-match drill-in emits nothing, View shows the filter line + only matches,
  UpdateFilter inert when not filtering, Hide closes the filter.
- Next: **M2-08c** — wire the namespace picker into the app shell: a `ns.switch`
  keymap action + a `NamespaceLister` seam (mirroring `WithWatcher`/`WithDiscoverer`);
  open seeds the picker with namespaces; while `picker.Filtering()`, route control
  keys → actions and text keys → `UpdateFilter`; selection sets `m.namespace` and
  re-scopes the live watch (M2-07c already reads it); regenerate `docs/keybindings.md`.
