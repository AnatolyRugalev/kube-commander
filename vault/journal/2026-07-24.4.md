# 2026-07-24 — M3-14b-1: exec TUI wire (in-process SPDY primary path)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Split the oversized **M3-14b** (exec TUI wire — container-picker reuse + PTY size
  queue + SIGWINCH + kubectl fallback, all needing real-terminal verification) into
  **M3-14b-1** (this leg) plus 14b-2 (multi-container picker reuse), 14b-3 (live SIGWINCH
  resize), 14b-4 (kubectl-binary fallback). Landed 14b-1: the Exec-shell action now
  suspends into an interactive shell in the selected Pod's **default container** with
  `/bin/sh` and restores the browse UI. New `internal/tui/exec.go`: an **`Execer` seam**
  (`WithExecer`; `*kube.Clients` satisfies it via M3-14a's `Exec`), `openExec` →
  **`tea.Exec`** driving an **`execCommand`** whose `Run()` calls the blocking `kube.Exec`
  (D124) off the update loop. Since bubbletea releases the terminal to *cooked* on suspend,
  `Run()` puts the *local* terminal **raw** itself (`golang.org/x/term`, promoted to a
  direct dep) so keystrokes/`^C` pass through to the remote PTY, and seeds the exec's
  **`execSizeQueue`** with the terminal's current size (one seed → session-end on close;
  live SIGWINCH resize deferred to 14b-3). Raw/size handling only engages when stdin is a
  real terminal, so `Run` is hermetically testable with a buffer. `SetStderr` is a no-op (a
  TTY exec folds stderr into stdout). Result reported to the status bar
  (`handleExecDone`): clean exit → neutral notice, any failure → error toast (D74). Inert
  with no execer or an empty ref. Wired `WithExecer(clients)` in `run.go`. → D125.
- Decisions: D125 — exec runs `kube.Exec` inside a `tea.Exec` `ExecCommand` that owns local
  raw mode (x/term, terminal-only); size queue seeds one size then session-end; this slice
  execs the default container with `/bin/sh` (picker reuse → 14b-2, SIGWINCH → 14b-3,
  kubectl fallback → 14b-4).
- Files: internal/tui/exec.go (new — Execer/WithExecer, openExec, handleExecDone,
  execDoneMsg, execCommand + newExecCommand, fileFd, execSizeQueue), internal/tui/app.go
  (execer field, handleRowAction rowActionExec case, execDoneMsg dispatch),
  cmd/kubecom/run.go (WithExecer wiring), internal/tui/exec_test.go (new — fakeExecer,
  podExecModel, TestExec{OpensSession,InertWithoutExecer,InertOnEmptyRef}, TestExecCommand
  Run{StreamsTTYShell,PropagatesError}, TestExecDoneReportsResult, TestExecSizeQueue
  {SeedThenEnd,DropsDegenerateSeed}), go.mod (x/term indirect→direct), vault/knowledge/
  decisions.md (D125), vault/tasks/board.md,
  vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md (new), vault/journal/2026-07-24.4.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Tests drive the
  hermetic surface without a cluster/TTY: the exec intent issues the tea.Exec suspend and
  opens no confirm modal; it is inert with no execer wired and on an empty ref; the
  `execCommand` adapter (non-terminal stdin → raw/size skipped) calls `kube.Exec` exactly
  once on the addressed pod's default container with `TTY:true`, `/bin/sh`, and the
  captured stdin/stdout attached (stderr nil), propagating an executor error; `handleExec
  Done` surfaces a neutral notice on clean exit and an error toast on failure; the size
  queue delivers the seeded size once then nil after close (idempotent), dropping a 0×0
  seed. The **live interactive exec** — raw-mode passthrough, an actual shell dropping in
  and the TUI restoring on exit, resize — needs a real terminal against a real pod, which
  the cluster-less sandbox cannot do, so it is **not verified here**: raised
  `vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md` (Blocks: none — advisory).
  The M3 exec exit criterion stays **unticked** pending that human run; Status stays
  in-progress. No bare-launch regression: the action only fires from the Pod actions menu
  and is inert until picked.
- Next: M3-14b-2 — multi-container picker reuse for exec: a multi-container Pod prompts
  which container to exec into (reuse the M3-07a `ctrPicker`, disambiguated by purpose
  logs↔exec); a single-container Pod execs directly (14b-1 always uses the default).
