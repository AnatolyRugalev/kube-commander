# 2026-07-24 — M3-15a: Edit — kube-layer apply/update primitive

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Split M3-15 (Edit → `$EDITOR`) into the kube primitive (this leg, mirroring
  the M3-14a rhythm) and the TUI suspend wire (M3-15b, back on the Backlog). Added
  `Clients.Update` (`internal/kube/apply.go`): the write half of Edit — parse the
  bytes coming back from `$EDITOR` (YAML→JSON→unstructured via `obj.UnmarshalJSON`,
  so integers decode as int64) and PUT-update the object through the generic dynamic
  client (built-ins + CRDs, no kubectl binary, D2). The edited buffer still carries
  `metadata.resourceVersion` (GetYAML strips only managedFields), so the PUT gets
  optimistic concurrency for free — a concurrent change → Conflict, not a clobber.
  Identity is guarded before any request: `parseEdited` rejects empty/null/invalid
  content; `guardIdentity` rejects an empty/renamed name or a mismatched namespace
  (a namespaced edit with an empty namespace is filled from the ref) — a botched edit
  never mutates the wrong object or wipes this one. No-change detection is left to the
  caller (M3-15b never calls Update on a no-op exit). → D129.
- Decisions: D129 — Edit applies via a client-side Update (PUT), not SSA; identity
  (name/namespace) is guarded, not editable; no-change is the caller's job.
- Files: internal/kube/apply.go (new: Update, parseEdited, guardIdentity),
  internal/kube/apply_test.go (new: round-trip int64/label apply, cluster-scoped
  namespace fill, invalid-YAML/empty/null rejection without mutation, name+namespace
  mismatch, no-name, NotFound wrapped), vault/knowledge/decisions.md (D129),
  vault/tasks/board.md (M3-15 split → M3-15a done / M3-15b backlog),
  vault/journal/2026-07-24.8.md (M3 milestone unchanged — the Edit exit criterion
  stays unticked until M3-15b wires the suspend flow)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  fake-dynamic-client coverage, no cluster: an edited Deployment round-trips (replicas
  3 as int64, marker label applied); a cluster-scoped Node edit with no namespace fills
  from the ref; invalid YAML / empty / whitespace / `null` are all rejected and leave
  the seeded object untouched; a renamed name and a mismatched namespace are refused
  before any request; a nameless edit is refused; a missing object surfaces a wrapped
  NotFound. No runtime surface changed — this is a pure kube-layer primitive, inert
  until M3-15b wires it, so no bare-launch regression.
- Next: M3-15b — Edit TUI wire: `res.edit`/`e` → `GetYAML` → temp file →
  `tea.ExecProcess $EDITOR` → read back → `Editor` seam (`kube.Update`); no-change
  (edited == original) and parse-error degrade without mutating; result to the status
  bar; raise a real-terminal dogfood human-task like exec (D125). After it, the M3
  Edit exit criterion is wired pending that dogfood.
