# 2026-08-09 — HT-relns-0809: fold in the release-namespace decisions; Homebrew tap moved, containers dropped

- Agent: antigravity
- Milestone: M5 (human-task fold-in)
- Did: Folded in the maintainer's answers to `2026-08-07-release-namespaces-after-org-move`
  and closed that task.   (1) **Homebrew** — moved the cask to the org-level tap
  `neuroplastio/homebrew-tap` (`brew tap neuroplastio/tap`, D253): created that repo
  (first tool: kubecom) with a README on its default branch, retargeted `.goreleaser.yml`
  (`owner: neuroplastio, name: homebrew-tap`), the release workflow comment, the README and
  `docs/install.md`, and the drift guards. (2) **Containers** — dropped container builds
  (D254, supersedes D184): deleted the `Dockerfile` and `internal/version/docker_test.go`,
  removed `dockers_v2:` from `.goreleaser.yml`, the buildx/login steps and `packages: write`
  from `release.yml`, and every container install path/mention from the README,
  `docs/install.md`, `stack.md` and the M5 milestone. (3) **AUR** — unaffected (D253 pt 3);
  noted in the AUR human task that Actions secrets do not survive a repo move, so
  `AUR_SSH_PRIVATE_KEY`/`HOMEBREW_TAP_TOKEN` may need re-adding on `neuroplastio/kubecom`.
  Updated `2026-07-30-homebrew-tap-access` (now just the token; the old tap's stale formula
  is moot) and `2026-07-30-first-release-tag` (container steps removed, two publishers,
  new tap address). The 2020 `AnatolyRugalev/homebrew-kubecom` tap is abandoned with no
  redirect, as the maintainer chose.
- Decisions: D253 (org tap), D254 (containers dropped)
- Files: .goreleaser.yml, .github/workflows/release.yml, README.md, docs/install.md,
  vault/knowledge/{decisions.md,stack.md}, vault/milestones/M5-release.md,
  vault/tasks/board.md, vault/human-tasks/{2026-07-30-homebrew-tap-access,
  2026-07-30-aur-package-access,2026-07-30-first-release-tag}.md, internal/version/
  homebrew_test.go, internal/vault/doc.go, deleted: Dockerfile,
  internal/version/docker_test.go, vault/human-tasks/2026-08-07-release-namespaces-after-org-move.md
  (external: created neuroplastio/homebrew-tap with README on its default branch)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: `2026-07-30-homebrew-tap-access` narrows to the token (human); agent-side, the top
  unblocked Backlog item — CTX-MEM-04 stays deferred, so e.g. a THEME-05 follow-up palette
  or the next M5/M4 item.
