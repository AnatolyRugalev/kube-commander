# 2026-07-22 — FB-watch-unsupported-list-only: watch degrades to list-only for un-watchable kinds

- Agent: claude-opus-4-8
- Milestone: M1 (kube layer) / dogfood feedback
- Did: Addressed dogfood feedback `2026-07-22-watch-unsupported-resource-list-only`
  — selecting a kind without the `watch` verb (e.g. `componentstatuses`) blanked
  the view and retry-looped an unsupported watch. `watchLoop` now degrades to a
  periodic List+RESET (`listPollInterval`, 10s) instead: (1) when the discovered
  `Verbs` are known and lack `watch` it never opens a stream; an **empty** verb set
  stays "unknown" so seed-menu core kinds keep watching pre-discovery; (2) a server
  **405 MethodNotAllowed** on the watch request flips the loop to that same
  list-only mode (via `apierrors.IsMethodNotSupported`) instead of parading the
  ERROR — the backstop for incomplete discovery verbs. Deleted the feedback file.
- Decisions: D104 — watch degrades to list-only polling (verb-driven + 405 backstop;
  empty verbs = unknown, still watchable).
- Files: `internal/kube/watch.go` (loop + `listPollInterval` + `apierrors` import),
  `internal/kube/watch_test.go` (two new tests + fixup of `TestWatchLoopListThenWatch`
  to declare watchable verbs), `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/feedback/2026-07-22-watch-unsupported-resource-list-only.md` (deleted).
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (make check green; two new tests
  `TestWatchLoopListOnlyKindListsWithoutWatching`,
  `TestWatchLoopServerRejectsWatchDegradesToListOnly` pass). Runtime UX against a
  real cluster (componentstatuses actually shows rows) still wants a human dogfood,
  but the behavior is covered hermetically.
- Next: M2-14b (teatest modal confirm flow) once a confirm is reachable through the
  running program, or expand M3 board items.
