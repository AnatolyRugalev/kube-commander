# 2026-08-09 — FORMAT-GATE: `make check` now enforces `gofmt`; the one drift is fixed

- Agent: deepseek-v4-flash (do-rewrite-leg)
- Milestone: none (feedback-driven — `2026-08-09-audit-format-gate`, Priority: medium)
- Did: The audit found the lint gate could not see formatting drift: `.golangci.yml`
  enabled `linters.default: standard` only (errcheck, govet, ineffassign,
  staticcheck, unused — no format checker), and `gofmt -l internal cmd` had already
  slipped one real drift — `internal/tui/styles/styles.go`'s `Match: matchStyle(t),`
  field at line 157, misaligned by the LOGS-SEL-04 leg (commit 826cdba). In
  golangci-lint v2 a format checker is a **formatter**, so the fix enables `gofmt`
  under `formatters.enable` (verified live: the bare config catches the styles.go
  drift, and `make check` fails on any reintroduced misalignment). The existing
  `styles.go` drift is fixed in the same leg; `gofmt -l internal cmd` is now clean.
  The M0 note "Confirm golangci-lint ruleset (start lenient, tighten later)" —
  which the audit named as still-open — is marked resolved for the format half
  only: the wider "tighten later" call (gofumpt, goimports, more linters) stays a
  separate, bigger decision. Feedback file deleted.
- Decisions: D263 (the format floor is `gofmt` via the golangci-lint v2
  `formatters:` block, and that run is the enforcement point — no `gofmt -l` step
  in the Makefile; a future leg must not ship a non-gofmt file or work around the
  gate, and a stricter ruleset is the M0 "tighten later" decision, kept apart).
- Files: .golangci.yml, internal/tui/styles/styles.go, vault/milestones/M0-groundwork.md,
  vault/knowledge/decisions.md, vault/knowledge/stack.md, vault/tasks/board.md,
  deleted vault/feedback/2026-08-09-audit-format-gate.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green; the gofmt
  formatter path reports the exact styles.go drift when reverted (probed)
- Next: the remaining audit half, `2026-08-09-audit-app-monolith` — resolve the
  promised `views/` split vs the 4.6k-line `internal/tui/app.go` god object; the
  audit offers a smaller alternative (record a decision revoking `views/`), which
  fits a leg better than the mechanical split.
