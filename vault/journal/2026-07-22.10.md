# 2026-07-22 — FB-status-bar-top: status bar to the top + browsed resource type; nav-model triage

- Agent: claude-opus (Opus 4.8)
- Milestone: M2
- Did: Addressed feedback `2026-07-22-status-bar-top-and-optional-left-panel`. **Now
  slice:** moved the status bar from the bottom to the **top** row (`View()` now
  stacks status · body · hint line) and made it name the **browsed resource type** —
  `kube.Resource.GVK.Kind` (e.g. `Pod`) is set from `selectResource` via a new
  `statusbar.SetResourceType`, rendered in the left segment as
  `context · namespace · <kind> · filter` and skipped before the first drill-in.
  Mouse click Y-mapping now subtracts `statusBarHeight` (the body starts one row
  down) so click-to-open/select keep hitting the right rows. **Direction triage:**
  split the optional-menu / popup-menu / command-palette-switch ask into ordered
  board tasks (FB-nav-menu-toggle → FB-nav-resource-palette → FB-nav-menu-popup) and
  recorded the target navigation model as D96 (the left pane is not a permanent
  fixture). Deleted the feedback file (D69).
- Decisions: D96 — status bar at the top + resource-type display are load-bearing;
  the left menu is on a path to optional/popup + a command-palette resource switch
  (the FB-nav-* tasks), all on the keymap registry (D11) and zero-shared-state.
- Files: `internal/tui/components/statusbar/statusbar.go` (SetResourceType +
  leftSegment), `internal/tui/app.go` (View order, selectResource sets the kind,
  handleMouseClick Y offset, layout/View comments), `internal/tui/tui_test.go`
  (new resource-type test + mouse-click Y coords), `vault/knowledge/decisions.md`
  (D96), `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`, deleted
  `vault/feedback/2026-07-22-status-bar-top-and-optional-left-panel.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: **FB-nav-menu-toggle** — a `menu.toggle` keymap action to show/hide the left
  pane (first slice of the D96 navigation direction); or the still-queued feedback
  `2026-07-22-text-selection-select-to-copy` (mouse-capture default), which is the
  next unaddressed inbox item and preempts the board.
