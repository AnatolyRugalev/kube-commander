# 2026-07-24 — FB-pf-bind-toast: port-forward bind failures become actionable

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Addressed the first (highest-value) slice of the high-priority port-forward
  feedback (`2026-07-24-port-forward-picker-and-local-port`). A local-listener bind
  failure — almost always the local port already in use (e.g. forwarding Redis 6379
  while Redis runs locally) — no longer surfaces client-go's opaque `unable to listen
  on any of the requested ports: [{6379 6379}]`. `handleForwardDone` now detects that
  sentinel (`isPortForwardBindErr`) and replaces it with an actionable status-bar hint
  (`portForwardBindHint`) naming the clashing local port(s) and telling the user to
  retry with `:6379`/`:0` to get an OS-assigned free local port (a capability
  `kube.PortForward` already has — leading-colon syntax + `Ports()` readback, so no
  kube-layer change). The ports prompt hint now advertises the `:80=free local` syntax.
  The larger parts of the feedback (a picker of the pod's declared ports; editable /
  one-keystroke-auto local port) were triaged into board tasks, not built here.
- Decisions: D130 — bind failures are actionable, not raw; `:0`/`:<remote>` is the
  documented free-local-port escape; port-picker/local-port editing deferred to
  FB-pf-port-picker / FB-pf-local-port.
- Files: internal/tui/app.go (handleForwardDone + isPortForwardBindErr /
  portForwardBindHint / splitPortSpec helpers + prompt hint), internal/tui/tui_test.go
  (bind-failure degrade + detection + hint tests), vault/knowledge/decisions.md,
  vault/tasks/board.md, vault/milestones (unchanged — port-forward exit criterion was
  already met), deleted vault/feedback/2026-07-24-port-forward-picker-and-local-port.md.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (0 issues). Hermetic only — no cluster;
  the bind-failure error and message mapping are unit-tested with a fake forward.
- Next: FB-confirm-modal-yn-keys or the remaining feedback (4 more items: confirm y/n
  keys, delete default `d`, unify yaml/edit, logs dedicated view, cluster search).
  Feedback inbox still preempts the board — the oldest normal item is
  `cluster-search-multi-resource`; a small quick win is `confirm-modal-yn-keys`.
