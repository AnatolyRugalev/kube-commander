# 2026-07-23 — M3-02: action surface + M3 keymap

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Landed the M3 actions surface + keymap (the trigger every later M3
  viewer/action needs), surface + routing only (D107). New row-action registry
  `internal/tui/rowaction.go` (`rowActions`): each entry is a `rowAction` id, menu
  title, optional bound `keymap.Action`, and a kind/verb-keyed applicability
  predicate. Registered six new keymap actions off the reserved nav keys —
  `actions.menu` (`a`), `res.describe` (`d`), `res.yaml` (`y`), `res.logs` (`L`),
  `res.edit` (`e`), `res.delete` (`x`); the rest of the curated set (scale, rollout
  restart, cordon/uncordon/drain, suspend/resume, port-forward, exec, reveal secret)
  is menu-only. Wired a third `picker.Model` (Kind `"action"`) into the shell
  (mirrors the resource palette, D100): `actions.menu` / a direct key over a live
  table opens the menu (or fires directly), listing exactly the actions applicable
  to the browsed kind over the selected row. Both entry points funnel through one
  typed `rowActionMsg{Action, Resource, Object}` dispatched as a Cmd; the placeholder
  `handleRowAction` surfaces a transient "not yet available" toast so routing is
  visible (D68) — each later M3 leg replaces/extends that branch. Added
  `actions.menu` to the table focus hint for discoverability. Regenerated
  `docs/keybindings.md` (`make keys-doc`); updated `knowledge/keybindings.md` to the
  finalized map.
- Decisions: D107 — M3 row actions dispatch a typed `rowActionMsg`; applicability is
  a kind-keyed registry; direct keys stay in the keymap, the rest are menu-only.
- Files: internal/tui/rowaction.go (new), internal/tui/app.go (actPicker field +
  wiring: openActionsMenu/handleActionSelected/triggerRowActionKey/dispatchRowAction/
  handleRowAction, activePicker/overlayActive/resize/View, picker msg routing),
  internal/tui/keymap/keymap.go (6 actions + defaults), internal/tui/keymap/help.go
  (actions.menu in HelpTable), internal/tui/keymap/help_test.go (FullHelp 7→9, 6→8),
  internal/tui/tui_test.go (5 actions-menu/dispatch tests), docs/keybindings.md
  (regenerated), vault/knowledge/decisions.md (D107), vault/knowledge/keybindings.md,
  vault/tasks/board.md (M3 milestone unchanged — no exit criterion met by the
  surface-only leg; Status already in-progress)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests drive
  the menu open + applicability (Pod lists Logs/Exec, not Cordon; Node the reverse),
  the inert-without-resource guard, and both dispatch paths (direct `y` key and a
  menu pick → rowActionMsg carrying the row's object). Runtime surface: `a` opens a
  live actions menu and `d/y/L/e/x` fire against the selected row, each landing a
  transient "not yet available" toast — no real action wired yet, so no cluster
  mutation and no bare-launch regression; a human live look is a dogfood follow-up.
- Next: M3-03 — YAML viewer wired: handle the `rowActionYAML` intent → `kube.GetYAML`
  (M1-07a) → M3-01 viewer overlay; errors degrade to a status-bar toast (D74). The
  first end-to-end proof of the M3-01 viewer + M3-02 surface pair.
