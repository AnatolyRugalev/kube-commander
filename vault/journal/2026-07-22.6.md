# 2026-07-22 — M2-12b: legacy config migration — launcher wiring

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Completed M2-12 by wiring the M2-12a `config.Migrate` primitive into the
  launcher (`cmd/kubecom/run.go` `maybeMigrate`). On first start, before loading the
  config, if no new-format `config.yaml` exists yet (checked with `os.Stat` — not
  `config.LoadFile`, which maps a missing file to the zero config and hides the
  present/absent distinction the one-shot needs) and a legacy `~/.kubecom.yaml` is
  present, it parses the legacy file, writes a fresh new-format config **once** (a
  present config then suppresses re-migration — one-shot), and returns the migration
  notes as a transient startup toast through the existing single `WithStartupError`
  seam. Every fault degrades and never blocks (principle 3): an absent legacy file, a
  malformed/unreadable legacy YAML, or a failed write of the new config all yield no
  migration and no toast, logging a `slog.Warn` (a fixed file migrates on a later
  start). When both a migration report and a per-context menu-config error exist, the
  migration notes take the single toast slot (rarer, first-start event); the menu
  error is still logged. README Configuration gained a "Migrating from the 2020
  kube-commander" note. This met the M2 "old config migrates cleanly" exit criterion.
- Decisions: D93 — migration is one-shot gated on `config.yaml` absence (via
  `os.Stat`); all degrade paths write nothing and only log; migration notes preempt
  the single startup-toast slot over a menu-config error (which is still logged).
- Files: cmd/kubecom/run.go (maybeMigrate, startupErr from migration, menu-error
  slot guard, errors/os imports), cmd/kubecom/run_test.go (5 maybeMigrate tests +
  writeLegacyConfig helper, strings import), README.md (migration note),
  vault/knowledge/decisions.md (D93), vault/tasks/board.md,
  vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic —
  the new cmd tests set `$HOME`/`config.LegacyPath` to a temp dir and drive
  `maybeMigrate` against a real legacy file + a real target path (no cluster, no
  network). Runtime surface: the launcher now migrates a legacy config on first
  start and shows a startup notice; live first-start-with-legacy-file check awaits a
  human dogfood (D68), no sandbox regression (bare launch unaffected — no legacy file).
- Next: M2-13 (column sort, #85) — client-side stable, type-aware-where-cheap sort
  by column on the table's row set via keymap `sort.*`, depends on M2-06. (M2-14b is
  still blocked on an M3 confirm-action wiring the modal into the shell, D88.)
