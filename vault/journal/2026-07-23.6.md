# 2026-07-23 — M3-07a: logs container picker for multi-container pods

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Split M3-07 into M3-07a (container picker) + M3-07b (pod-owning kinds) and did
  07a. Opening logs on a pod now resolves the pod's containers before streaming rather
  than streaming blindly (the API server errors on an empty container name for a
  multi-container pod). Added a kube-layer `PodContainers(ctx, ref)` (a pod Get →
  `spec.containers` names in spec order) and a `ContainerLister` shell seam
  (`WithContainerLister`; `*kube.Clients` satisfies it). `openLogsViewer` for a pod now:
  with a lister wired, fetches container names off the update loop (tagged with a fresh
  `viewerGen` so a superseded request is dropped); a **single** container streams
  directly (reusing that gen), **multiple** open the reused modal picker (new
  `ctrPicker`, kind `container`) and the pick streams the chosen container; a fetch
  error / no-containers degrades to a status-bar toast (D74). Factored the streaming
  half into `streamLogsInto(res, ref, container, gen)` shared by the no-lister path
  (streams the default/sole container, empty `Container` — the M3-05/06 behaviour, so
  the picker is simply not offered without a lister), the single-container path, and the
  picker-select path. The chosen container is named in the viewer title
  (`Logs ns/pod · container`). Wired the new picker into activePicker/overlayActive/
  SetSize/View and the SelectedMsg/CancelledMsg switch; launcher wires
  `WithContainerLister(clients)`. → D111.
- Decisions: D111 — logs on a multi-container pod resolves containers first and prompts
  which to stream (single streams directly; no lister → default container, no picker);
  regular containers only (init/ephemeral a later refinement); the resolve fetch rides
  the shared viewerGen guard; streaming factored into streamLogsInto.
- Files: internal/kube/logs.go (PodContainers), internal/kube/logs_test.go (podWith +
  4 PodContainers tests), internal/tui/app.go (ContainerLister seam + WithContainerLister,
  containerLister/logStreamRes/logStreamRef fields, ctrPicker field+init+title,
  containerPickerKind, containersLoadedMsg, openLogsViewer rewrite, handleContainersLoaded,
  handleContainerSelected, streamLogsInto; ctrPicker wired into activePicker/overlayActive/
  SetSize/View + SelectedMsg/CancelledMsg + Update dispatch), cmd/kubecom/run.go
  (WithContainerLister wiring), internal/tui/tui_test.go (fakeContainerLister +
  logsModelWithContainers/openLogsFetch/resolveContainers helpers + 7 tests),
  vault/knowledge/decisions.md (D111), vault/tasks/board.md, vault/journal/2026-07-23.6.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New kube tests
  drive PodContainers (spec-order multi, single, empty-name reject, get-error wrap). New
  tui tests drive single-container-streams-directly (stream addressed to the resolved
  container), multi-container-opens-picker (2 items, no stream yet), pick-streams-chosen
  (stream Container=="sidecar", title names it), picker-cancel (no stream), resolve-error
  + no-containers degrade to a toast, and the stale-gen fetch dropped. The pre-existing
  logs tests (no lister) stay green unchanged via the default-container fallback. Runtime
  surface: `L` (or the actions menu) over a live multi-container pod now opens a container
  picker; picking one tails that container; single-container pods stream directly as
  before. A human live-cluster visual confirm (a real multi-container pod, sidecar logs,
  init-container absence) is a dogfood follow-up (D68), not reproducible in the sandbox —
  no bare-launch regression (the picker/viewer open only on the action). M3 milestone
  Status unchanged — the logs exit criterion is compound and pod-owning kinds (M3-07b)
  remain.
- Next: M3-07b — Logs for pod-owning kinds (#84): resolve a backing pod for Deployment/
  RS/StatefulSet/DaemonSet/Job/RC (label selector → newest ready pod), then feed the
  resolved pod ref into openLogsViewer's pod path so D111's container resolution/picker
  applies. Replaces the non-pod "not yet available" degrade.
