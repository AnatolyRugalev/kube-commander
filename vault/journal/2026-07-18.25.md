# 2026-07-18 — M1-06c: cordon/uncordon node actions

- Agent: Claude Opus 4.8 (do-rewrite-leg)
- Milestone: M1
- Did: Added `Clients.Cordon` / `Clients.Uncordon` to `internal/kube/actions.go` —
  they merge-patch a node's `spec.unschedulable` (true/false) through the dynamic
  client, exactly as `kubectl cordon`/`uncordon` do, reusing the shared
  `resourceInterface` addressing helper (cluster-scoped, so the ref's namespace is
  dropped). Idempotent → no UID guard; empty name rejected; NotFound wrapped (#86).
  Narrowed M1-06c to cordon/uncordon and split **drain** out to new M1-06e (eviction
  API is its own green leg).
- Decisions: D37 (cordon/uncordon = generic spec.unschedulable merge patch; M1-06c
  narrowed, drain → M1-06e).
- Files: `internal/kube/actions.go`, `internal/kube/actions_test.go`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`,
  `vault/knowledge/decisions.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`; envtest N/A — hermetic
  fake dynamic client, D18)
- Next: **M1-06d** cronjob suspend/resume (merge-patch `spec.suspend`), then
  **M1-06e** drain (eviction API) to finish the M1-06 action set.
