# 2026-07-18 — PROC-01: /do-rewrite-run orchestrator for scheduled runs

- Agent: claude (Fable 5), interactive with the maintainer
- Milestone: M0 (process, no product code)
- Did: Added the `/do-rewrite-run` skill for the scheduled routine: it spawns a
  fresh subagent per leg (each running exactly one `/do-rewrite-leg`),
  sequentially, capped at 4 legs / 90 minutes of leg-starts per run, stopping on
  any failure without retry. Keeps orchestrator context small instead of using
  `/loop` in one growing session. Routine trigger: `0 3,8,13,18,23 * * *`
  (last ~2h of each 5-hour usage block).
- Decisions: D21 — scheduled runs batch legs via fresh subagents; legs stay
  strictly sequential.
- Files: `.claude/skills/do-rewrite-run/SKILL.md`, `CLAUDE.md`,
  `vault/knowledge/decisions.md`, board, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/skill only; unchanged code)
- Next: **M0-07** — delete the legacy trees, prune `go.mod`, drop the
  `.golangci.yml` path excludes (D14).
