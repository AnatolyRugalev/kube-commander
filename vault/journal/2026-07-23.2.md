# 2026-07-23 — M3-03: YAML viewer wired

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Wired the first end-to-end M3 viewer — the `res.yaml`/`y` action (and its
  actions-menu entry) now opens the shared M3-01 `viewer.Model` over the selected
  row and streams in the object's YAML. Added a narrow `YAMLGetter` seam (single
  `GetYAML`, wired via `WithYAMLGetter`; `*kube.Clients` satisfies it via M1-07a),
  mirroring the existing watcher/discoverer/lister seams so the tui package still
  constructs no client and stays hermetically testable. `handleRowAction` now
  branches on `rowActionYAML` → `openYAMLViewer`: it shows the viewer immediately
  (empty, so the gesture feels instant) and issues the `GetYAML` fetch off the update
  loop as a Cmd, seeding content when the typed `yamlLoadedMsg` lands. Each open bumps
  a `viewerGen` carried on the load message, so a fetch that returns after the viewer
  closed or a newer one opened is dropped (the watchGen/seqGen stale-guard). A fetch
  error degrades to a status-bar toast + closes the viewer (D74) rather than an empty
  box. While the viewer is active the root captures input (`handleAction` →
  `handleViewerAction`: scroll on nav, close on nav.back/quit, swallow the rest), it
  composites over the base browse view via `overlayCenter` (D95), and `overlayActive`
  now includes it so mouse events stay inert. Launcher wires `WithYAMLGetter(clients)`.
  The remaining `rowAction`s still fall through to the "not yet available" toast until
  their own legs (D107 unchanged). → D108.
- Decisions: D108 — M3 viewer legs wire through a narrow kube getter seam, fetch async
  with a generation guard, degrade on error, and capture input while open (the pattern
  M3-04/05/08 follow).
- Files: internal/tui/app.go (YAMLGetter seam + WithYAMLGetter, viewer/yamlGetter/
  viewerGen fields, viewer construction, openYAMLViewer/handleYAMLLoaded/viewerTitle/
  handleViewerAction, handleRowAction branch, yamlLoadedMsg + viewer.ClosedMsg Update
  cases, resize/overlayActive/View compositing), cmd/kubecom/run.go (WithYAMLGetter
  wiring), internal/tui/tui_test.go (fakeYAMLGetter + 5 tests: open+content, close,
  fetch-error degrade, inert-without-getter, stale-gen drop), vault/knowledge/
  decisions.md (D108), vault/tasks/board.md, vault/journal/2026-07-23.2.md (M3
  milestone Status unchanged — the YAML/describe/logs exit criterion is compound and
  not yet fully met)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests drive
  the full path (y → intent → viewer open + GetYAML fetch → content composited over the
  base menu), esc-close, an error toast + close on GetYAML failure, the no-getter inert
  case, and the generation guard dropping a stale fetch. Runtime surface: `y` (or the
  actions menu) over a live table now opens a scrollable YAML pager for the selected
  object against the real cluster; a human live-cluster visual confirm (real object
  YAML, scroll feel) is a dogfood follow-up (D68), not reproducible in the sandbox — no
  bare-launch regression (viewer opens only on the action, the browse view is untouched).
- Next: M3-04 — Describe viewer wired: same pattern (a `Describer` seam →
  `kube.Describe` M1-07b → the shared viewer), reusing D108's seam/fetch/gen-guard/
  capture shape; `res.describe`/`d` is already routed by M3-02.
