# 2026-07-19 — M1-06e-2: drain eviction loop

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Landed the eviction half of drain in `internal/kube/drain.go` —
  `Clients.Drain(ctx, nodeRes, node, opts)` completes the action: it computes the
  M1-06e-1 `DrainCandidates` first (refusing upfront on any blocking pod), **then**
  cordons the node (reuse M1-06c) — candidates-before-cordon so a refused drain
  never leaves the node cordoned — then evicts each pod through the typed policy/v1
  Eviction API (`Clientset.PolicyV1().Evictions(ns).Evict`, PDB-honoring, the same
  subresource `kubectl drain` uses). `evictPod` retries `429 TooManyRequests` after
  `evictionRetryInterval` until the budget clears or ctx expires (ctx deadline is
  the whole budget); NotFound = already gone = success. Two-pass evict-all then
  wait-all: `waitPodDeleted` polls `drainPollInterval` until each pod is NotFound
  or its name resolves to a different UID. Completes the M1-06 action set
  (delete/scale/restart/cordon/suspend/drain).
- Decisions: D40 — drain eviction loop via policy/v1 Eviction API, PDB-aware
  429-retry to the ctx deadline, candidates-before-cordon ordering, two-pass
  evict-then-wait; timing knobs as package vars for hermetic tests.
- Files: `internal/kube/drain.go`, `internal/kube/drain_test.go`,
  `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues)
- Next: **M1-07** — streaming: logs; describe (kubectl/pkg/describe); get-as-YAML.
  Then M1-08 background port-forward, M1-09 typed graceful errors. (M1-04b lazy
  group detail stays parked until the M2 menu exists.)
