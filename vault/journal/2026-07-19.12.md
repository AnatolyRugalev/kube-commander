# 2026-07-19 — M2-01c: config `keys:` wiring + `kubecom keys`

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Gave the user config its first real field and the keymap its first config
  surface (on M2-01a's `Merge`). `internal/config` becomes a real package (was a
  doc-only stub): `Config.Keys map[string][]string` (`json:"keys"`) — the
  action-id → key-tokens override table; the zero value is valid and runs on
  defaults. Decoding uses **`sigs.k8s.io/yaml`** (already a direct dep since
  M1-07a — no new YAML module): plain user YAML decodes through JSON tags, and
  `UnmarshalStrict` **rejects unknown top-level fields** so a typo (or a
  not-yet-wired section written early) fails loudly instead of silently doing
  nothing. `Load(io.Reader)` + `LoadFile(path)` share a `parse`; a **missing file
  is not an error** (`LoadFile` returns the zero config on `os.ErrNotExist`).
  `Path()` = `os.UserConfigDir()/kubecom/config.yaml` (D20). **`Config.Keymap()`**
  casts each string id → `keymap.Action` and calls `DefaultKeymap().Merge(...)`,
  reusing M2-01a validation wholesale (unknown-action / bad-token / collision are
  errors; a nav-key shadow is a returned warning) — no resolution logic duplicated;
  config imports keymap one-way. New **`kubecom keys`** subcommand loads config
  (default path or `--config`), resolves, and prints the effective map in registry
  order (disabled actions → `(disabled)`); merge **warnings → stderr**, table →
  stdout, an invalid keymap fails the command (exit 1). `printKeys` is split from
  the cobra `RunE` for hermetic testing.
- Decisions: D49 — `internal/config` real package; `keys` field; `sigs.k8s.io/yaml`
  reused (no new dep) with strict unknown-field rejection; missing file → zero
  config; `Config.Keymap()` reuses `Merge`; `kubecom keys` prints (warnings→stderr).
- Files: `internal/config/config.go` (+ `doc.go` reword), `internal/config/config_test.go`,
  `cmd/kubecom/keys.go`, `cmd/kubecom/keys_test.go`, `cmd/kubecom/main.go`
  (register `keys`), `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry. **No new deps** (`go mod tidy`
  no-op).
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic:
  config tests (empty/keys/unknown-field-reject/missing-file/read/Path + Keymap
  resolve/nav-shadow-warn/unknown-action/bad-token); cmd tests (defaults / override
  with warning-to-stderr / invalid → error). Also ran the built binary end-to-end:
  `kubecom keys` on no config → vim-first defaults; on an override → resolved map +
  a `warning:` line on stderr; on a bogus action → `Error: keymap: unknown action`
  and exit 1. Pure/synchronous — no `-race` surface.
- Next: **M2-01d** — help/keybindings generation from the registry: derive
  `bubbles/key.Binding`s + a help overlay (and a generated keybindings doc) from
  the keymap so help can't drift from the bindings (D11). Adds the `bubbles` dep
  and needs a small model/overlay surface. That closes M2-01 (action registry +
  configurable keymap); the M2 app shell (root model, browse view, live table,
  pickers, config load/save + legacy migration) is the rest of M2.
