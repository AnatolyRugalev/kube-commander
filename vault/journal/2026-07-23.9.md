# 2026-07-23 — M3-08b: secret viewer copy-to-clipboard

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Completed the Secret viewer (#89) with a per-entry cursor + copy, ticking the
  M3 secret exit criterion. Gave the viewer a **selection cursor** (`secretSel`, an
  index into the key-sorted `secretData.Entries`, reset to 0 on every open/load):
  `renderSecret` now takes `sel`, marks the selected entry with a 2-cell cursor gutter
  (`> ` vs `  `, equal width so keys stay column-aligned), and returns each entry's
  0-based output line so the selection can be kept on screen. While the secret viewer
  is up, **`nav.up`/`nav.down` move the entry cursor instead of line-scrolling** (a
  Secret's body is small; walking entries is the useful gesture) and the selection is
  pulled back on screen via a new `viewer.EnsureLineVisible` (wrapping the viewport's
  `EnsureVisible`); half/full-page keys still scroll for a large revealed value, and
  the other viewers keep their j/k scroll (the branch is `viewerKindSecret`-gated).
  Added a registered `secret.copy` action (`c`, D11 — no raw key) handled in
  `handleViewerAction`: it writes the selected entry's **decoded value** to the system
  clipboard via **`tea.SetClipboard`** (bubbletea v2's built-in OSC-52 command — no
  external clipboard dep, works over SSH), inert on the other viewers and when the
  Secret has no entries. **Copy works masked or revealed** (the clipboard write is
  itself the deliberate gesture; the value need not be on screen) and the confirmation
  echoes only the key + byte length, never the value. The confirmation is a **neutral
  status-bar notice** via a new `SetNotice`/`ClearNotice` channel on the status bar
  (Accent-styled, auto-cleared on its own `statusNoticeGen`/`noticeClearMsg` timer —
  the non-error twin of `SetError`; an error outranks a notice in `View`). Regenerated
  `docs/keybindings.md` (`make keys-doc`, new `secret.copy` row); the keymap group
  count is unchanged (copy joins the existing `secret` group). → D114.
- Decisions: D114 — the secret viewer has an entry cursor (nav.up/down select, not
  scroll); `secret.copy` (`c`) yanks the selected decoded value to the clipboard via
  bubbletea's OSC-52, masked or revealed; confirmation is a neutral status-bar notice
  echoing only key + byte length.
- Files: internal/tui/keymap/keymap.go (ActionCopySecret const/meta/default `c`),
  internal/tui/components/viewer/viewer.go (EnsureLineVisible),
  internal/tui/components/statusbar/statusbar.go (noticeText field + SetNotice/
  ClearNotice/HasNotice + View notice branch),
  internal/tui/app.go (secretSel/secretEntryLines fields, statusNoticeGen field,
  noticeClearMsg + its Update case, surfaceNotice helper, renderSecret sel-param +
  cursor gutter + line offsets, renderSecretViewer helper, secretSel reset in
  openSecretViewer/handleSecretLoaded, secret.copy + entry-cursor branches in
  handleViewerAction),
  internal/tui/tui_test.go (copyKey, copiedClipboard + loadedSecret helpers +
  TestRenderSecretCursorAndLines, TestSecretCopyCopiesSelectedValueMasked,
  TestSecretCopyFollowsCursor, TestSecretCopyInertWithoutEntries,
  TestCopyInertOnYAMLViewer),
  docs/keybindings.md (regenerated), vault/knowledge/decisions.md (D114),
  vault/knowledge/keybindings.md (secret.copy row), vault/tasks/board.md,
  vault/milestones/M3-actions-viewers.md (secret exit criterion ticked),
  vault/journal/2026-07-23.9.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). A renderSecret
  unit test drives the cursor gutter placement, per-entry line offsets, mask-safety
  (no value in a masked render), and the revealed multi-line indent + offset tracking.
  New tui tests drive the full path: copy places the selected entry's decoded value on
  the clipboard **while it stays masked on screen** (the confirmation names the key,
  not the value); the cursor follows nav.up/down and copy targets the current entry,
  clamping at both ends; copy is inert (no cmd, no notice) with no entries and on the
  YAML viewer (which stays open). Runtime surface: over a live Secret row the viewer
  now shows a `> ` cursor; `j`/`k` walk entries, `c` copies the selected decoded value
  to the system clipboard and flashes `copied "key" (N bytes)`. A human live-cluster
  visual confirm (OSC-52 actually reaching the system clipboard through the user's
  terminal/tmux/ssh, a multi-line cert value) is a dogfood follow-up (D68), not
  reproducible in the sandbox — no bare-launch regression (the viewer opens only on the
  action). M3 milestone: the secret exit criterion is now met and ticked; Status stays
  in-progress (the action-wiring slices M3-09…M3-15 remain).
- Next: M3-09 — Delete action wired through the confirm modal (root owns a
  `modal.Model`, `ShowConfirm` on the selected row → `ConfirmedMsg` → `kube.Delete`,
  result to the status bar; accept = `nav.drillIn`, decline = `nav.back`, no raw y/n).
  First confirm wiring (D88) and it **unblocks M2-14b** (the modal-flow teatest).
