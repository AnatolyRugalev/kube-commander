# 2026-07-20 — M2-09b: table filter app wiring + search nav

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Wired the M2-09a table filter core into the app shell. `app.filter` (`/`)
  opens a `bubbles/textinput` field over the current resource table (no-op unless a
  table is showing); while open the root routes keys through a new `routeFilterKey`
  that reuses the picker's control/text split (D73) — text narrows the rows live via
  `table.SetFilter`, mapped no-text keys are control actions. enter commits the
  narrowed view (routing resumes), esc clears-and-closes it (restores every row,
  D78); esc also clears a committed filter, and selecting a new resource resets the
  filter state. Settled n/N: with a narrowing filter the visible rows *are* the
  matches, so `app.searchNext`/`Prev` step the selection through them with wrap
  (new `table.SelectNextWrap`/`SelectPrevWrap`), no-op with no filter. The active
  filter surfaces as a status-bar left segment (`statusbar.SetFilter`) — the live
  input while editing, `/query` once committed. No new keymap actions (filter/search
  ids already registered), so no doc regeneration.
- Decisions: D80 — filter input mode (control/text split, enter-commits/esc-clears,
  n/N step-with-wrap, status-bar indicator).
- Files: `internal/tui/app.go` (routing + openFilter/routeFilterKey/handleFilterAction/
  commit/clear/searchMove/syncFilterStatus, filterInput field), `internal/tui/
  components/table/table.go` (SelectNextWrap/SelectPrevWrap), `internal/tui/components/
  statusbar/statusbar.go` (SetFilter + left-segment render), tests in all three
  packages, `vault/knowledge/decisions.md` (D80), board, milestone, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New shell
  tests drive the real update loop: open→narrow (full set intact), commit keeps
  narrowing, esc restores, letter-types-vs-arrow-navigates split, n/N wrap through
  matches, n inert without a filter, new-resource clears; a View render assert
  confirms the prompt shows in the status bar without breaking layout. Component
  tests cover the wrap helpers and the status indicator. (Real-cluster/visual check
  still rides the open dogfood human-task; the filter logic is fully fake-driven.)
- Next: **M2-14** — teatest coverage (update loop · menu reconcile · modal flow),
  the top unblocked M2 item (M2-10…13 gated by the dogfood human-task). Note M2-14's
  modal-flow slice depends on M2-10, itself gated — so M2-14 can cover the update
  loop + menu reconcile now and defer the modal portion, or the loop waits on the
  dogfood pass.
