# 2026-08-09 — THEME-06: `gruvbox-light` joins the registry, taking it to fourteen

- Agent: deepseek-v4 (do-rewrite-leg)
- Milestone: none (feedback-driven THEME line — the values-only leg its own note named)
- Did: the feedback inbox is empty and every open human task is advisory except
  `2026-07-30-first-release-tag` (gates only M5-11), so this is a board pick — **THEME-06**,
  porting `gruvbox-light` as the registry's third light palette. The values come from the
  community fork's own light-mode branch of `colors/gruvbox.vim` (fetched this leg to check,
  not recalled): bg0=light0 `#fbf1c7`, fg1=dark1 `#3c3836`, chrome on light2/light1, and the
  `faded_*` accent set (the dark branch uses `bright_*`; the light branch uses `faded_*` — a
  mirror alone would have got this wrong). Measured: body 10.22:1, selected row 6.76:1,
  status bar 8.45:1 — all clear the 4.5 floor (D251 pt 1) with room to spare. Registered in
  `builtins`, asserted in `wantBuiltins`, attributed in `TestPortedPalettesCarryTheirAttribution`,
  and documented in `docs/configuration.md` + README (count now "Fourteen").
- Decisions: none (values-only; the board note said none was needed — the fork defines the
  light palette explicitly, so the `faded_*` choice is reading upstream, not deciding).
- Files: internal/tui/styles/{themes.go,themes_test.go,styles.go}, docs/configuration.md,
  README.md, vault/knowledge/themes.md, vault/tasks/board.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: the top unblocked Backlog item — CTX-WARM-02/03/04 and M5-11 stay gated on their
  human tasks; e.g. the next M5/M4 item or another THEME values leg.
