# 2026-07-20 — PROC-05: feedback inbox (`vault/feedback/`)

- Agent: claude (Opus 4.8), interactive with the maintainer
- Milestone: cross-cutting (process; no product code)
- Did: Added a human → agent **feedback inbox** at `vault/feedback/` so the
  maintainer can steer the autonomous loop between reviews. Format mirrors the
  journal (one `YYYY-MM-DD-slug.md` per item; `README.md` documents the flow and
  is the only non-item file). Wired it into the leg loop: **Orient lists the
  inbox and unaddressed feedback preempts the board** — the oldest / highest-
  priority item becomes the leg, gets addressed (implement / triage onto board
  tasks / decide+record), and the file is **deleted in the same commit**, linked
  from the journal. Updated CLAUDE.md (leg loop + "where things live"), the
  `do-rewrite-leg` skill (Orient + Pick), `vault/README.md` (layout + process),
  and recorded D69.
- Decisions: D69 (feedback inbox: drained before every leg, deleted once addressed).
- Files: `vault/feedback/README.md`, `CLAUDE.md`,
  `.claude/skills/do-rewrite-leg/SKILL.md`, `vault/README.md`,
  `vault/tasks/board.md`, `vault/knowledge/decisions.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/vault only; no code touched)
- Next: **M2-RUN** (empty inbox) — launch the TUI against a real cluster.
