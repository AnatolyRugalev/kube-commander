# 2026-08-16 — STORY-06f: a dedicated `events` action — the selected resource's events (kind, reason, message, age) as its own list

- Agent: opencode (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: Gave events their own surface, the feedback `2026-08-15-events-action.md`
  asked for: S02's describe was the diagnostic lever only because events lived
  nowhere else. **Kube layer** (`internal/kube/events.go`): `Clients.Events` lists
  the object's core/v1 events as a server-printed Table — the `kubectl get events`
  columns, no hard-coded event column (D33) — filtered by `involvedObject.uid`
  (kubectl describe's own precise key), falling back to `involvedObject.name` for a
  degraded row, scoped to the ref's namespace (empty → across all namespaces, where
  a Node's events live); an empty name is rejected rather than listed unfiltered.
  **TUI** (`res.events`): a `rowActionEvents` entry (gate `canGet` like describe)
  and the default key `E` — the capital-letter family's **E**vents, partner to `d`
  describe, `e` untouched — opening the shared viewer with the aligned text table
  (every column padded to its widest cell except the last, which flows/wraps). New
  `EventLister` seam on the Cluster bundle, `openEventsViewer`/`handleEventsLoaded`
  mirroring the describe viewer with the viewerGen guard; an empty list says
  "(no events)", an error degrades to a toast + close (D74). Exported the table
  package's `FormatCell` so the text renderer formats the same server cells the
  browse table shows.
- Decisions: **D274** — events are listed per-object by `involvedObject.uid`
  (name fallback, never an unfiltered list), rendered as the server-printed Table
  with the last column flowing (kubectl-identical, no hard-coded columns), and `E`
  is the default key.
- Files: internal/kube/events.go, internal/kube/events_test.go, internal/tui/events.go,
  internal/tui/events_test.go, internal/tui/app.go, internal/tui/cluster.go,
  internal/tui/cluster_test.go, internal/tui/rowaction.go, internal/tui/rowaction_test.go,
  internal/tui/keymap/keymap.go, internal/tui/components/table/{table,color}.go + their
  tests, docs/keybindings.md, README.md, vault/tasks/board.md, vault/knowledge/decisions.md,
  vault/feedback/2026-08-15-events-action.md (deleted), this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. New hermetic
  tests: kube `Events` request path (UID field selector, namespace scoping,
  cluster-scoped empty-namespace list, empty-name rejection), tui viewer open →
  fetch → render, empty-list notice, error degrade, inert-without-seam, and the
  `TestRenderEventsTable` column-alignment pin; `TestRowActionConfirmsMatchesTheHandlers`
  now covers the 16th action. **Live**: the binary builds and launches; and the new
  primitive was exercised against the running `kubecom-story` cluster — for the
  crash-looping pod, `Events` returned exactly its two real events (Warning BackOff
  + Normal Pulled) with the server's wide columns, proving the UID selector and
  Table decode against a real apiserver (throwaway probe, deleted). No install /
  launch / config surface changed, so D68's dogfood clause is limited to the binary
  launching; the README's keylist table carries the new key via the generated
  `docs/keybindings.md`.
- Next: **STORY-06g** — quick access to unhealthy workloads spanning kinds, one
  keypress from anywhere to "what's broken, sorted to the top / filtered"
  (`2026-08-15-unhealthy-workloads-quick-access.md` +
  `2026-08-15-pod-first-blinds-non-pod-failures.md`), the main story's miss.
