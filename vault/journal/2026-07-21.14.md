# 2026-07-21 — FB-menu-config-03: load the per-context menu file on start

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Final slice of feedback `2026-07-21-02` (D83) — wired the per-context menu
  file into the app launch. `cmd/kubecom/run.go` resolves the active context once
  (`kube.ContextName`, now reused for both the status bar and the menu file), then
  `loadMenuExtras` reads `config.MenuPath(ctx)` → `config.LoadMenuFile`. A blank/
  unresolved context or a missing file yields no extras and no error (the default
  menu, the common case); only a malformed/unreadable file returns an error, which
  the launcher logs (`slog.Warn`) and turns into a `*tui.ErrorMsg`. Either way the
  app launches on the default menu — never blocked (principle 3). The tui shell
  gained two options: `WithMenuExtras([]config.MenuResource)`, applied in
  `NewWithKeymap` via the existing `menu.AddExtras` **before** discovery so a
  discovered twin dedupes against the extra (FB-menu-config-02), and
  `WithStartupError(*ErrorMsg)`, surfaced as a transient status-bar toast batched
  into `Init` alongside the discovery start. `Init` now `tea.Batch`es the two cmds
  (still resolves to nil when neither is set, preserving the inert-without-discoverer
  contract). README's Configuration section gained a "Per-context menu" subsection.
- Decisions: none (no new binding constraint — the file resolution, the degrade-to-
  default + startup-toast behaviour, and the two shell options are the concrete
  realization of D83's already-decided per-context menu design; recorded here per
  D67). This completes the D83 feedback triad (config schema → menu merge → app
  wiring).
- Files: `cmd/kubecom/run.go` (resolve ctx once, `loadMenuExtras`, `WithMenuExtras`/
  `WithStartupError` wiring, `strings` import), `cmd/kubecom/run_test.go` (new:
  blank-context/missing-file/valid-file/malformed-file), `internal/tui/app.go`
  (`config` import, `menuExtras`/`startupErr` fields, `WithMenuExtras`/
  `WithStartupError` options, `AddExtras` call in `NewWithKeymap`, batched `Init`),
  `internal/tui/tui_test.go` (`config` import, `TestMenuExtrasFoldedIn`,
  `TestStartupErrorSurfacesToast`), `README.md` (per-context menu subsection),
  board, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Runtime:
  hermetic — `loadMenuExtras` is exercised against real temp-dir menu files
  (XDG_CONFIG_HOME) through the real `config.MenuPath`/`LoadMenuFile`; the tui tests
  drive `WithMenuExtras` (an extra CRD appears in the menu) and `WithStartupError`
  (Init emits the toast → status bar HasError). A live-cluster confirmation that a
  context's CRDs show up in the menu, and the malformed-file toast, ride the standing
  dogfood loop.
- Next: **FB-hintbar-dedicated** — promote the focus-aware key hint into a dedicated
  always-visible bottom line of its own (D85 remainder), or start the M2 app-shell
  remainder with **M2-10** (confirm/prompt modal), which also unblocks M2-14b.
