# 2026-07-18 — M1-06b: scale + rollout-restart actions

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Added `Clients.Scale` and `Clients.RolloutRestart` to `internal/kube/actions.go`,
  the second slice of the in-process action set (after 06a delete). Both go through
  the shared dynamic-client `resourceInterface` helper, so they are generic over
  built-ins and CRDs with no per-kind wiring. Scale merge-patches the `scale`
  subresource (`spec.replicas`); rollout-restart merge-patches the pod template's
  `kubectl.kubernetes.io/restartedAt` annotation (UTC RFC3339, kubectl's exact key
  → the two tools interoperate). Merge (not strategic) patch keeps both schema-free
  for CRDs. No UID precondition — these are idempotent and PatchOptions carries none.
- Decisions: D36 — scale via scale-subresource merge patch; rollout-restart via
  kubectl-keyed annotation merge patch; merge-not-strategic for CRD generality.
- Files: `internal/kube/actions.go`, `internal/kube/actions_test.go`,
  `vault/knowledge/decisions.md`, `vault/knowledge/stack.md`, board, milestone.
- Verify: build ✓ · test ✓ (8 new tests) · vet ✓ · lint ✓ (0 issues)
- Next: **M1-06c** — cordon/uncordon + drain (node actions; eviction API), building
  on the same action layer.
