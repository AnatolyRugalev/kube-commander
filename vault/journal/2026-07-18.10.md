# 2026-07-18 — M0-02: toolchain bump (Go 1.23 + cobra)

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M0
- Did: Set `go.mod` `go` directive to **1.23** (stack floor) and rewired
  `cmd/kubecom` from the hand-rolled `run(out, args)` dispatch onto **cobra
  v1.10.2** (D13's planned replacement): `newRootCmd()` constructor + `version`
  subcommand; `--version`/`-v` use a custom version template so they print
  `version.Info()` verbatim. Behavior parity kept (version output, help on
  no-args, error+exit-1 on unknown command) plus free `help`/`completion`.
  `ioutil` was already removed by M0-07, so that clause was a no-op.
- Decisions: D23 (Go 1.23 floor + cobra v1.10.2 root command shape).
- Files: `go.mod`, `go.sum`, `cmd/kubecom/main.go`, `cmd/kubecom/main_test.go`,
  `vault/knowledge/decisions.md`, board, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (golangci-lint v2.5.0, 0 issues);
  binary smoke-checked (version/--version/-v/help/unknown-cmd).
- Next: **M0-04** — GitHub Actions CI running `make check` on Linux+macOS (D17);
  the first independent green signal for a reviewer, sequenced before more
  feature legs.
