# 2026-07-19 — M2-01d: help generated from the keymap registry

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Built the help side of "zero hard-coded keys" (D11), all derived from the
  resolved keymap so it can't drift from actual bindings. `internal/tui/keymap/help.go`
  bridges the registry to bubbles: `(*Keymap).Binding(a)` → a `bubbles/v2/key.Binding`
  (WithKeys = resolved tokens, WithHelp = `gg/home`-style display + registry
  `Describe()`; empty/unknown → a **disabled** binding, no panic), `Bindings()` for
  the whole registry, and `HelpKeyMap` (`HelpMap()`) satisfying bubbles' `help.KeyMap`
  — `ShortHelp()` a curated status-bar subset (enabled-only), `FullHelp()` every
  enabled binding grouped into columns by action namespace (id prefix before `.`),
  first-seen column order, registry order within, disabled dropped. New
  `internal/tui/help` package is the toggleable overlay: a `Model` over
  `bubbles/help.Model` (`ShowAll`) with `Toggle`/`SetVisible`/`Visible`/`SetWidth`/
  `View()` (full when visible, `""` hidden) / `ShortHelpView()` (always-on hint).
  It **matches no raw keys** — the (future) root model resolves `app.help` via the
  keymap and calls `Toggle`; this only chooses what help to show. Rendered both
  views to confirm output (two registry-derived columns, width-based short-help
  ellipsis).
- Decisions: D50 — registry→bubbles/key bridge + overlay, both generated from the
  keymap; pin `charm.land/bubbles/v2` **v2.0.0** (go directive 1.24.2, requires
  bubbletea v2.0.0 → MVS keeps our v2.0.2; v2.1.0 needs Go 1.25) in lockstep with
  the bubbletea/Go-1.24.2 floor (D26). Split the standalone generated markdown doc
  + drift check out to **M2-01e**.
- Files: `internal/tui/keymap/help.go`, `internal/tui/keymap/help_test.go`,
  `internal/tui/help/help.go`, `internal/tui/help/help_test.go`, `go.mod`/`go.sum`
  (bubbles v2.0.0 + lipgloss v2.0.0 direct/indirect; minor MVS bumps to
  colorprofile/displaywidth/go-runewidth/x/sys within the charm graph, no Go bump),
  `vault/knowledge/decisions.md`, `vault/knowledge/stack.md`, `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  pure-logic tests: keymap bridge (binding from defaults, disabled/unknown →
  disabled, `Bindings()` covers the registry, ShortHelp subset + disable-drop,
  FullHelp grouping/order + collapse-a-column-on-disable) and the overlay
  (toggle/visibility, hidden→empty & visible→non-empty full help mentioning a
  known description, short-help always renders). No goroutines/runtime surface.
- Next: **M2-01e** — emit a committed keybindings markdown doc from the default
  keymap + a drift-check test/`make` target so it stays honest (D11), reusing
  `keymap.Bindings()`/`Keys()`/`Describe()`. Then the **M2 app shell** — root
  `tea.Model` routing input through the keymap/sequencer, embedding this help
  overlay, the two-pane browse view, and the live table fed by the M1 watch channel.
  (M1-04b lazy group detail still parked until the menu exists.)
