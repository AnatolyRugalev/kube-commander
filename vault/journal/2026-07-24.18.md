# 2026-07-24 — FB-pf-port-picker-b: port-forward port picker (TUI wire)

- Agent: claude-opus-5
- Milestone: M3
- Did: Wired the D137 declared-ports primitive into the port-forward flow, so the
  action now offers the ports the object actually declares instead of asking for a
  typed remote port. New `internal/tui/ports.go`: a `PortLister` seam
  (`PodPorts`/`ServicePorts`, mirroring `ContainerLister`/`ServiceResolver`,
  `WithPortLister`, wired to `*kube.Clients` in `run.go`), an async listing run off the
  update loop stamped with the existing `pfResolveGen` (`portsLoadedMsg`), and the
  three outcomes: **no declared ports / listing error / no lister wired → the M3-13a
  free-text ports prompt** (unchanged), **exactly one → forward immediately** with the
  bare pod-side number (kubectl's local = remote shorthand), **several → the reused
  modal picker** (`portPicker`, kind `port`, title "Port-forward port") whose pick
  forwards straight away. Rows are labelled `8080 (http · app)`, and a Service-derived
  port shows both numbers — `80 → 8080` — since a Service forwards through its backing
  pod and the remote side must be the targetPort (D137 pt 3); the label is the join key
  back to the `kube.Port` (pickers resolve to a string, D65). The Service path threads
  the Service ref through `serviceResolvedMsg.svc` so the resolved pod and the Service
  are both available to `ServicePorts`. Message-only (principle 1), no raw keys (D11) —
  the picker is the shared component, so nav/filter/cancel come for free.
- Decisions: D138 — the picker never gates the action (every failure mode falls back to
  the free-text prompt, silently); a pick is a *complete* spec (local = remote), not a
  prefilled prompt — FB-pf-local-port must not turn the common case back into typing;
  one generation (`pfResolveGen`) guards the whole Service → pod → ports chain.
- Files: internal/tui/ports.go (new: PortLister/WithPortLister, portPickerKind,
  portsLoadedMsg, resolvePortsFor, handlePortsLoaded, handlePortSelected,
  portForwardSpec/portPickerItems/portLabel/portForLabel),
  internal/tui/ports_test.go (new), internal/tui/app.go (portPicker + portLister/pfPorts
  fields, constructor/title, picker Selected/Cancelled routing, portsLoadedMsg case,
  activePicker/overlayActive/SetSize/View, openPortForwardPrompt → resolvePortsFor,
  serviceResolvedMsg.svc + handleServiceResolved → resolvePortsFor),
  cmd/kubecom/run.go (WithPortLister), vault/knowledge/decisions.md (D138),
  vault/tasks/board.md, vault/journal/2026-07-24.18.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  fake-lister coverage, no cluster: single declared port forwards directly with spec
  `8080` and no prompt/picker; two ports open the picker (labels rendered in the View)
  and the pick forwards `9090`; picker cancel starts nothing; an empty listing and a
  listing error each fall back to the ports prompt, which still forwards a typed
  `8080:80`; no lister wired opens the prompt in the same update with no listing hop; a
  Service resolves its pod then lists through `ServicePorts` with both refs and forwards
  the pod-side `8080` at the resolved pod; a superseded listing (`pfResolveGen` bumped)
  opens nothing; `portLabel`/`portForwardSpec`/`portForLabel` are pure-tested across
  bare/named/container/service-mapped ports. No bare-launch regression (D68) — the
  Port-forward action keeps every prior path reachable and only *adds* the picker hop;
  README needs no change (install/launch/config/usage unchanged, and keybindings are
  untouched — no new action, no new binding, so `docs/keybindings.md` is unaffected).
  No human-task raised: the flow is fully driveable hermetically, and the real-cluster
  value of the picker will show up in the existing dogfood loop.
- Next: **FB-pf-local-port** — editable local port with a one-keystroke "use a free
  port" (`:0`) gesture in the picker/prompt flow, completing the port-forward feedback
  line. It must keep D138 pt 2 (a plain pick stays one keystroke — the local edit is an
  opt-in gesture on top, not a mandatory prompt).
