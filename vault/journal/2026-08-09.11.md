# 2026-08-09 — LOGS-SEL-04: the match highlight paints the bright yellow on dark canvases

- Agent: kimi-k3 (do-rewrite-leg)
- Milestone: none (feedback-driven LOGS-SEL line — `2026-08-09-log-match-highlight-background`)
- Did: The maintainer's verdict on the LOGS-SEL-03 eye-check: "Highlighted text
  (matches) should have bright (yellow) background. Selection row looks good."
  THEME-05 had stripped `styles.Match` to bold+underline because no painted pair
  cleared the 4.5:1 body floor on the light palettes — but the maintainer
  dogfoods a dark one, where paint always could. Measuring the candidates first
  (the pattern LOGS-SEL-03 set) found a better mapping than the pre-THEME-05
  one: **canvas (`Background`) on `Warn`** clears the floor on all eleven dark
  built-ins at 4.68–12.91:1 — including `solarized-dark` at 4.68, which the old
  `StatusBarBg`-on-`Warn` mapping shipped at 4.05 — because D251 pt 1's
  admission guard already guarantees the canvas is the dark palette's darkest
  rung. `Match` is now bold+underline everywhere, plus that paint where
  `IsDark(t.Background)` — the shared luminance threshold, one rule keyed on a
  measured property rather than a per-palette exemption. The highlight
  background is `Warn` again, so the bar/highlight separation is the same
  4.05–9.89:1 D252 pt 1 blessed, and the two-background rule is untouched. The
  three light palettes keep weight-only (the feedback named the pair out of
  scope; D252 pt 3's measurement still forbids paint there). Rewrote
  `TestMatchHighlightIsDistinguishable` to pin the new contract per palette
  (weight everywhere; dark → canvas-on-Warn with 4.5/3.0 floors; light → no
  colors), and refreshed the stale THEME-05 comment in
  `TestSetStylesRepaintsPaintedHighlights` — its highlight assertion is
  load-bearing again now that two dark themes' Match escapes differ. One style,
  three surfaces (logs grep, table filter, cluster search). Feedback file
  deleted.
- Decisions: D259 (canvas-on-Warn on dark, weight-only on light; weight kept
  everywhere; the gate is measured polarity, not a per-palette patch) —
  partially supersedes THEME-05's treatment (which carried no Dn).
- Files: internal/tui/styles/styles.go, internal/tui/styles/themes_test.go,
  internal/tui/components/logsview/logsview_test.go,
  vault/knowledge/decisions.md, vault/knowledge/themes.md,
  vault/tasks/board.md, deleted
  vault/feedback/2026-08-09-log-match-highlight-background.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green; render smoke:
  default/solarized-dark emit fg canvas + bg Warn escapes, latte/gruvbox-light
  emit weight only
- Next: two feedback items remain and preempt the board —
  `2026-08-09-theme-picker-live-preview` (re-theme live as the picker selection
  moves; `enter` commits, `esc` restores) and `2026-08-09-screencast-tape-tuning`
  (fix the search example, idempotent state dir for reruns, more of the tour —
  the agent share is the tape edits; recording is the maintainer's).
