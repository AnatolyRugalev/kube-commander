# 2026-07-19 — M2-01e: keybindings doc generated from the registry + drift check

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Closed the "generated keybindings doc" half of D11 (the help overlay was
  the in-app half, D50). Added `(*Keymap).Markdown()` (`internal/tui/keymap/doc.go`)
  which renders the committed reference `docs/keybindings.md` straight from the
  registry — a fixed banner + one table per action **namespace** (reusing
  `groupOf` from help.go), columns Action / Keys / Description in registry order,
  keys quoted and `/`-joined vim-first (`docKeys`). Nothing restates a literal
  key; it's all `Actions()`/`Keys()`/`Describe()`. The committed doc is
  **drift-guarded** by `TestKeybindingsDoc` (`doc_test.go`): it compares the
  file to `DefaultKeymap().Markdown()` and fails `make check` if stale; a
  `-update` flag rewrites it, exposed as `make keys-doc`. The golden test doubles
  as the generator — no standalone `cmd/`. `TestMarkdownFromRegistry` asserts
  every id/key/description appears (derivation, not restatement). New `docs/` dir
  (repo root, discoverable); linked from `vault/knowledge/keybindings.md`.
- Decisions: D51 — generate the doc from the registry; golden `-update` drift test
  in-package (cwd == package dir → `../../../docs/keybindings.md`); golden-update
  over a `cmd/` generator (the test is the generator); doc at repo-root `docs/`.
- Files: `internal/tui/keymap/doc.go`, `internal/tui/keymap/doc_test.go`,
  `docs/keybindings.md` (new, generated), `Makefile` (`keys-doc` target),
  `vault/knowledge/keybindings.md` (pointer), `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Sanity:
  appended a stray line to the doc → `TestKeybindingsDoc` failed with the
  "run `make keys-doc`" message (drift guard proven), then `make keys-doc`
  regenerated it clean and the gate went green again.
- Next: **M2 app shell** — the root `tea.Model` routing input through the
  keymap/sequencer and embedding the M2-01d help overlay, then the two-pane
  browse view + live table fed by the `kube` watch channel. The M2-01 action
  registry group (01a–01e) is now complete. (M1-04b lazy group detail still parked
  until the M2 menu exists.)
