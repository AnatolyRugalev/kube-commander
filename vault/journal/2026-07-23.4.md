# 2026-07-23 — M3-05: logs viewer wired (initial, no follow)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Wired the first *streaming* M3 viewer — the `res.logs`/`L` action (and its
  actions-menu entry) now opens the shared M3-01 `viewer.Model` over the selected pod
  and streams its logs in line by line. Extended D108's one-shot-fetch shape with the
  channel→msg pump rhythm (M2-02/D53): a new `LogStreamer` seam (single `Logs`, wired
  via `WithLogStreamer`; `*kube.Clients` satisfies it via M1-07c) returns a channel;
  `logPump` (msg.go, mirroring `watchPump`) reads one `kube.LogEvent` per Cmd →
  `LogLineMsg` (append + re-pump), `LogClosedMsg` (EOF ends the chain), or a bridged
  `ErrorMsg`. `handleRowAction` branches `rowActionLogs` → `openLogsViewer`: shows the
  viewer immediately (empty), starts the stream on a cancellable context, and issues
  the first `logMsg{gen,msg}` pump. Each pumped line rides the **shared `viewerGen`**
  (the watchMsg stale-guard), so a line from a superseded/closed viewer is dropped.
  Teardown via a new `stopLogStream` helper — cancel + clear logCh/logCancel — called
  before a new stream, on viewer close (`viewer.ClosedMsg` + the `q` path), when a
  YAML/describe viewer supersedes the stream, and on quit (the watch's cancel-on-
  reselect twin). Added `viewer.AppendContent` (grows the buffer preserving scroll — no
  auto-scroll; M3-06's follow slice adds that via the existing `AtBottom`) and
  `viewer.Empty` so error handling can refine D108: an open failure closes the empty
  box + toast, a mid-stream error after lines already showed keeps them on screen.
  Scope is **pods first** (`LogOptions{}` = whole log, default container, no follow);
  the pod-owning kinds the menu also lists for logs degrade to a "not yet available"
  toast until M3-07. Launcher wires `WithLogStreamer(clients)`. → D109.
- Decisions: D109 — streaming viewer legs pump a kube channel line-by-line into the
  shared viewer via a gen-tagged pump, cancel on close/supersede (`stopLogStream`), and
  append preserving scroll; open-failure closes the empty box, mid-stream error keeps
  shown lines. The M3-06/07 base.
- Files: internal/tui/app.go (LogStreamer seam + WithLogStreamer, logStreamer/logCh/
  logCancel fields, logMsg type, openLogsViewer/pumpLogs/handleLogMsg/stopLogStream,
  handleRowAction rowActionLogs branch, logMsg Update case, teardown in viewer.ClosedMsg
  / handleViewerAction quit / ActionQuit / openYAMLViewer / openDescribeViewer),
  internal/tui/msg.go (LogLineMsg/LogClosedMsg + logPump), internal/tui/components/
  viewer/viewer.go (content field, AppendContent, Empty; SetContent tracks content),
  cmd/kubecom/run.go (WithLogStreamer wiring), internal/tui/tui_test.go (fakeLogStreamer
  + logsViewerModel + drainLogPump + 7 tests), vault/knowledge/decisions.md (D109),
  vault/tasks/board.md, vault/journal/2026-07-23.4.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests drive
  the full path (`L` → intent → viewer open + stream → lines appended and composited
  over the base menu), esc-close (viewer hidden + stream torn down), an open-failure
  degrade (empty box closed + toast), a mid-stream error keeping shown lines + toast,
  the no-streamer inert case, the non-pod "not yet available" degrade (streamer not
  called), and the generation guard dropping a superseded stream's line. Runtime
  surface: `L` (or the actions menu) over a live pods table now opens a scrollable log
  pager streaming the pod's logs against the real cluster; a human live-cluster visual
  confirm (real streaming logs, scroll feel, a large/rapid log) is a dogfood follow-up
  (D68), not reproducible in the sandbox — no bare-launch regression (the viewer opens
  only on the action, the browse view is untouched). M3 milestone Status unchanged — the
  logs/describe/YAML exit criterion is compound and follow (M3-06) + pod-owning kinds
  (M3-07) are not yet wired.
- Next: M3-06 — Logs viewer follow + reconnect: wire the M1-07d reconnecting/resuming
  follow (`LogOptions{Follow:true}`) into the viewer via a `logs.follow` toggle; auto-
  scroll while following (the viewer's `AtBottom` already exists for exactly this). Same
  seam/pump/gen-guard shape (D109) with the stream kept open rather than ending at EOF.
