# 2026-07-23 — M3-06: logs viewer follow + reconnect

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Turned the M3-05 logs viewer into a *following* viewer. It now opens with
  `kube.LogOptions{Follow:true}` so the M1-07d reconnecting/resuming stream stays
  open and tails live output (was `LogOptions{}` — whole log to EOF). Added a
  shell-owned `logFollow` bool (default on at open, like `kubectl logs -f`): while
  following, each pumped `LogLineMsg` snaps the viewport to the bottom via a new
  `viewer.GotoBottom()` (D109's `AppendContent` still preserves scroll when paused).
  A new keymap action `logs.follow` (default `f`, off the reserved nav keys) toggles
  follow, but only while the logs viewer is up — the shared viewer is now restamped
  per open with `viewer.SetKind` (yaml/describe/logs), and `handleViewerAction` gates
  the toggle on `viewer.Kind()==viewerKindLogs` (inert on YAML/describe and in the
  browse view). Re-enabling snaps to the bottom. A manual up-scroll while following
  (nav.up/top/halfPageUp/pageUp, via `isScrollUp`) pauses follow so scrollback isn't
  yanked back to the tail. The viewer title carries a `[following]`/`[paused]` marker
  (`syncLogViewerTitle`, base title kept in `logTitle`) so the mode is always visible
  (D68). Regenerated `docs/keybindings.md` (drift guard) and added the follow row to
  the vault keybindings spec. → D110.
- Decisions: D110 — logs viewer opens following (stream + auto-scroll); `logs.follow`
  (`f`) toggles it (logs-viewer-only), a manual up-scroll pauses it; per-open
  `viewer.SetKind` distinguishes the shared viewer's content for kind-gated behaviour.
- Files: internal/tui/keymap/keymap.go (ActionLogsFollow + meta + `f` default),
  internal/tui/components/viewer/viewer.go (SetKind, GotoBottom), internal/tui/app.go
  (logFollow/logTitle fields, viewerKindLogs/Describe consts, SetKind in the three
  open paths, Follow:true + syncLogViewerTitle in openLogsViewer, auto-scroll in
  handleLogMsg, follow toggle + isScrollUp + pause-on-up-scroll in handleViewerAction),
  internal/tui/tui_test.go (fakeLogStreamer.gotOpts + 4 tests: opens-following,
  toggle, pause-on-up-scroll, inert-on-yaml), internal/tui/keymap/keymap_test.go +
  help_test.go (new logs.* namespace: 10 columns; free override key), docs/keybindings.md
  (regenerated), vault/knowledge/keybindings.md, vault/knowledge/decisions.md (D110),
  vault/tasks/board.md, vault/journal/2026-07-23.5.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests drive
  the follow default (viewer opens following, streamer asked with Follow:true, title
  marks `[following]`), the `f` toggle (pause→resume with the title marker tracking),
  the pause-on-manual-up-scroll (`k` pauses, `j` keeps following), and `f` inert on the
  YAML viewer. Runtime surface: `L` over a live pods table now opens a *tailing* log
  pager that reconnects across drops and auto-scrolls; `f` pauses to read history and
  resumes. A human live-cluster visual confirm (real tailing feel, reconnect on a pod
  restart, a rapid log) is a dogfood follow-up (D68), not reproducible in the sandbox —
  no bare-launch regression (the viewer opens only on the action). M3 milestone Status
  unchanged — the logs/describe/YAML exit criterion is compound and pod-owning kinds
  (M3-07) are not yet wired.
- Next: M3-07 — Logs container picker + pod-owning kinds (#84): a container picker for
  multi-container pods (reuse the M2-08 picker) and enable logs for Deployment/RS/
  StatefulSet/DaemonSet/Job by resolving a backing pod, replacing the "not yet
  available" degrade. Extends D110's follow bool + kind-gated toggle with a container
  dimension.
