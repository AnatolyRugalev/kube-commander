# 2026-08-09 — LOGS-09: the palette passes through the logs view; `o` is the previous-instance default

- Agent: kimi-k3 (do-rewrite-leg)
- Milestone: none (feedback-driven LOGS line — `2026-08-09-logs-view-palette-bindings`
  + `2026-08-09-context-switch-key-from-overlays`, paired as the .8/.9 journals
  suggested: both were keys-captured-by-a-surface items)
- Did: The logs view was the one long-lived surface where the app-global switchers
  were unreachable, which made both the palette's discoverability and `C` dead
  there. `handleLogsAction` now passes the palette family through with the grep
  closed: `:` opens the command palette over the stream (View composites the
  cmdPicker over the logs body — the one overlay the full-screen view opens), and
  `T`/`R`/`ctrl+n`/`C` open their pre-typed stages (D207). Over the logs view the
  palette lists the view's own verbs (follow/grep/regex/wrap/timestamps/previous/
  select/yank) beside the globals — the feedback's discoverability ask — each pick
  dispatching through handleAction exactly as its key does (D197). Row verbs are
  withheld there and `a` stays swallowed: their target is the browse table's
  selection, invisible under the full-screen view, and a pick like Delete would
  open the confirm modal *invisibly* while it captured input (the trap the View's
  one-body switch creates). The ctx half of the other feedback closes with it:
  `C` from a log stream opens `:context `, and the switch it drives tears the
  stream down with the old cluster (resetCluster — its overlay-dismissal was
  already pinned by TestResetClusterDismissesSurfacesAndStashes, so the feedback's
  pt-2 "test gap" proved not real; the test now also arms the port picker and the
  palette). Transient capture surfaces keep owning their keys by design —
  documented in `vault/knowledge/keybindings.md` ("Which surface owns a key").
  `logs.previous` rebinds to `o` (the maintainer: "Ctrl+P is a bit weird"), keeping
  `ctrl+p` as the second binding because it is the one form that fires mid-grep
  (D140 pt 1); `docs/keybindings.md` regenerated, README's logs paragraph names the
  palette reach. Six new tests in `logspalette_test.go` walk the whole paths,
  including `C` from a stream to a landed switch. Both feedback files deleted.
- Decisions: D258 (palette family passes through the logs view; row verbs withheld;
  transient modals keep their keys; logs verbs listed over the view; `o` +
  `ctrl+p`).
- Files: internal/tui/logs.go, internal/tui/palette.go, internal/tui/app.go,
  internal/tui/keymap/keymap.go, internal/tui/logspalette_test.go,
  internal/tui/reset_test.go, internal/tui/logs_test.go, docs/keybindings.md,
  README.md, vault/knowledge/decisions.md, vault/knowledge/keybindings.md,
  vault/tasks/board.md, deleted vault/feedback/2026-08-09-logs-view-palette-
  bindings.md + …/2026-08-09-context-switch-key-from-overlays.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green; binary smoke:
  `kubecom keys` shows `logs.previous  o, ctrl+p`
- Next: three feedback items remain and preempt the board —
  `2026-08-09-log-match-highlight-background` (match highlight takes the bright
  yellow background; a role-color change beside D252 pt 1, likely the smallest),
  `2026-08-09-theme-picker-live-preview` (live re-theme as the picker selection
  moves), `2026-08-09-screencast-tape-tuning` (tape fixes; recording is the
  maintainer's, so the agent share is the tape edits + idempotent state dir).
