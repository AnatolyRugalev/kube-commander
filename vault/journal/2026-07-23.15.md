# 2026-07-23 — M3-11b: Drain wired (confirm + eviction progress stream)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Wired the Node **Drain** action end-to-end. Kube layer: added
  `kube.DrainStream` (channel twin of M1-06e-2's `Drain`) emitting a `DrainEvent`
  per step (cordon plan → each pod evicted → each pod removed), a clean channel
  close as the success terminator and a terminal `DrainEvent{Err}` on failure;
  refactored `Drain` into a thin consumer of the stream so the two never diverge and
  the existing full-sequence tests still cover the core. TUI: `Drainer` seam
  (`WithDrainer`, nil → inert), a confirm modal (`drainModalKind`, reusing the D115
  gate — unlike idempotent cordon/uncordon, a drain evicts pods), `runDrain` opening
  a cancellable `DrainStream` on `defaultDrainOptions`, a `drainPump` mirroring the
  log pump (`drainGen`-tagged so a superseded/cancelled drain's steps drop), each
  progress step flashed to the status bar as a neutral notice, success/failure
  reported at the end, and `stopDrain` teardown wired into the quit path
  (cancel-on-quit). Launcher wires `WithDrainer(clients)`. Decided the drain policy
  default (D121). → D121.
- Decisions: D121 — drain default `{IgnoreDaemonSets:true}` (Force/DeleteEmptyDirData
  off → refuse upfront over silent data loss); long-running actions stream progress
  via a pump like the log stream, cancel-on-quit.
- Files: internal/kube/drain.go (DrainEvent + DrainStream; Drain → stream consumer),
  internal/kube/drain_test.go (2 DrainStream tests), internal/tui/msg.go
  (DrainProgressMsg/DrainDoneMsg/drainPump), internal/tui/app.go (Drainer seam +
  WithDrainer, drain fields, rowActionDrain case, drainModalKind/defaultDrainOptions/
  drainMsg, openDrainConfirm/runDrain/pumpDrain/handleDrainMsg/stopDrain, modal-confirm
  + Update dispatch + quit teardown), cmd/kubecom/run.go (WithDrainer), internal/tui/
  tui_test.go (fakeDrainer + 6 drain tests + acceptModal/runDrainToDone helpers),
  vault/knowledge/decisions.md (D121), vault/tasks/board.md,
  vault/milestones/M3-actions-viewers.md, vault/journal/2026-07-23.15.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New kube tests
  drive DrainStream to a clean close (progress lines name the cordon + each evicted/
  removed pod) and to a terminal-error refusal (no cordon left behind). New tui tests
  drive the full path: the Drain intent opens the confirm modal (not an outright
  drain); accept runs DrainStream against the selected Node with the strict default
  options, pumps progress to the status bar, and the clean close surfaces a success
  notice; decline starts nothing; a terminal error degrades to a toast; quit cancels
  the in-flight drain's context; no drainer wired → inert (no modal). Runtime surface:
  the actions menu's "Drain" over a live Node now confirms, then streams eviction
  progress to the top status bar. A human live-cluster confirm (a real node's pods
  actually evicting, PDB retries pacing, the status flipping via the watch) is a
  dogfood follow-up (D68), not reproducible in the sandbox — no bare-launch regression
  (the action fires only on the menu pick). M3 milestone: the per-action-with-confirm
  exit criterion advances (drain done); Status stays in-progress (suspend/resume M3-12
  + port-forward/exec/edit remain).
- Next: M3-12 — CronJob suspend/resume wired (`kube.Suspend`/`Resume`, M1-06d) via the
  actions menu. Both are idempotent (merge-patch of `spec.suspend`), so they follow the
  **direct** dispatch shape (D120, like cordon/uncordon) — no confirm modal, result to
  the status bar; a `Suspender` seam (`WithSuspender`, nil → inert) bundling both verbs.
