# 2026-07-20 — M2-09a: table filter core (row narrowing in the table component)

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Split the oversized M2-09 (table filter + textinput wiring + n/N) into
  **09a** (this leg, the filter core in the table component) and **09b** (app
  wiring: `/`+textinput, active-filter indicator, and the n/N search-nav decision).
  Landed 09a: the table now keeps an authoritative unfiltered `full kube.Table`
  plus the displayed `table` view, which is `full` narrowed by a case-insensitive
  substring `filter` across the **visible** (priority-0) columns. `SetFilter`/
  `ClearFilter`/`Filter`/`TotalRowCount` are the new API; `SetFilter` preserves the
  selection by object UID (cursor follows the row if it still matches, else clamps
  into the narrowed range) and re-derives from `full` each call so narrow→widen
  never loses rows. Refactored the delta path so `upsertRow`/`deleteRow`/RESET
  mutate `full` (via a package `indexOfUID(rows, uid)`), then `applyFilter`
  re-derives the view; `computeColumns` split into `selectVisible` (columns only,
  reused as the filter's match scope) + `measureWidths` (sizes to the displayed
  rows). `SetTable` clears the filter (a new resource must not inherit a stale one);
  `ApplyEvent` preserves it (a watch reconnect's RESET keeps the user's filter,
  part of the D59 selection continuity). Component-only: the shell never calls
  `SetFilter` yet, so the running binary is unchanged (filter starts empty) — no
  regression (D68), 09b wires it in.
- Decisions: D78 — table filter is a view over an authoritative unfiltered set;
  deltas mutate `full` (clearing restores every row); match visible columns only,
  case-insensitive substring, UID-preserved selection; SetTable clears / ApplyEvent
  preserves the filter; component only narrows, root owns the keymap/text trigger.
- Files: `internal/tui/components/table/table.go` (full/filter fields, SetFilter/
  ClearFilter/Filter/TotalRowCount, applyFilter/rowMatches, selectVisible/
  measureWidths split, indexOfUID→package func, ApplyEvent/SetTable/restoreSelection
  rewired, docs), `internal/tui/components/table/table_test.go` (+11 filter tests),
  `vault/knowledge/decisions.md` (D78), `vault/tasks/board.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); `-race` clean
  on the table package. New tests cover: narrowing + Total unchanged, case-
  insensitivity, matching a non-name visible cell, hidden priority-1 column not
  matching, clear-restores-all + selection return, selection preserved-by-UID and
  the clamp fallback when the selected row is filtered out, zero-match set is a
  navigable no-op, SetTable clears the filter, deltas survive an active filter
  (non-matching add joins `full` only; matching add shows immediately; clear reveals
  all), and a MODIFY that renames a row out of the match hides it while keeping it
  in `full`.
- Next: **M2-09b** — wire the filter into the shell: `/` (`app.filter`) opens a
  `bubbles/textinput` over the focused table, typing narrows live via `SetFilter`,
  esc/back clears-then-closes (mirror `routePickerKey`'s control/text split, D73),
  show the active filter + `matched/total` (TotalRowCount) in the status bar or
  table border, and decide `n`/`N` (`app.searchNext`/`Prev`) against the narrowing
  model (with narrowing, the visible rows *are* the matches — n/N cycles the
  selection or is folded away; record it).
