# 2026-07-22 — M2-11b-2: last-namespace load-on-start + persist wiring

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Completed M2-11b by wiring the per-context state store (D90) into the app.
  The launcher (`cmd/kubecom/run.go`) now resolves the active context's state
  (`loadState` → `config.LoadStateFile`, degrading blank-context / missing /
  malformed to the zero State with a logged warning) and picks the initial watch
  scope via `initialNamespace`: an explicit `-n` wins for the run — detected by
  `cmd.Flags().Changed("namespace")` in `main.go`, so `-n ""` explicitly forces all
  namespaces — and does not overwrite stored state; with no `-n`, the stored
  `LastNamespace` is restored. Added a `tui.NamespacePersister` seam
  (`WithNamespacePersister`, mirroring `WithNamespaceLister`); `handleNamespaceSelected`
  persists the picked namespace off the update loop via `persistNamespace` (a
  `tea.Cmd`), and the launcher's `statePersister` writes it through `State.SaveFile`.
  A write failure degrades to a transient toast (principle 3), never blocks input,
  and the picked scope still applies. This came in on a clean tree after an
  interrupted prior invocation left the `chore(board): claim M2-11b-2` commit with
  no implementation — I held the claim, so I finished the leg rather than re-picking.
- Decisions: D91 — explicit `-n` overrides the stored last-namespace for the run
  (gate on the flag being *set*, not its value; don't write the override back on
  start); a namespace picked in the UI is persisted; a malformed state file degrades
  silently (kubecom-owned, unlike a hand-authored menu file).
- Files: cmd/kubecom/run.go (loadState, initialNamespace, statePersister, wiring),
  cmd/kubecom/main.go (namespaceSet from Changed), cmd/kubecom/run_test.go,
  internal/tui/app.go (NamespacePersister seam, persistNamespace), internal/tui/tui_test.go,
  README.md (Remembered namespace), vault/knowledge/decisions.md, vault/tasks/board.md,
  vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic —
  tui persister tests use a fake `NamespacePersister`; cmd tests round-trip through
  the real `config` state store under a temp `XDG_CONFIG_HOME`. Runtime surface:
  the launcher now restores the last namespace and persists switches; live-cluster
  dogfood of restart-restores-scope awaits a human pass (D68), no sandbox regression.
- Next: M2-13 (column sort, #85) — client-side stable sort by column on the table's
  row set via keymap `sort.*`, depends on M2-06. Or M2-12 (legacy config migration).
