# 2026-07-22 — FB-left-pane-width-smaller: narrower default menu pane

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Addressed feedback `2026-07-22-left-pane-width-smaller` — the left menu
  pane's `menuPaneWidth = total/4` (floored at `minMenuWidth`) grew to a full
  quarter of the screen on wide terminals, eating room the table needs. Added a
  `maxMenuWidth = 28` constant (total incl. border, sized close to the standard
  resource-name content) and a cap in `menuPaneWidth`, ordered between the
  existing `minMenuWidth` floor and the narrow-terminal even-split guard. On an
  80-col terminal the pane stays at the 20-col floor (`80/4 == 20`, unchanged);
  a 200-col terminal drops from 50 → 28, freeing ~22 columns for the table. The
  feedback's other ask — ellipsis-truncating long item names rather than widening
  to fit — already landed in FB-menu-scroll (D84: menu rows clip to one line via
  `clip`), so this leg only needed the narrower default.
- Decisions: none — a UX default constant, not a cross-cutting constraint a future
  leg must not contradict (kept out of decisions.md per D67). A config option for
  the width was declined as out of scope (the feedback marked it welcome-not-required).
- Files: internal/tui/app.go (`maxMenuWidth` const + cap in `menuPaneWidth`,
  updated doc comment), internal/tui/tui_test.go
  (`TestMenuPaneWidthNarrowsWideTerminals`), vault/tasks/board.md; deleted
  vault/feedback/2026-07-22-left-pane-width-smaller.md (M2 milestone untouched —
  no exit criterion covers the pane width, Status already `in-progress`)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New unit
  test drives `menuPaneWidth` directly (white-box `package tui`): the table cases
  cover zero / 80→floor / 200+→cap / narrow even-split, and a 1..500 sweep asserts
  the pane never starves the table below `minTableWidth`, never exceeds the cap,
  and stays in range. Runtime surface: on a wide terminal the menu pane is
  visibly narrower and the table wider; a human live look is a dogfood follow-up
  (D68), no bare-launch regression.
- Next: FB-status-bar-top-and-optional-left-panel — the next feedback item (triage
  into board tasks: do the "status bar to top + show resource type" slice now,
  queue the optional/popup left-panel + resource-switch-flow direction). Then
  FB-text-selection-select-to-copy (record a mouse-capture decision). Board work
  after the inbox drains: M2-13b (column-sort keymap wiring).
