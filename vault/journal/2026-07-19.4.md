# 2026-07-19 — M1-07a: get object as YAML

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Split M1-07 (streaming logs; describe; get-as-YAML) into 07a (get-as-YAML,
  this leg) + 07b (describe) + 07c (pod logs) and landed the first slice in a new
  `internal/kube/yaml.go`. `Clients.GetYAML(ctx, r, ref)` fetches any resource
  through the dynamic client (reusing the shared `resourceInterface` helper), strips
  managedFields (kubectl hides them from `get -o yaml` by default since v1.21), and
  renders with `sigs.k8s.io/yaml` (JSON round-trip → deterministic, kubectl-identical
  output). Generic over built-ins and CRDs, ns dropped for cluster-scoped; empty
  name rejected, NotFound wrapped (#86). First in-process viewer (D2 — no external
  pager, no kubectl binary).
- Decisions: D41 — get-as-YAML via the dynamic client + `resourceInterface`;
  managedFields stripped on a DeepCopy; `sigs.k8s.io/yaml`; M1-07 split 07a/07b/07c.
- Files: `internal/kube/yaml.go`, `internal/kube/yaml_test.go`, `go.mod`/`go.sum`
  (`sigs.k8s.io/yaml` indirect→direct), `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues)
- Next: **M1-07b** — describe (kubectl/pkg/describe): human-readable describe output
  in-process; pulls `k8s.io/kubectl`, its own leg. Then M1-07c pod logs (reconnecting
  stream, watch-shaped), M1-08 port-forward, M1-09 typed errors. (M1-04b lazy group
  detail stays parked until the M2 menu exists.)
