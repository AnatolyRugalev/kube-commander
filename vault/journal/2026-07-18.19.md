# 2026-07-18 — M1-03: async full discovery → reconcile signal; per-group fault isolation

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Added `internal/kube/discovery.go` — the background-discovery half of D8.
  `discoverResources` runs one `ServerPreferredResources` pass and flattens it to
  a sorted `[]Resource` (GVK/GVR/scope/verbs/shortnames/categories) of listable,
  non-subresource kinds. Per-group fault isolation: a partial
  `*discovery.ErrGroupDiscoveryFailed` keeps the healthy resources and records
  each broken/denied group in `DiscoveryResult.Failed` (#87, #76) instead of
  blanking the menu; a total failure lands in `.Err` with empty results.
  `StartDiscovery(ctx, d) <-chan DiscoveryResult` runs the pass in a goroutine and
  delivers the snapshot once on a cap-1 buffered channel — the "discovery ready"
  reconcile signal — returning immediately so it never blocks first paint. Also a
  `(*Clients).StartDiscovery` convenience. Zero TUI imports; the TUI wraps the
  snapshot into a reconcile `tea.Msg` in M2.
- Decisions: D31 (ServerPreferredResources; ErrGroupDiscoveryFailed isolation;
  one-shot buffered reconcile channel; listable/non-subresource filter; narrow
  fakeable interface; cache/re-discovery deferred to M1-04).
- Files: `internal/kube/discovery.go`, `internal/kube/discovery_test.go`,
  `vault/knowledge/decisions.md`, board, milestone, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓  (envtest N/A — gate off; hermetic
  stub discoverer per D18)
- Next: **M1-04** — on-disk discovery cache (kubectl-style) + invalidation; lazy
  group detail on first open. Persist the DiscoveryResult and re-discover on a
  cache miss/expiry, feeding the same reconcile signal.
