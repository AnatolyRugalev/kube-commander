# 2026-07-20 — FB-menu-nesting: group the resource menu into Dashboard-style sections

- Agent: claude-opus (Opus 4.8), autonomous leg (feedback-driven)
- Milestone: M2
- Did: Addressed feedback `2026-07-20-menu-structure-nesting` (the flat left menu
  read as disorganized). The resource menu now renders **grouped sections** with
  non-selectable headers — `Cluster` (non-namespaced: Namespaces, Nodes,
  PersistentVolumes, StorageClasses) → `Workloads` (Pods…CronJobs + Events) →
  `Config` (ConfigMaps, Secrets) → `Network` (Services, Ingresses) → `Storage`
  (PVCs) → `Access Control` (ServiceAccounts), plus a trailing `Custom Resources`
  section that every discovered CRD/extra group is appended into. Chose **static
  section headers over collapse/expand** (D77): a header is a render-only row
  (styled `styles.Header`, bold/accented); the cursor only lands on items and
  nav (`up`/`down`/`top`/`bottom`) skips headers — no new action, no per-section
  state, no raw keys (D11). Added `Item.Section`; `menu.rows()` expands the item
  slice into header+item display rows (one header per contiguous section); the
  scroll offset became a display-row offset with `cursorRow()` keeping the
  selection visible (scrolling up onto a section's first item pulls its header in).
  Items are indented two spaces under their header for the tree look. Reconcile
  (D57) unchanged except appended extras now carry `Section = "Custom Resources"`.
  Verified the rendered pane end-to-end. Deleted the feedback file.
- Decisions: D77 — grouped menu via static, non-selectable section headers;
  `Item.Section` contiguous-per-section; Reconcile appends extras into the trailing
  Custom Resources section; offset is a display-row offset.
- Files: `internal/tui/components/menu/menu.go` (Section field, section consts,
  `rows`/`cursorRow`, header-aware scroll + View + `renderHeader`, indented items),
  `internal/tui/components/menu/seed.go` (section-grouped seed), `internal/tui/
  components/menu/menu_test.go` (offset test updated to display-rows; +5 grouping
  tests), `vault/knowledge/decisions.md` (D77), `vault/tasks/board.md`,
  `vault/feedback/2026-07-20-menu-structure-nesting.md` (deleted), this entry.
  No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Rendered the
  focused, reconciled menu (28×40): bold section headers, indented items, the
  selected Namespace highlighted, a discovered Widget CRD under Custom Resources.
  Tests: seed sections contiguous, one header per section, navigation skips
  headers, view renders all six seed headers, CRD lands in Custom Resources,
  reconcile still preserves selection/scroll and marks failed groups unavailable.
- Next: **M2-09** (board fallback, inbox now empty) — the next unblocked M2 slice
  (table filter / modal / config persistence — take the top of the M2 backlog).
