# 2026-07-22 — M2-13b: column sort keymap + app wiring

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Wired the M2-13a table sort primitive (D94) into the running app (#85). Two
  new registry actions (D11): `sort.column` (default `s`) and `sort.clear` (default
  `S`). `sort.column` runs a single **cycle** in `handleAction`→`sortNext`: unsorted →
  col0 asc → col0 desc → col1 asc → … → last col desc → `ClearSort` → col0 asc, so
  every visible column and both directions are reachable with one key and no separate
  column-selection gesture. The cycle is **stateless** — derived from the table's own
  `SortColumn()`/`SortDescending()`/`VisibleColumnCount()` each press, no sort cursor
  on the root model (principle 1). The table now renders a header **sort indicator**
  (`▲`/`▼`) on the sorted column, its width reserved in `measureWidths` so the arrow
  never overflows and misaligns the data rows. `sort.column` added to the
  table-context short-help hint for discoverability. `docs/keybindings.md` regenerated
  (new `sort` group). → D98.
- Decisions: D98 — one cycling key over a stateless derivation of the table's sort
  state; no separate column-selection gesture; rejected leftmost-visible-column
  (unreachable columns on tables that fit); header indicator lives in the component.
- Files: internal/tui/keymap/keymap.go (+2 actions/bindings), internal/tui/keymap/
  help.go (sort in HelpTable context), internal/tui/keymap/{keymap,help}_test.go,
  internal/tui/components/table/table.go (VisibleColumnCount, header arrow +
  measureWidths reserve), internal/tui/components/table/table_test.go,
  internal/tui/app.go (sortNext + handleAction cases), internal/tui/tui_test.go,
  docs/keybindings.md, vault/knowledge/decisions.md (D98), vault/tasks/board.md,
  vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`). New tests: keymap
  `s`→sort.column / `S`→sort.clear resolution; sort in table-context short help /
  not menu; FullHelp now 5 groups (4 with nav disabled); table VisibleColumnCount,
  header shows/hides ▲/▼, arrow keeps header↔data aligned; app sort-cycle advances
  columns+directions then clears, sort.clear restores order, sort inert without a
  resource. Runtime surface: `s`/`S` sort the live table against a real cluster
  (a human visual confirm of the arrow is a dogfood follow-up, D68); no bare-launch
  regression.
- Next: M2-14b (teatest modal-confirm flow) is still gated on an M3 action wiring the
  modal into the shell, so the next unblocked board pick is the FB-nav-* navigation
  slices (FB-nav-menu-toggle first, D96), or opening M3 (actions/viewers) since M1/M2
  are effectively feature-complete.
