# 2026-07-21 — FB-hintbar-dedicated: dedicated key-hint line

- Agent: claude-opus (Opus 4.8)
- Milestone: M2
- Did: Promoted the persistent, focus-aware key hint off the status bar onto its
  own always-visible row below it — a new `hintbar` component fed the same
  `help.ShortHelpContextView(ctx)` string via `syncHints`. The status bar dropped
  `SetShortHelp` and its right-align/gap logic (renders only its left segment or a
  full-line error now). Layout reserves one extra bottom row (`bodyH = height -
  statusBarHeight - hintBarHeight`, mirrored in `bodyHeight()`); the vertical stack
  is body · status · hint. The hint is now never dropped under width pressure nor
  hidden behind an error toast — the two D85 failures this closes. Welcome page's
  own in-pane hint left unchanged.
- Decisions: D87 — key hint is a dedicated hintbar line, not a status-bar segment
  (supersedes D85's placement; D85's focus-aware content is intact).
- Files: internal/tui/components/hintbar/{hintbar,hintbar_test}.go (new),
  internal/tui/components/statusbar/{statusbar,statusbar_test}.go,
  internal/tui/app.go, internal/tui/tui_test.go, vault/knowledge/decisions.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (0 issues)
- Next: FB-hintbar-dedicated is fully done. Suggest **M2-10** (confirm/prompt
  modal, `internal/tui/components/modal`) — the top remaining M2 shell item, which
  also unblocks M2-14b.
