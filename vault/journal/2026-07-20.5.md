# 2026-07-20 — M2-07b: two-pane browse layout with focus switching

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Composed the root model's browse view in `internal/tui/app.go` (M2-07b, on
  top of the M2-07a skeleton). The root `tea.Model` now owns the M2-05 resource
  menu (left pane), the M2-06 resource table (right pane), and the M2-04 status
  bar (bottom line), laid out on `WindowSizeMsg`: status bar 1 line, menu+table
  splitting the width (`menuPaneWidth = total/4`, floored at 20, never leaving the
  table below 20; even split on a narrow terminal). Exactly one pane holds focus
  (accented border) — the menu starts focused. `nav.right` moves focus menu→table;
  `nav.left` moves focus table→menu **only when the table is at its left edge**
  (`HOffset()==0`), otherwise it scrolls the table's columns — the
  pane-focus-vs-scroll arbitration the table's `HOffset` accessor was added for
  (D60). Non-switching nav actions route to the focused pane; the open help overlay
  swallows navigation. The menu's `ResourceSelectedMsg` / table's `RowSelectedMsg`
  are emitted but not yet acted on (watch is M2-07c, discovery M2-07d).
- Decisions: D62 — two-pane browse layout; focus switching via nav.left/nav.right
  with the HOffset edge arbitration; help overlay swallows nav; watch/discovery
  handling deferred to 07c/07d.
- Files: `internal/tui/app.go` (menu/table/statusbar fields + New wiring, `resize`/
  `menuPaneWidth`, `routeNav` focus switching, JoinHorizontal panes + status bar in
  View; dropped the M2-07a version placeholder), `internal/tui/doc.go` (package doc),
  `internal/tui/tui_test.go` (smoke + sized-render now key on a menu kind; added
  focus-starts-on-menu, focus-switch, nav-routed-to-focused-pane, help-swallows-nav
  tests), `vault/knowledge/decisions.md` (D62), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Rendered the
  sized View end-to-end: menu pane (accent/focused border) beside the empty table
  pane (subtle border) over the short-help status line; focus flips border accent on
  nav.right/nav.left. Gotcha recorded: the teatest terminal emulator does not surface
  the **selected** row's background-filled cells to a plain byte scan, so the smoke
  test keys on an unselected menu row ("Node"), not the selected first item.
- Next: **M2-07c** — wire the live table to `kube.Watch` via the M2-02 pump: handle
  the menu's `ResourceSelectedMsg` to start/stop the watch (cancel the previous),
  feed `ResourceEventMsg`→`table.ApplyEvent` and the initial List→`table.SetTable`.
