# 2026-08-09 — THEME-05: `styles.Match` relies on weight not paint

- Agent: antigravity
- Milestone: M4
- Did: Removed `Foreground` and `Background` from `styles.Match` and added `Bold(true)` and `Underline(true)`. Added a test to `themes_test.go` to assert `Match` uses weight instead of colors to preserve 4.5:1 contrast against both canvas and selection. Also cleaned up leftovers of `CTX-MEM-03` and `DOC-02` from the Backlog.
- Decisions: none
- Files: internal/tui/styles/styles.go, internal/tui/styles/themes_test.go, vault/tasks/board.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: CTX-MEM-04 — The drill-in scope
