# 2026-07-18 — M1-00: envtest integration harness (opt-in)

- Agent: Claude Opus 4.8 (claude-opus), autonomous leg
- Milestone: M1 (first M1 leg; opens the kube layer)
- Did: Landed the opt-in envtest harness (D18) in `internal/kube/envtest_test.go`.
  A `requireEnvtest(t)` guard skips every envtest-backed test unless
  `KUBECOM_TEST_ENVTEST=1`, so `go test ./...` / `make check` stay hermetic and
  need no control-plane binaries. `TestEnvtestSmoke` stands up a real
  kube-apiserver + etcd via controller-runtime envtest, builds a client-go
  clientset, and GETs the `default` namespace as an end-to-end reachability
  proof. Added a `make test-envtest` target (fetches binaries via `setup-envtest`,
  `ENVTEST_K8S_VERSION ?= 1.31.x`). This leg also introduces the first kube deps —
  the foundation later M1 legs build the real `internal/kube` on.
- Decisions: D28 (envtest harness + pinned client-go v0.31.4 / apimachinery
  v0.31.4 / controller-runtime v0.19.4; gate + guard; not in `make check`).
- Files: `internal/kube/envtest_test.go` (new), `Makefile` (test-envtest target),
  `go.mod`, `go.sum`, `vault/knowledge/decisions.md`, `vault/knowledge/stack.md`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` green, 0 lint issues).
  Behavior checked both ways: gate off → `SKIP`/green; gate on w/o binaries →
  clean `t.Fatalf` (no panic), naming the missing etcd binary.
- Note on the run: this session first took M0-06, but a concurrent agent landed an
  equivalent M0-06 (remote `2e03392`, D27) first; discarded the duplicate per the
  skill's race rule (D16) and took the next unblocked item, M1-00. Claim
  `1d31ff1` pushed as the lock before implementing.
- Next: **M1-01** — client bootstrap: clientset + dynamic + discovery + RESTMapper
  from kubeconfig/context/flags (the harness now exists to integration-test it).
