# 2026-07-20 — REVIEW-04: add the launch leg (M2-RUN), dogfooding rule, README

- Agent: claude (Opus 4.8), interactive with the maintainer
- Milestone: M2 (planning/docs; no product code)
- Did: A status review found the binary never launches the TUI (no
  `tea.NewProgram` anywhere) and never builds a real client outside tests — so
  nothing had run end-to-end against a real apiserver, and **no leg in the plan
  wired it up**. Added **M2-RUN** as the next pick (bare `kubecom` builds a real
  `*kube.Clients` from kubeconfig/context/ns flags, injects via
  `WithWatcher`/`WithDiscoverer`, runs `tea.NewProgram`; real-cluster smoke is
  part of "done"). Made **real-cluster dogfooding a standing testing rule**
  (D68): a human periodically installs + runs kubecom against a live cluster, and
  every leg from M2-RUN on keeps it launchable and incrementally improves — never
  regresses — that experience. Added it as goals.md principle 8, the M2 top exit
  criterion, and the leg loop's Verify step. Made **keeping the README current** a
  hard rule (CLAUDE.md + skill), and **rewrote `README.md`** for v1 with a real
  install (`go install …@v1` / build from source) + usage guide + config +
  keybindings/vault pointers, honest about current status. Also trimmed the M2
  milestone `Status:` narrative (~75 lines → 1) per D67.
- Decisions: D68 (runnable + dogfooded; land M2-RUN; README stays current).
- Files: `README.md`, `CLAUDE.md`, `.claude/skills/do-rewrite-leg/SKILL.md`,
  `vault/goals.md`, `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, `vault/knowledge/decisions.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/vault only; no code touched)
- Next: **M2-RUN** — make bare `kubecom` launch the browse UI against a real
  cluster.
