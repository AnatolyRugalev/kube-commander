# 2026-07-22 — FB-nav-menu-popup: retired won't-do-separately (folded into the palette)

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Executed the reassess D100 flagged for the third D96 navigation slice. D96
  ("the left pane is not a permanent fixture") was triaged into three slices; slice 1
  (menu toggle, `m`/D99) and slice 2 (resource palette, `:`/D100) landed, and D100
  explicitly left slice 3 — floating the whole sectioned menu as an `overlayCenter`
  popup — to reassess once both were in. Reassessed and **folded, not built**: the
  pane-free navigator is already the palette (filterable, works with the menu hidden)
  and "default view = table only" is already the toggle; a second overlay floating the
  entire sectioned menu (sections + namespace seam + active marker) would duplicate the
  palette with no recorded UX benefit and would need a fresh UX decision — the M1-04b
  (D81) situation exactly. Also rejected flipping the *default* to menu-hidden (a first
  launch would then show only a welcome/empty table with no visible navigator, hurting
  discoverability of a dogfooded tool — principle 6/8), so the menu stays shown by
  default. README Usage already documents the final `m`/`:` model coherently, so no
  product or doc change was needed. Vault-only leg. This completes the M2 FB-nav-* line.
- Decisions: D101 — FB-nav-menu-popup retired won't-do-separately, folded into the
  resource palette; constraint: don't build a separate floating-menu overlay on this
  old slice alone (palette = pane-free navigator, menu = toggleable pane), and reopening
  needs a decision superseding D99/D100 rather than a revival.
- Files: vault/knowledge/decisions.md (D101), vault/tasks/board.md (claim → Done +
  Last-updated), vault/milestones/M2-core-tui.md (Status line), vault/journal/2026-07-22.15.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues — no code changed,
  gate re-run to confirm the tree stays green). Runtime surface: unchanged — no
  bare-launch or dogfood regression (the navigation model `m`/`:` is exactly as it was
  after D100; this leg only records that it is the final one).
- Next: M2-14b (teatest modal-confirm flow) is still M3-gated — the modal component
  (M2-10/D88) needs an M3 action to wire it into the shell before the flow can be driven
  end-to-end. With the FB-nav-* line and all other M2 board items done, the strongest
  next pick is **opening M3 (actions/viewers)**: expand M3-actions-viewers.md into
  ordered, leg-sized Backlog slices (an M2-PLAN-style expansion is itself a valid leg,
  D52), which also unblocks M2-14b's confirm wiring.
