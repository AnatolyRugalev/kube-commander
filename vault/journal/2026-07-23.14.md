# 2026-07-23 — M3-11a: cordon/uncordon wired

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Wired the Node cordon/uncordon actions through to M1-06c's `kube.Cordon`/
  `Uncordon`. Added a `Cordoner` shell seam (both verbs on one interface — they share
  `setUnschedulable` in the kube layer) with `WithCordoner` (nil → inert), and the
  launcher wires `WithCordoner(clients)`. The `rowActionCordon`/`rowActionUncordon`
  cases in `handleRowAction` call a new `runCordon(msg, cordon bool)` that dispatches
  the patch **directly off the update loop** — **no confirm modal** (cordoning is
  idempotent, D115/D35) and **no target stash** (the row's ref is in hand from the
  intent, so no `mutateRes`/`mutateRef` needed) — reporting via `cordonDoneMsg`.
  `handleCordonDone` surfaces a neutral status notice (`cordoned`/`uncordoned <node>`)
  on success or an error toast (D74) on failure; the Node's `Unschedulable` status
  flips through the live watch stream, not by touching the table. The Cordon/Uncordon
  menu rows already existed from M3-02 (menu-only, `kindIn("Node")`) — this leg only
  wired the intents. → D120.
- Decisions: D120 — idempotent mutating actions dispatch directly (no confirm modal,
  no target stash); cordon/uncordon set the pattern that M3-12 suspend/resume follows.
- Files: internal/tui/app.go (Cordoner seam + WithCordoner, cordoner field,
  rowActionCordon/Uncordon cases in handleRowAction, cordonDoneMsg + runCordon +
  handleCordonDone, cordonDoneMsg Update dispatch), cmd/kubecom/run.go (WithCordoner
  wiring), internal/tui/tui_test.go (fakeCordoner + nodeModel helper + 4 tests),
  vault/knowledge/decisions.md (D120), vault/tasks/board.md,
  vault/milestones/M3-actions-viewers.md, vault/journal/2026-07-23.14.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tui tests
  drive the full path: cordon dispatches kube.Cordon against the selected Node row
  **without opening a modal** → success surfaces a neutral notice; uncordon dispatches
  the reverse verb (Uncordon once, Cordon zero); a failed cordon degrades to an error
  toast; both intents are no-ops (no cmd issued) with no cordoner wired. Runtime
  surface: the actions menu's "Cordon"/"Uncordon" over a live Node row now patches the
  node and flashes the result. A human live-cluster confirm (a real Node actually going
  unschedulable, the status flipping via the watch) is a dogfood follow-up (D68), not
  reproducible in the sandbox — no bare-launch regression (the action fires only on the
  menu pick). M3 milestone: the actions exit criterion advances (cordon/uncordon done);
  Status stays in-progress (drain M3-11b + suspend/resume + port-forward/exec/edit remain).
- Next: M3-11b — Drain wired (`kube.Drain`, confirm) on Node rows: the long eviction
  loop reports progress to the status bar and cancels on quit. The confirm reuses
  D115/D117; the drain progress stream (mirror the log pump) + cancel-on-quit is the
  new piece, and a DrainOptions default (Force/IgnoreDaemonSets/DeleteEmptyDirData)
  must be decided in the leg.
