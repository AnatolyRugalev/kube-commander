# 2026-07-29 — M4-11: built-in themes + registry

- Agent: claude-opus (do-rewrite-leg)
- Milestone: M4
- Did: Added the two ported built-in palettes the M4 themes bullet asks for —
  `MonokaiTheme` and `SolarizedDarkTheme` — beside `DefaultTheme`, plus the registry
  over them (`Themes()` default-first then sorted, `ThemeNames()`, `ByName()` with
  trim/case-folded but never fuzzy lookup). Pure data in
  `internal/tui/styles/themes.go`: no component, config field or picker selects a
  theme yet, that is M4-12. The name is deliberately `solarized-dark`, not
  `solarized`, because M4-12 persists it (D169 pt 1).
- Decisions: D169 — a theme name is a persisted identifier (rename = migration, so
  variant-explicit names), lookup is lenient about formatting but never fuzzy, every
  built-in must set every `Theme` role (a nil role leaks the terminal default), and
  `builtins` is the one registry whose order everything else reads.
- Files: `internal/tui/styles/themes.go` (new), `internal/tui/styles/themes_test.go`
  (new), `internal/tui/styles/styles_test.go` (folded its inline role map into the
  shared `themeColors` helper), `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M4-capabilities.md`
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). 8 new tests:
  every built-in complete and uniquely named (`TestBuiltinThemesAreComplete`,
  `TestBuiltinThemeNamesAreUniqueAndDefaultFirst`), the order contract
  (`TestThemeNamesMatchThemes`), lookup round-trip and leniency
  (`TestByNameRoundTripsEveryBuiltin`, `TestByNameIsLenientAboutCaseAndSpace`), the
  no-fuzzy guard (`TestByNameRejectsUnknownAndPartialNames`), the registry not being
  shared state (`TestThemesResultIsNotSharedState`) and the themes actually rendering
  differently (`TestBuiltinThemesRenderDistinctly`). No runtime surface changed, so
  the running binary is untouched (D68) and the README needs no update until M4-12
  adds the `theme:` field.
- Next: **M4-12** — theme selection + persistence: the `config.yaml` `theme:` field
  applied at launch (unknown name → default + startup toast, principle 3) and the
  picker that writes the choice back via `Config.Save`. Split on pickup if the live
  restyle (a `SetStyles` on every component that caches a `styles.Styles`) proves
  large: land the config field first, the picker second. It is the last M4 item.
