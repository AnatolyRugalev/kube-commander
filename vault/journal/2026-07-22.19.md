# 2026-07-22 — M3-PLAN: expand M3 (actions & viewers) into leg-sized slices

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Board-expansion leg (the D52/M2-PLAN precedent). M2's board section was down
  to only blocked/deferred items — M2-14b is M3-gated (D88), M1-INT is deferred
  envtest — and both inboxes (`feedback/`, `human-tasks/`) held only their READMEs,
  so nothing on M2 was pickable. Decomposed the M3 milestone (a single prose
  paragraph) into ordered, dependency-noted, leg-sized Backlog slices **M3-01 …
  M3-15** and flipped M3 to `in-progress`. Key framing recorded: M3 is almost
  entirely the **TUI surface** — the kube layer already implements every verb
  (logs M1-07c/d, describe M1-07b, YAML M1-07a, delete/scale/rollout-restart/cordon/
  drain/suspend M1-06*, port-forward M1-08) — so the slices wire those into a shared
  read-only viewer (M3-01), an actions surface off the reserved nav keys (M3-02),
  the viewers (M3-03…08), the confirm-modal action wiring (M3-09…12), the
  port-forward panel (M3-13), and the exec/edit suspend flows (M3-14/15). Noted that
  **M3-09** (delete-via-confirm) is what unblocks M2-14b, and updated M2-14b's board
  note to point at it.
- Decisions: D105 — M3 decomposed into ordered leg-sized slices M3-01…M3-15;
  slicing, bottom-up build order, and the inherited constraints (overlay-composite
  D95, zero shared mutable UI state / msg-pump-only D53, no raw-key matching D10/D11)
  recorded so a future M3 leg does not re-derive or contradict them.
- Files: vault/tasks/board.md (M3 Backlog written; M2-14b note points at M3-09;
  M3-PLAN claimed→done; header updated), vault/knowledge/decisions.md (D105),
  vault/milestones/M3-actions-viewers.md (Status → in-progress + breakdown note),
  this entry. **No code.**
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues — docs-only
  leg, ran as the green gate; nothing to sanity-check at runtime).
- Next: **M3-01** — the read-only viewer/pager component
  (`internal/tui/components/viewer`) wrapping `bubbles/viewport` v2: a scrollable,
  keymap-routed (vim nav + `gg`/`G` + `Ctrl+u/d`, D10), read-only text pane that
  returns a bare box overlaid via `overlayCenter` (D95). Component-only, hermetic —
  the shared substrate every M3 viewer (YAML/describe/logs/secret) renders through,
  and the first pickable M3 slice.
