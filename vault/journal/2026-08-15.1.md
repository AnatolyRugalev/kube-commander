# 2026-08-15 — UX-PLAN: the maintainer gates the tag on a walked user path

- Agent: claude-opus-5 (interactive session with the maintainer)
- Milestone: M5 (planning; no product code changed)
- Did: The maintainer opened with "it's time to release" and, after the state review,
  named four things that must land before `v1.0.0`: a **story** to test UX and user paths
  against, run by a human, with keystrokes logged so the presses can be analysed; the
  tape/GIF re-cut around the main story; stories backed by a **clean k3s setup committed to
  the repo**; and a README/docs pass. Expanded that into ten ordered board slices under M5 —
  **STORY-01** (committed `stories/cluster/` fixture), **STORY-02** (`--keylog`),
  **STORY-03** (`kubecom keys analyze`), **STORY-04** (the stories), **STORY-05** (the
  maintainer's walk, a human task), **STORY-06** (fold-in), **TAPE-01** (re-cut the
  screencast), **DOC-03/04/05** (README landing page, `docs/usage.md`,
  `docs/troubleshooting.md`) — and re-gated the tag on them. Two forks went to the
  maintainer rather than to a default, because both change what gets built: the keylog
  **ships off-by-default and documented** (over a hidden flag or a build tag), and the docs
  **reorganize without a published site**. Orientation data the plan rests on: the July
  dogfood workload that every story and the tape's `shop` filter assume was applied ad hoc
  and **never committed** (journal `2026-07-21.3`), which is exactly the gap the fixture
  slice closes; `app.go:1253`'s `case tea.KeyPressMsg` is the single funnel every press
  already passes through, so the keylog needs no new seam; k3d and docker are both on the
  maintainer's box and k3d *is* k3s-in-docker, so "k3s based" and "clean every run" are the
  same choice. Also confirmed the tree is green and the backlog was otherwise empty — the
  release half of M5 is agent-done and the rc proved the pipeline, so this line is about the
  product, which nothing so far has measured.
- Decisions: D268 — a story is a goal, not a script (and does not name keys, because which
  keys the user reaches for is the measurement); the keylog ships off by default, its
  highest-value output being the *unresolved* press, with the typed-text privacy note
  carried in the doc; the cluster fixture is committed and recreated clean on every run,
  on k3d; the docs reorganize but do not become a site.
- Files: vault/tasks/board.md, vault/knowledge/decisions.md,
  vault/milestones/M5-release.md, vault/human-tasks/2026-07-30-first-release-tag.md,
  this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green (run before and after;
  the `internal/vault` board guards read every edit made here). Vault markdown only, no
  product code, so the binary is untouched and D68's dogfood clause is N/A.
- Next: **STORY-01** — commit the k3s fixture (`stories/cluster/up.sh`, `down.sh`,
  `manifests/`) rebuilding the `shop`/`data`/`broken` namespaces from journal
  `2026-07-21.3`. It is the one slice nothing else depends on being done first, and both
  STORY-04 and TAPE-01 rest on it. STORY-02 (`--keylog`) is independent and could run in
  parallel if a second agent takes it.
