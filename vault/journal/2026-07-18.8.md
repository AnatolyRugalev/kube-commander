# 2026-07-18 — PROC-04: cloud runs commit with the maintainer's git identity

- Agent: claude (Fable 5), interactive with the maintainer
- Milestone: M0 (process, no product code)
- Did: The M0-07 cloud leg committed as `Claude <noreply@anthropic.com>` — fresh
  cloud checkouts don't carry the repo-local identity CLAUDE.md mandates. The
  routine's bootstrap prompt and `/do-rewrite-run` step 0 now both set
  `git config user.name "Anatoly Rugalev"` / `user.email
  "anatoly.rugalev@gmail.com"` before any commit; repo-local config persists in
  the shared checkout so leg subagents inherit it.
- Decisions: none new (enforces the existing CLAUDE.md identity rule).
- Files: `.claude/skills/do-rewrite-run/SKILL.md`, this entry, board.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs only; unchanged code)
- Next: **M0-02** — bump to Go 1.23+, adopt latest cobra, drop `ioutil`.
