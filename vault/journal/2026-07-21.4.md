# 2026-07-21 — FB-ns-menu-seam: namespace picker as the menu's cluster/namespaced seam

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Addressed the oldest dogfood feedback (`2026-07-21-01-namespace-picker-in-menu`):
  surfaced the namespace picker **in the left menu** as the visual seam between
  cluster-scoped and namespace-scoped resources (ctrl+n stays as the shortcut).
  Introduced `menu.Item.Kind` (`ItemResource` default / `ItemNamespace`); the seed
  now inserts one `ItemNamespace` **seam row** right after the `Cluster` section and
  before the first namespaced section. The seam is selectable — drilling into it
  emits the new `menu.NamespaceRequestedMsg`, which the root handles by opening the
  namespace picker (identical to `ns.switch`/ctrl+n, reusing all existing wiring, no
  new action/keymap/registry change). The seam renders the live scope via
  `menu.SetNamespace` ("" → "all namespaces"), which the root keeps current from the
  initial `-n` flag and every picker selection — alongside the status bar and welcome
  page. Discovery `Reconcile` now skips non-resource rows (`Kind != ItemResource`):
  they have no discovered twin and no API group, and selection-preservation resolves
  the seam by kind (it has no GVR); the D57 twin-fill/mark-unavailable and D77
  one-header-per-contiguous-section guarantees are untouched (the seam's empty
  `Section` emits no header and sits un-grouped between the two).
- Decisions: D82 — menu carries non-resource `Item.Kind` rows; the namespace picker
  is a seam row between the cluster-scoped and namespaced sections; Reconcile skips
  non-resource rows; the general "special menu row" shape for future switchers.
- Files: `internal/tui/components/menu/menu.go` (Item.Kind, NamespaceRequestedMsg,
  namespace field + SetNamespace, drill-in branch, Reconcile skip + kind-based
  selection restore, renderNamespace), `internal/tui/components/menu/seed.go` (seam
  insertion), `internal/tui/app.go` (handle NamespaceRequestedMsg; menu.SetNamespace
  on construct + on ns selection), `internal/tui/components/menu/menu_test.go` +
  `internal/tui/tui_test.go` (updated 3 all-item loops to skip the seam; new seam
  placement/drill-in/render/reconcile + app seam-opens-picker + selection-updates-seam
  tests), `vault/knowledge/decisions.md`, board, milestone, deleted the feedback file.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`). Runtime: hermetic —
  drives through the real update loop (seam drill-in → picker) and asserts the
  rendered seam scope; a live-cluster visual check rides the standing dogfood loop.
- Next: continue draining the feedback inbox oldest-first. Next is
  **02 dynamic-menu-crds-per-context-config** — flagged larger-than-a-leg (board
  triage into concrete slices, then do the first). If deferring that, **03
  menu-scroll-and-overflow-broken** is a concrete bug fix.
