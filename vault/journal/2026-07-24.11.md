# 2026-07-24 — SEARCH-01: cluster-search kube primitive + feedback triage

- Agent: claude-opus-4-8
- Milestone: M4 (capability pulled forward by feedback; M3 still the active milestone)
- Did: Drained the oldest-by-filename feedback item
  (`2026-07-24-cluster-search-multi-resource.md`; all 5 inbox items were normal
  priority, and both the board's `Last updated:` line and the prior leg (journal .10)
  named this one as the oldest normal item — consistent with the README's filename
  date-prefix sort, even though git shows the other four were committed a few minutes
  earlier). It's a big multi-leg feature, so per D69 I **triaged it into board tasks +
  did the first slice** and recorded the architecture decision. Triage: SEARCH-01 (this
  kube primitive) done; SEARCH-02 (search mini-app shell + `search.cluster` action),
  SEARCH-03 (streaming results/progress/cap), SEARCH-04 (scope widen + fuzzy/label
  matching) onto the Backlog under a new "Cluster search" subsection. First slice,
  bottom-up (M1/M3 rhythm): `internal/kube/search.go` — `Clients.Search`/`searchRows`
  fan out one-shot **concurrent** server-side `List`s (M1-05a) over a caller-supplied
  `[]Resource`, match `Row.Object.Name` by case-insensitive substring, and stream
  `SearchHit{Resource,ObjectRef}` on a channel. Per-kind failure isolates (List error
  swallowed → no contribution, principle 3); a hit **cap** reserves slots under a mutex
  then cancels the sibling lists once reached; a child ctx closes the channel on
  completion/cap/caller-cancel; a background goroutine owns all sends (principle 1).
  `CommonSearchResources` filters a discovered set to the curated default kinds
  (Pods/Deployments/StatefulSets/DaemonSets/Services/ConfigMaps/Secrets/PVCs/Jobs/
  CronJobs/Ingresses) so the default never enumerates every type (D8/principle 4).
  Recorded the search architecture + default-scope decision (D131). Kube-layer only —
  inert until SEARCH-02 wires the TUI, so no bare-launch surface changed.
- Decisions: D131 — cluster search is a one-shot concurrent fan-out over List,
  curated-scope (current-ns common kinds) by default with whole-cluster/all-ns an opt-in
  widen, per-kind isolation, capped + cancellable, name-substring to start.
- Files: internal/kube/search.go (new: Search, searchRows, sendHit, SearchHit,
  commonSearchGroupKinds, CommonSearchResources), internal/kube/search_test.go (new:
  cross-kind case-insensitive match, empty-query-matches-all, per-kind failure isolation,
  hit cap, cluster-scoped ns="", cancelled-ctx closes channel, curated-set filter),
  vault/knowledge/decisions.md (D131), vault/tasks/board.md (SEARCH-01 done + SEARCH-02…04
  backlog + Last updated), vault/milestones/M4-capabilities.md (search scope bullet + exit
  criterion), vault/feedback/2026-07-24-cluster-search-multi-resource.md (deleted),
  vault/journal/2026-07-24.11.md (new)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  fake-`rowLister` coverage, no cluster (D18): matches stream across two kinds
  case-insensitively; an empty query matches every named row (empty-name rows skipped); a
  forbidden deployments list degrades to nothing while pods still match; the cap emits
  exactly `limit` hits; a cluster-scoped Node is listed with an empty namespace; an
  already-cancelled ctx still closes the channel (no hang); `CommonSearchResources` keeps
  only the curated kinds. No runtime surface changed — the primitive is inert until
  SEARCH-02, so no bare-launch regression.
- Next: SEARCH-02 — search mini-app shell + `search.cluster` action (D11): full-screen
  search view over `kube.Search(CommonSearchResources(discovered), currentNamespace, …)`,
  results list (Kind · ns · name), `enter` drills into the hit, gen-tagged pump + cancel on
  new query/close. Or continue draining the inbox (4 items left; the oldest by commit-time
  is `confirm-modal-yn-keys`, a small self-contained leg).
