# 2026-07-24 — M3-14b-4: exec kubectl parity fallback

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Added the exec parity escape hatch (the last M3-14b slice). `execInto`
  (`internal/tui/exec.go`) now, when the `kubectl` binary is on PATH, suspends into
  **`kubectl exec -i -t <pod> [-c ctr] -- /bin/sh`** via `tea.ExecProcess` instead of the
  in-process SPDY wire (D125); the in-process path becomes the **fallback** used only when
  no kubectl is installed — so exec still works with zero kubectl dependency (#68/D2) but
  uses the battle-tested kubectl PTY/resize/edge-case handling when it is available (D128).
  New **`lookupKubectl`** package-var seam (overridable in tests → deterministic routing
  without a real kubectl on the runner), **`kubectlExecProc`** (builds the `*exec.Cmd` or
  returns false), and a **pure `kubectlExecArgs`** builder that emits `--kubeconfig` /
  `--context` / `-n` only when set so the shelled-out kubectl targets the same cluster
  kubecom launched with. Added a **`WithKubeconfig`** option (+ `m.kubeconfig` field) and
  wired `WithKubeconfig(opts.kubeconfig)` in `run.go` so the explicit `--kubeconfig` is
  forwarded (`WithContext` already carried the context). Container arg omitted when empty
  (kubectl picks the default, matching the SPDY path). → D128.
- Decisions: D128 — exec prefers `kubectl exec` when on PATH (parity), SPDY is the
  no-kubectl fallback; both target the same kubeconfig/context/-n; kubectl lookup is a
  testable seam; does not close the M3 exec exit criterion (still needs a dogfood).
- Files: internal/tui/exec.go (execInto kubectl branch, lookupKubectl seam,
  kubectlExecProc, kubectlExecArgs, os/exec import, header comment), internal/tui/app.go
  (m.kubeconfig field, WithKubeconfig option, WithContext doc), cmd/kubecom/run.go
  (WithKubeconfig wiring), internal/tui/exec_test.go (forceKubectl helper,
  TestKubectlExecProc{WhenPresent,Absent}, TestKubectlExecArgs{Minimal,NamespaceOnly},
  TestExecRoutesToKubectlWhenPresent; slices import), vault/knowledge/decisions.md (D128),
  vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md (note both paths),
  vault/tasks/board.md, vault/journal/2026-07-24.7.md (M3 milestone unchanged — exec exit
  criterion stays unticked pending the dogfood)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New hermetic tests
  cover the fallback without a cluster/kubectl/TTY: with the seam forced present,
  `kubectlExecProc` yields the resolved binary and the full argv
  (`--kubeconfig … --context prod -n web exec -i -t api-1 -c sidecar -- /bin/sh`), and the
  end-to-end exec intent still issues a suspend command **without calling the in-process
  execer**; with the seam forced absent it reports no process (SPDY fallback); the pure
  `kubectlExecArgs` omits unset connection flags and the container. Existing exec/SPDY
  tests unchanged (the SPDY wire is untouched and still exercised when kubectl is absent).
  The **live interactive exec on both paths** — a real shell dropping in, raw-mode
  passthrough, TUI restore, kubectl-vs-SPDY behaviour — is a real-terminal/real-cluster
  check the sandbox can't do; the advisory human-task
  (`vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md`, now covering both paths)
  tracks it and the M3 exec exit criterion stays **unticked**. No bare-launch regression:
  the branch only runs inside an Exec-shell suspend, inert otherwise.
- Next: M3-15 — Edit: `tea.ExecProcess` suspend to `$EDITOR` on the object's YAML, apply
  on save (server-side apply/update); no-change / parse-error degrade without mutating.
  The second (and last) sanctioned suspend action; after it the M3 exit criteria are all
  wired pending the exec/edit dogfood.
