# 2026-07-22 — FB-mouse-optin: mouse capture off by default, opt-in via `mouse.toggle`

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Addressed feedback `2026-07-22-text-selection-select-to-copy` — the app
  captured the mouse on every frame (`View` set `MouseModeCellMotion`
  unconditionally, D86), which stole the terminal's native click-drag
  select-to-copy. Flipped mouse capture **off by default** (new `Model.mouseEnabled`,
  false) so native selection works out of the box; `View` now requests
  `MouseModeCellMotion` only while enabled and leaves `MouseModeNone` otherwise.
  Made mouse opt-in via a runtime keybind: new registry action `mouse.toggle`
  (default `M`, D11), handled in the app-mode branch of `handleAction` (works even
  with the help overlay open). Added a persistent `mouse` status-bar marker
  (`statusbar.SetMouse`) so this invisible mode is always visible. The D86 mouse
  handlers are untouched — they just get no events until capture is on. Recorded the
  call as D97; updated README Usage + regenerated `docs/keybindings.md`. Deleted the
  feedback file.
- Decisions: D97 (mouse off by default, opt-in via `mouse.toggle`; supersedes D86's
  unconditional-capture clause).
- Files: internal/tui/keymap/keymap.go, internal/tui/app.go,
  internal/tui/components/statusbar/statusbar.go, docs/keybindings.md, README.md,
  vault/knowledge/decisions.md, plus tests (tui_test.go, keymap_test.go,
  help_test.go, statusbar_test.go); deleted
  vault/feedback/2026-07-22-text-selection-select-to-copy.md.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` green)
- Next: FB-nav-menu-toggle — first slice of the D96 navigation direction (a
  `menu.toggle` keybind to hide/show the left pane), or M2-13b (column-sort keymap
  wiring). Both are top-unblocked M2 board items.
