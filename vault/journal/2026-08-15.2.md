# 2026-08-15 — STORY-01: the story cluster is committed, and verified end to end

- Agent: claude-opus-5 (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: Committed `stories/cluster/` — `up.sh`, `down.sh` and four manifests rebuilding the
  July dogfood workload that until now existed only in one agent's shell history (journal
  `2026-07-21.3`): `shop` (storefront + ConfigMap, checkout + Secret with two containers,
  logspam, Services, Ingress), `data` (redis StatefulSet + bound PVCs, DaemonSet, a
  completed Job, a CronJob every 2 min) and `broken`. `up.sh` **deletes before it creates**,
  so every run starts identical (D268 pt 3) — a story compared against a drifted cluster
  measures the drift. One design rule worth keeping when the fixture is next edited: the
  five failures in `broken` have five *different* answers — the container's logs
  (crashloop), the pod's events (bad-image, which has no logs at all), the scheduler's
  message (unschedulable), the probe (never-ready, which is Running and never Ready), and
  the claim's own status (an unbindable PVC) — because "a pod is unhealthy, find out why"
  is a story, and a fixture where every failure is a CrashLoopBackOff tests one path five
  times. Guards in a new `internal/stories`: manifests parse, both directions of the
  namespace relation hold, and every string the tape *types as a filter or a grep* must
  appear in the fixture — the last one reads the tape's own `# kubecom-input:` annotations
  rather than line numbers, so TAPE-01 can re-cut the tour without the guard needing to
  know its new shape.
- Decisions: none — D268 pt 3 already fixes the fixture's constraints and this leg
  implements them. The `broken`-namespace rule above is a design note, not a new
  constraint, so it lives here and in the manifest's own comment (D67).
- Files: stories/README.md, stories/cluster/{up.sh,down.sh}, stories/cluster/manifests/
  {00-namespaces,10-shop,20-data,30-broken}.yaml, internal/stories/{doc.go,fixture_test.go},
  vault/tasks/board.md, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. Beyond the gate, and
  beyond what D79 assumed a leg could reach: the maintainer's box has docker, so the whole
  script ran for real. `kubectl apply --dry-run=server` validated all 21 objects against a
  live k3s apiserver; then `up.sh` built the cluster from nothing and every intended state
  came out — `ImagePullBackOff`, `Error`→crashloop, Running-but-0/1-Ready, `Pending`
  unschedulable, `Pending` PVC on the missing class, redis's two PVCs `Bound`, the CronJob
  already through one run, and logspam emitting the `info` line the tape greps for. Both
  new guards were driven red first (a tape filtering for `frontend`; a renamed `shop`
  namespace) and each produced the intended message. `down.sh` then removed the cluster and
  the maintainer's `kubectl` context was restored to `k3d-kubecom-test`, so the machine is
  as it was found. No product code: the binary is untouched and D68's dogfood clause is N/A.
  Diff is ~810 lines, over the ~300 guideline — 404 of it is declarative manifest data and
  265 is the guard, leaving two ~60-line scripts as the logic surface; splitting the fixture
  from the test that keeps it honest would have pushed an unguarded fixture, so it landed
  whole deliberately.
- Next: **STORY-02** — `--keylog`, at the single `case tea.KeyPressMsg` funnel
  (`app.go:1253`), writing JSONL with the resolved action and the mode the press was routed
  through. Independent of STORY-03/04, and the fixture it will be exercised against now
  exists.
