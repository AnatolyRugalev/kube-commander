# 2026-08-09 — HTFOLD: fold in both human-task responses; neither closes

- Agent: antigravity
- Milestone: M4 / M5 (human-task fold-in)
- Did: Folded in the maintainer's responses to two open human tasks. The
  context-switch dogfood answer ("context switch UX is working well") is
  qualitative only — no pt 7 `connect=`/`discovery=`/`total=` numbers, no pt 3-6
  leak-check confirmation, no pt 8 pane-restore reading — so the task stays open,
  CTX-WARM-02/03/04 stay blocked, and the M4 context-switch exit criterion stays
  unticked (D79: no fake green). The release-tag answer ("a bit too early for
  that") defers the tag, so that task stays open and M5-11 stays blocked. Both
  files annotated verbatim + dated; board `Last updated:` and the blocked notes
  updated to match. Also fixed a pre-existing red found while gating this leg:
  THEME-05 (ac51776) made `styles.Match` theme-independent, which made
  `TestSetStylesRepaintsPaintedHighlights` assert an unobservable distinction
  (old vs new theme's highlight bytes are now identical). Rewrote the test to
  observe the painted-cache rebuild through the stamp's `Subtle` color — still
  theme-dependent, and part of the cache — instead of through `Match`.
- Decisions: none (no load-bearing choice changed; existing D79 gating holds)
- Files: vault/human-tasks/2026-07-29-context-switch-live-dogfood.md,
  vault/human-tasks/2026-07-30-first-release-tag.md, vault/tasks/board.md,
  internal/tui/components/logsview/logsview_test.go (pre-existing THEME-05 fix)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (vault-only leg; `make check` still
  run as the canonical gate)
- Next: any unblocked Backlog item — none are blocked by these two tasks
  (CTX-WARM-02/03/04 and M5-11 are the only gated items), e.g. a THEME-05
  follow-up palette or the next M5/M4 backlog item.
