# 2026-07-18 — PROC-03: split models — Sonnet orchestrates, Opus executes legs

- Agent: claude (Fable 5), interactive with the maintainer
- Milestone: M0 (process, no product code)
- Did: The scheduled routine's session model is set to `claude-sonnet-5`
  (orchestration only — negligible tokens), and `/do-rewrite-run` now spawns
  each leg subagent with `model: "opus"`, so legs always execute on Opus 4.8
  regardless of the routine's session model. Routine cron corrected for UTC
  interpretation: `0 1,6,11,16,21 * * *` UTC = 03/08/13/18/23 Amsterdam (CEST).
  (Rebased over the concurrent M0-07 leg, which claimed journal slot `.6`.)
- Decisions: none new (operational config; encoded in the skill + routine).
- Files: `.claude/skills/do-rewrite-run/SKILL.md`, this entry, board.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs only; unchanged code)
- Next: **M0-02** — bump to Go 1.23+, adopt latest cobra, drop `ioutil`.
