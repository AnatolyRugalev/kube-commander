# 2026-07-20 — M2-07d: async discovery on Init → menu reconcile + spinner

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Wired async discovery into the root shell (`internal/tui/app.go`,
  completing the M2-07 group). Added a narrow `Discoverer` seam
  (`StartDiscovery(ctx) <-chan kube.DiscoveryResult`; `*kube.Clients` satisfies it;
  nil → discovery-inert) injected via a new `WithDiscoverer` option mirroring
  `WithWatcher` (D63). `Init` now kicks off discovery when a discoverer is wired,
  deferring the start one message hop (`startDiscoveryMsg`) because a value-receiver
  `Init` can't mutate the model — `startDiscovery` (in `Update`) opens a
  cancellable pass, calls the discoverer, starts the M2-04 status-bar spinner, and
  batches its tick with the M2-02 `discoveryPump`. `spinner.TickMsg` is forwarded
  to `status.Update` so the animation advances (and self-terminates once done).
  `DiscoveryReadyMsg` stops the spinner, cancels the one-shot context, and calls
  `menu.Reconcile` (M2-05b) — a no-op on a total failure so the menu degrades to
  its navigable seed (principle 3). `app.quit` now also cancels the in-flight pass.
- Decisions: D64 — `Discoverer` seam + `WithDiscoverer`; Init defers start via
  `startDiscoveryMsg`; spinner ticks forwarded to the status bar; ready → stop
  spinner + `menu.Reconcile` (no-op/seed on total failure); quit cancels discovery.
- Files: `internal/tui/app.go` (Discoverer/WithDiscoverer, discoverer+
  discoveryCancel fields, `startDiscoveryMsg`, `Init`, `startDiscovery`/
  `handleDiscovery`, spinner.TickMsg forward, quit-cancels-discovery, docs),
  `internal/tui/tui_test.go` (fakeDiscoverer + 4 tests), `vault/knowledge/decisions.md`
  (D64), `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`, this entry.
  No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); `-race` clean
  on the tui package. Drove the real Update loop with a fake discovery channel:
  Init emits `startDiscoveryMsg`; delivering it calls `StartDiscovery`, starts the
  spinner, and issues the batched pump cmd; a `DiscoveryReadyMsg` carrying a CRD
  appends it to the menu (+1 item), stops the spinner, and cancels the pass ctx;
  a total-failure result stops the spinner but leaves the seed menu intact; a model
  with no discoverer has an inert `Init` and never starts the spinner.
- Next: **M2-08** — namespace picker (`internal/tui/components/picker`, bubbles/list):
  a namespace switcher whose selection emits a "namespace changed" msg re-scoping
  the watch (the `m.namespace` field the M2-07c watch already reads). Depends on
  M2-07c (done).
