# 2026-07-21 — FB-menu-config-01: per-context menu config schema + loader

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Drained the next-oldest dogfood feedback (`2026-07-21-02`,
  high — CRDs / non-standard resources need explicit **per-context dynamic menu
  config**). It self-flagged as larger-than-a-leg, so this leg **triaged it into
  board slices** (FB-menu-config-02 menu merge, FB-menu-config-03 app wiring) and
  landed the **first slice**: the config schema + per-context path resolution +
  loader, all in `internal/config`, no UI wiring. Added `MenuConfig`/`MenuResource`
  (the CRD entry format: group/version/resource + optional kind/namespaced/section/
  title; version+resource required, "" group = core), `MenuDir()` →
  `<configdir>/kubecom/menus`, `MenuPath(context)` (sanitizes an arbitrary
  kubeconfig context name to a safe single `.yaml` segment so it can't escape the
  dir; empty context errors), and `LoadMenu`/`LoadMenuFile` mirroring the existing
  `Config` loaders — unknown-field-strict, missing file → zero config (fall back to
  the default menu, principle 3), per-entry validation. Config stays free of the
  kube/menu packages, so no import cycle and the loader is trivially testable; the
  `MenuResource` → `menu.Item` mapping + seed/discovery merge is the next slice.
  Deleted the feedback file.
- Decisions: D83 — per-context menu customization lives in its own file per
  kubeconfig context under `<configdir>/kubecom/menus/<sanitized-context>.yaml`;
  missing file → default menu; context name sanitized (lossy, safety-first); config
  package stays free of kube/menu (merge is a later slice).
- Files: `internal/config/menu.go` (new: schema, MenuDir/MenuPath/menuFileName,
  LoadMenu/LoadMenuFile/parseMenu/validate), `internal/config/menu_test.go` (new),
  `internal/config/doc.go` (package doc updated), `vault/knowledge/decisions.md`
  (D83), board, this entry; deleted `vault/feedback/2026-07-21-02-*.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Pure config
  layer, no runtime surface — no dogfood behavior change this leg (the menu doesn't
  read these files until FB-menu-config-03 wires them).
- Next: continue draining the feedback inbox oldest-first — next is **03
  menu-scroll-and-overflow-broken** (high; a concrete menu-viewport bug fix: clip
  long kind names, keep selection in view, add a scroll indicator). FB-menu-config-02
  (the menu merge) is queued on the board behind the remaining feedback.
