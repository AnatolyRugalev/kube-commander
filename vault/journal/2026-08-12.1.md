# 2026-08-12 — DOC-02: the install docs catch up with the tag that actually exists

- Agent: claude-opus-5 (do-rewrite-leg)
- Milestone: M5
- Did: An Orient-found documentation-truth bug (bug fixes are never gated by a human
  task). `v1.0.0-rc.1` was tagged, published and verified on 2026-08-10 (journal
  `2026-08-10.1`), but that leg touched only `.goreleaser.yml` — so `README.md` still
  opened with "**no version has been tagged yet**" and `docs/install.md` still framed
  every path as "waiting on the first tagged release", both false for two days and four
  Orients. The docs now name the version that exists: the README status note and
  `## Install` lead with the rc and `go install …@v1.0.0-rc.1`, explaining that the
  version must be named because `@latest` skips pre-releases; `docs/install.md` opens
  with where the release stands, promotes the release archive to the recommended path
  (it needs no Go toolchain and is the only artifact carrying real
  version/commit/date ldflags), and splits the old checkout section into `go install`
  and local-checkout paths — the `@v1` explainer stays but is corrected, since the
  reason it fails changed from "no `v1.x.x` tag exists" to "the only one is a
  pre-release, which the query excludes". Two accuracy fixes fell out of the pass: the
  checkout recipe said `cd kube-commander` after cloning into `kubecom` (a copy-paste
  bug from the rename, and the one line in that recipe a reader cannot work around),
  and the package-manager section implied Homebrew/AUR were waiting on *a* tag when
  they are inert on missing credentials (D173 pt 2) and would publish on the next tag
  of any kind once the secrets exist. Also ticked the M5 exit criterion the rc closed —
  "`goreleaser release` produces Linux+macOS artifacts from a tag via CI" — which the
  2026-08-10 leg earned and did not claim; the four archives, four bare binaries and
  `checksums.txt` are on the published release, and the criterion asserts the pipeline,
  which a pre-release tag exercises in full. The tag human task stays **open**: step 2
  (stable `v1.0.0`) is untouched, and it is what the remaining criteria need.
- Decisions: D266 — publishing a release is an install-docs change (D68's trigger misses
  it: a tag changes what the instructions mean without touching what they say); the docs
  name the exact version rather than the state of the tag list; a pre-release is asked
  for by name; publisher inertness is a credential fact, not a pre-release rule; and an
  artifacts-from-CI criterion is closed by the rc while the distributed-v1 ones are not.
- Files: README.md, docs/install.md, vault/milestones/M5-release.md,
  vault/knowledge/decisions.md, vault/human-tasks/2026-07-30-first-release-tag.md,
  vault/tasks/board.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. The two README/docs
  drift guards (`TestReadmeBrewTapMatchesTheCask`, `TestReadmeAURPackageMatchesTheConfig`)
  read both files as one set and still pass, so the tap and package names survived the
  rewrite. The published
  release was checked against the GitHub API, not assumed: `v1.0.0-rc.1`,
  `prerelease: true`, published 2026-08-10T10:08:29Z. Not verifiable here: that the
  `go install` line works from a clean GOPATH — the 2026-08-10 leg verified exactly that
  command, which is why it is the one documented.
- Next: nothing on the board is unblocked (M5-11 waits on the stable tag). The honest
  next leg is whatever a review surfaces; if the maintainer cuts `v1.0.0`, it is the
  fold-in of `2026-07-30-first-release-tag` step 3 — restore `@latest` as the primary
  install line, drop the rc-specific framing D266 pt 3 makes temporary, tick the
  remaining M5 criteria and close the resolved GitHub issues.
