# 2026-08-21 — Feedback: the selection bar under plx (awareness only)

- Agent: claude-opus-5
- Milestone: M5
- Did: A knowledge leg, not a code leg. The feedback file
  `2026-08-21-selection-bar-under-plx.md` filed a diagnosis and explicitly asked
  for **nothing to change** in kubecom — the selection bar's background dying at
  the last glyph, seen dogfooding inside plexos, was plx's own BCE bug and was
  fixed there. Verified the diagnosis against the vendored renderer, sharpened it,
  and wrote it into `vault/knowledge/stack.md` (the "TUI rendering" section) so the
  next agent triages a similar report in one read instead of re-deriving it. The
  feedback file is deleted (D69).
- What the source actually says (checked, not taken on trust —
  `ultraviolet@v0.0.0-20260205113103` `terminal_renderer.go`):
  1. `canClearWith` (l. 575) gates compressing a blank run into `SGR bg` +
     `ECH`/`EL`, and its comment states the `bce` assumption outright: *"This
     assumes that the terminal supports bce terminfo capability."*
  2. Nothing checks it. `hasECH := s.caps.Contains(capECH)` (l. 596), and `caps`
     comes from a `$TERM`-prefix switch (l. ~1560) that hands `allCaps` to a name
     list **including `tmux`**, and `allCaps &^ capREP` to `screen` — both of whose
     terminfos deliberately omit `bce`. So the optimization is keyed off terminal
     *detection*, and for the two multiplexers the detection and the capability
     disagree. That is a sharper statement than the feedback file made, and it is
     what makes the triage rule reliable rather than a hunch.
- Why no code and no decision: kubecom's own output is correct (lipgloss pads with
  styled literal spaces), so there is nothing here to fix or to constrain a future
  leg with — a `Dn` would only record a library's behaviour, which D67 puts in
  knowledge, not in the decisions log. No board item either: per D69 the feedback
  item *is* the leg.
- Triage rule now on record: a full-width styled fill that stops at the last glyph
  under tmux/screen is the multiplexer's BCE handling; reproduce outside it before
  touching any painting code.
- Feedback closed: `2026-08-21-selection-bar-under-plx.md` (deleted, D69). The inbox
  is empty again.
- Files: vault/knowledge/stack.md, vault/tasks/board.md, vault/journal/,
  vault/feedback/ (one file deleted)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`). No code changed, so the
  binary is untouched and there is nothing to dogfood.
- Next: STORY-06i-3 (reverse-selector relations) — the last open STORY-06 slice, and
  with the inbox empty the board is back in charge.
