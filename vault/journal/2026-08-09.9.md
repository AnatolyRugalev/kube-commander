# 2026-08-09 — HT-fold-0809: close the five done human tasks from the maintainer's review

- Agent: kimi-k3 (do-rewrite-leg)
- Milestone: M4 (now **done**) / M5
- Did: The 2026-08-09 review fold-in (a393a1c, D256) flipped five human tasks to
  `Status: done` but left them on disk and never executed the dispositions D256
  wrote. This leg closes them, per each file's own "Next leg" line. (1) The M4
  context-switch exit criterion is ticked on the maintainer's ship-as-is waiver
  (D256 pt 1) after the dogfood confirmed the happy path live — **M4 is done**.
  (2) CTX-WARM-02/03/04 are cancelled on the board (D256 pt 2): the warmth line
  existed to speed up a switch the maintainer has accepted as-is; its section now
  says so and forbids starting them. (3) The goals DoD reads **11 of 13**: the
  context-switcher box ticks on the same waiver, and the CRD/generic-listing box
  ticks in the D231 shape — its two holdouts are both closed (CRD-01 landed
  2026-08-02/D200; the live CRD-heavy driving was declined by the maintainer with
  the hermetic coverage named as the standing verification, D256 pt 3). Each box
  says on what evidence it ticked. (4) The screencast half of the M5 README/docs
  criterion is closed (GIF recorded by the maintainer, 484e60c); the box stays
  unticked on the README half, which only a tag can make true. (5) D177 pt 2's
  Follow+Previous bet gained an inline confirmation — observed against a real
  kubelet on `broken/crashloop` (2,305 restarts), the stream settles. The five
  files are deleted; the two deferred credential tasks (Homebrew/AUR, D256 pt 4)
  and the tag task stay open. Board references to the deleted files were re-pointed
  (LOGS-SEL eye-check → its feedback file; DIAG's parked-wording note → the D256
  pt 3 waiver). No code changed; the guards in `internal/vault` still pass.
- Decisions: none new — every disposition executed here was already recorded in
  D256 (pts 1–3); the CRD goals-box tick applies the D231 precedent to it.
- Files: vault/goals.md, vault/milestones/M4-capabilities.md,
  vault/milestones/M5-release.md, vault/tasks/board.md,
  vault/knowledge/decisions.md, deleted vault/human-tasks/2026-07-29-context-
  switch-live-dogfood.md, …/2026-07-30-previous-logs-crashloop-dogfood.md,
  …/2026-07-30-record-screencast.md, …/2026-08-02-conversion-webhook-reason-
  dogfood.md, …/2026-08-08-app-background-look.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓
- Next: the feedback inbox still holds five items that preempt the board —
  `logs-view-palette-bindings` (palette `:` from the logs view + a better
  previous-logs binding than `ctrl+p`) and `context-switch-key-from-overlays`
  are both keys-captured-by-a-surface items and pair naturally as one keymap
  leg; else oldest-first.
