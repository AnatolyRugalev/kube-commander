# 2026-07-21 — M2-14d: teatest coverage of the error-toast path through the real program

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: The dogfood human-task (`vault/human-tasks/2026-07-20-dogfood-first-usable-build.md`)
  still gates M2-10…M2-13 (and M2-14b via M2-10); the feedback inbox is empty. The
  task explicitly leaves "teatest coverage of existing behavior" unblocked, and the
  prior leg (M2-14c) pre-identified the error-toast path as the remaining gap. Took
  that. The D74/FB-errors-layout fix — a classified error degrading into a transient
  status-bar toast without breaking the fixed browse layout — was covered only by
  hand-threaded direct-`Update` tests (`TestWatchStartErrorSurfaces` et al.), never
  through the running program's composed render. Added
  `TestProgramErrorToastDegradesGracefully` (teatest/v2, M0-05 harness): it drives a
  live `ErrorMsg` into the running program and asserts on the **final model's own
  `View().Content`** — the toast text is present, `status.HasError()`, and the composed
  view is still exactly one screen (identical line count to an error-free model),
  proving the error rendered as an in-layout toast that grew no pane and scrolled
  nothing. It reads the raw View string rather than `teatest.Output()` because the
  whole status bar is `Background`-styled, so its cells (toast included) are written by
  the emulator through a path a plain byte scan misses — the same reason M2-14c asserts
  on the final model. Delivering the `ErrorMsg` directly (rather than inducing it via a
  failing watcher) keeps the test deterministic: it is processed synchronously before
  the queued `Quit`, so the final model always reflects the toast; the watch→`ErrorMsg`
  wiring itself is already covered elsewhere, so what this adds is purely the
  program-level render + layout-preservation assertion.
- Decisions: none (test-only; introduces no constraint a future leg must honor, D67).
- Files: `internal/tui/tui_test.go` (new `TestProgramErrorToastDegradesGracefully`;
  `errors`/`strings` imports), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); new test
  `-race`-clean.
- Next: The explicitly-unblocked existing-behavior teatest carve-out is now essentially
  drained (update loop + menu reconcile M2-14a, filter M2-14c, error-toast M2-14d; the
  only remaining M2-14 slice is M2-14b, the modal flow, which is blocked by the gated
  M2-10). With M2-10…M2-13 all gated by the open dogfood human-task and the feedback
  inbox empty, the loop is **blocked pending the human dogfood pass** — the next leg
  should report blocked on that human task unless feedback is filed or the task is
  closed. Manufacturing further coverage legs would be busywork (skill guardrail).
