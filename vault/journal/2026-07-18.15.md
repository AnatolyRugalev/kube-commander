# 2026-07-18 — M0-06: goreleaser skeleton (Linux+macOS)

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M0
- Did: Reshaped `.goreleaser.yml` from the M0-07/M0-08 stopgap into the release
  skeleton D22/D25 deferred here. Migrated to goreleaser v2 syntax
  (`version: 2`, `archives.ids`/`formats`, `snapshot.version_template`); collapsed
  the three per-OS build blocks into one `kubecom` build over
  `goos:[linux,darwin] × goarch:[amd64,arm64]`; dropped the `kubecom-windows`
  target (D7) and added arm64. Removed all publishers: the `brews` block is
  deprecated in v2 and carried a `kubectl` dep that contradicts D2 — distribution
  (Homebrew/AUR/Docker) is M5's to wire and verify, so the skeleton is now purely
  artifact-building. Verified with `goreleaser check` (v2.17.0, clean) and
  `goreleaser release --snapshot --clean --skip=publish`, which produced exactly
  the four Linux+macOS artifacts (tar.gz + raw binary + checksums), and the built
  binary prints the ldflags-injected version. README left for M5.
- Decisions: D27 (goreleaser skeleton = artifact-only; publishers → M5;
  Linux+macOS × amd64+arm64; v2 syntax).
- Files: `.goreleaser.yml`, `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M0-groundwork.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` green) · `goreleaser
  check` ✓ · snapshot build ✓
- Note on the run: rebased over the concurrent **M0-09** leg (README → vault
  banner), which closed the last M0 exit criterion and took journal slot `.14`;
  this entry moved to `.15`. With M0-06 done, **all M0 exit criteria are met** and
  M0 flips to `done`.
- Next: **M1-01** — client bootstrap (clientset + dynamic + discovery + RESTMapper
  from kubeconfig/context), or **M1-00** (envtest harness) per milestone order.
  M1 is the active milestone.
