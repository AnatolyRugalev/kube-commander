# 2026-07-23 — M3-08a: secret viewer (reveal/decode)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Split M3-08 into M3-08a (reveal/decode viewer) + M3-08b (copy) and did 08a
  (#89). Added a kube-layer `SecretData(ctx, ref) (kube.SecretData, error)`
  (`internal/kube/secret.go`): it Gets the Secret through the **typed clientset**
  (`CoreV1().Secrets`) — unlike the dynamic-client YAML/describe viewers — because the
  typed client decodes the wire base64 into raw bytes (`Secret.Data map[string][]byte`)
  for us, so no manual base64 decode is threaded through and a malformed value can't slip
  by undecoded. It returns `SecretData{Type, Entries []SecretEntry{Key,Value}}` with
  entries **sorted by key** (the API data map has no order); empty name rejected,
  NotFound/RBAC wrapped never panicked (principle 3). Wired a `SecretGetter` shell seam
  (`WithSecretGetter`; `*kube.Clients` satisfies it) mirroring the YAML/describe getters.
  `handleRowAction`'s `rowActionSecret` case → `openSecretViewer`: shows the shared M3-01
  viewer immediately (kind `secret`), fetches off the update loop tagged with a fresh
  `viewerGen` (superseded/stale result dropped — `handleSecretLoaded`), and seeds content
  **masked** (`secretRevealed=false` on every open — the deliberate-reveal contract). A
  fetch error degrades to a toast + closes the viewer (D74). `renderSecret(data,
  revealed)` renders a `Type:`/`Data:` header then one line per key: masked shows `key:
  •••••••• (N bytes)` (length, not content — nothing leaks pre-reveal); revealed shows the
  decoded value (a multi-line value indented under its key). Reveal is a **registered
  keymap action** `secret.reveal` (`r`, D11 — no raw-key match), handled in
  `handleViewerAction` gated on `viewer.Kind()==viewerKindSecret` (inert on the other
  viewers and off-viewer, exactly like `logs.follow`/`f`); toggling re-renders the same
  fetched data (no re-fetch). Launcher wires `WithSecretGetter(clients)`. Regenerated
  `docs/keybindings.md` (new `## secret` group) via `make keys-doc`; bumped the keymap
  FullHelp column-count test (10→11 groups). → D113.
- Decisions: D113 — the secret viewer opens masked; values revealed only by the deliberate
  `secret.reveal` (`r`) gesture (registered action, viewer-gated); decode via the typed
  clientset (already-decoded `.Data`), entries key-sorted; `WithSecretGetter` gates it;
  reveal masks/unmasks all at once — per-entry copy is M3-08b.
- Files: internal/kube/secret.go (SecretData + SecretData/SecretEntry types),
  internal/kube/secret_test.go (secretWith helper + 4 tests: decode+sort, empty, empty-name,
  not-found), internal/tui/app.go (SecretGetter seam + WithSecretGetter, secretGetter/
  secretData/secretRevealed fields, viewerKindSecret, secretLoadedMsg, openSecretViewer,
  handleSecretLoaded, renderSecret, secret.reveal branch in handleViewerAction,
  secretLoadedMsg Update dispatch, rowActionSecret case, fmt/strings imports),
  internal/tui/keymap/keymap.go (ActionRevealSecret const/meta/default `r`),
  internal/tui/keymap/help_test.go (11-group count), internal/tui/tui_test.go
  (fakeSecretGetter + secretViewerModel/openSecret helpers + 6 tests),
  cmd/kubecom/run.go (WithSecretGetter wiring), docs/keybindings.md (regenerated),
  vault/knowledge/decisions.md (D113), vault/knowledge/keybindings.md (secret.reveal row),
  vault/tasks/board.md, vault/milestones/M3-actions-viewers.md, vault/journal/2026-07-23.8.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New kube tests
  drive SecretData (2-key decode + key-sort, empty-data non-nil, empty-name reject,
  NotFound wrap). New tui tests drive the full path: the secret intent opens the viewer +
  fetches the selected row → lands masked (key + mask shown, value NOT shown) → `r` reveals
  the value → `r` re-masks it; a fetch error closes the viewer + toasts; no getter → inert
  (no open, no fetch); esc closes; a stale-gen fetch is dropped (value never shown even
  after reveal); `secret.reveal` is inert on the YAML viewer. Runtime surface: the actions
  menu's "Reveal secret" over a live Secret row now opens the masked viewer; `r` toggles
  reveal. A human live-cluster visual confirm (a real Opaque/tls Secret, a multi-line cert
  value, a binary value) is a dogfood follow-up (D68), not reproducible in the sandbox — no
  bare-launch regression (the viewer opens only on the action). M3 milestone: the secret
  exit criterion stays unticked (reveal done, copy is M3-08b); Status stays in-progress.
- Next: M3-08b — copy the revealed secret value to the clipboard (#89). Needs a per-entry
  selection model over the secret entries (08a reveals all at once) and a copy mechanism —
  the mouse/clipboard story (D86/D97) or an OSC-52 write; decide in the leg. Ticks the M3
  secret exit criterion.
