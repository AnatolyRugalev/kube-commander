# 2026-07-21 — Dogfood the first usable build: human-task done, feedback filed

- Agent: claude (Opus 4.8), interactive session with the maintainer
- Milestone: M2 (dogfood of the running binary; no product code changed)
- Did: Set up a real cluster for the maintainer to dogfood against and drained the
  resulting feedback. Spun up k3d cluster `k3d-kubecom-test` (1 server + 2 agents,
  k3s v1.35.5), installed `kubectl`/`k3d` to `~/.local/bin`, applied a mixed
  workload set across 3 namespaces — `shop` (nginx storefront + ConfigMap volume,
  http-echo checkout + Secret, chatty logspam, Services, Ingress), `data`
  (redis StatefulSet + bound PVCs, DaemonSet, completed Job, 2-min CronJob), and
  `broken` (CrashLoopBackOff, ErrImagePull, unschedulable/Pending, failing
  readiness probe, Pending PVC on a missing StorageClass) so healthy and unhealthy
  states are both reachable. Built `kubecom` from `v1` and the maintainer ran it
  against the cluster: **it launches and resources browse** — the first-usable
  experience is confirmed, so the dogfood human-task is resolved and deleted (this
  unblocks the M2-10..13 slices the two concurrent M2-14c/M2-14d legs had reported
  themselves blocked on). Captured 8 feedback items into `vault/feedback/` (indexed
  to preserve the maintainer's order): namespace picker in the menu as the
  cluster/namespaced seam; explicit CRD/non-standard types via per-context dynamic
  menu config in the config dir; broken menu scroll + overflow items wrapping
  outside the pane (screenshots); Esc → back to left pane; two menu item states
  (cursor vs opened); bottom key-hint bar; help as a popup overlay not a full-screen
  replacement; mouse support. Also added `/kubecom` to `.gitignore` (dogfood build
  artifact was untracked and un-ignored).
- Decisions: none (feedback captured for the loop to triage; the CRD one, item 02,
  is larger-than-a-leg and flags itself for board triage).
- Files: deleted `vault/human-tasks/2026-07-20-dogfood-first-usable-build.md`;
  added `vault/feedback/2026-07-21-0{1..8}-*.md`; `.gitignore`; this entry.
- Verify: docs/vault + `.gitignore` only, no code touched (build/test unaffected).
  Runtime dogfood: maintainer confirmed the binary launches and browses live
  resources against `k3d-kubecom-test`.
- Next: feedback now preempts the board (D69) — drain `vault/feedback/` oldest-first
  starting with **01 namespace-picker-in-menu**; **02 dynamic-menu-crds** needs
  board triage rather than a single leg.
