# 2026-07-21 — FB-ns-seam-followup: namespace seam dropdown + all-namespaces picker sentinel

- Agent: claude (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Addressed the high-priority dogfood feedback
  (`2026-07-21-09-namespace-seam-followup`, high — picked ahead of the normal-priority
  05/06/07/08 items because D69 lets highest-`Priority` preempt oldest-first, and its
  third fix was a functional dead-end). Three fixes:
  (1) **Seam reads like a dropdown.** `renderNamespace` dropped the literal
  `"Namespace: "` label for a `namespaceArrow` (`▾ `) prefix + value, so the row reads
  `▾ (all)` / `▾ kube-system` like a collapsed dropdown rather than a labelled field.
  (2) **Unscoped renders `(all)`.** `namespaceAll` const changed `"all namespaces"` →
  `"(all)"` (menu package only; the welcome page keeps its sentence wording — different
  surface, out of the feedback's scope).
  (3) **Fixed the picker dead-end.** The app launches unscoped, but the namespace
  picker only listed concrete namespaces — once a namespace was picked there was no way
  back to the all-namespaces view without restarting. `handleNamespacesLoaded` now pins
  an `all namespaces` sentinel (`namespaceAllItem`) at the top of the seeded list, and
  `handleNamespaceSelected` maps that value back to the empty scope (re-scopes the live
  watch to every namespace). The sentinel can never collide with a real namespace — DNS
  labels cannot contain a space.
- Decisions: none (no new binding constraint; the sentinel-maps-to-empty-scope and the
  `(all)`/`▾` seam wording are local UX choices, recorded here not in decisions.md per
  D67). Deleted the feedback file.
- Files: `internal/tui/components/menu/menu.go` (`namespaceAll`/`namespaceArrow` consts,
  `renderNamespace`, `SetNamespace` doc), `internal/tui/components/menu/menu_test.go`
  (seam-render asserts `▾ (all)`/`▾ <ns>`), `internal/tui/app.go` (`namespaceAllItem`
  const, `handleNamespacesLoaded` prepend, `handleNamespaceSelected` sentinel→empty
  mapping), `internal/tui/tui_test.go` (seed-count now 3 + sentinel pinned,
  capture-input steps to `default`, `menuSeamNamespace` helper reads `(all)`, new
  `TestNamespaceAllSentinelResetsScope`), board, this entry; deleted
  `vault/feedback/2026-07-21-09-namespace-seam-followup.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`). Runtime: hermetic — the
  tests drive the seam render and the sentinel round-trip (scope into a namespace, then
  the sentinel returns to unscoped) through the real update loop; a live-cluster visual
  confirmation of the dropdown glyph rides the standing dogfood loop.
- Next: continue draining the feedback inbox — remaining items **05 menu-two-item-states**
  (nav cursor vs. opened/active as two visual states), **06 status-bar key hints**
  (persistent bottom hint bar), **07 help-as-popup** (reuse the M2-08a modal overlay),
  **08 mouse-support** (all normal priority). Take 05 next unless a higher-priority item
  arrives.
