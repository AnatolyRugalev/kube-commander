# 2026-07-23 — M3-04: describe viewer wired

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Wired the second M3 viewer — the `res.describe`/`d` action (and its
  actions-menu entry) now opens the shared M3-01 `viewer.Model` over the selected
  row and streams in the object's `kubectl describe` output. Applied D108's viewer
  pattern verbatim: a narrow `Describer` seam (single `Describe`, wired via
  `WithDescriber`; `*kube.Clients` satisfies it via M1-07b), `handleRowAction` now
  branches `rowActionDescribe` → `openDescribeViewer` which shows the viewer
  immediately (empty, so the gesture feels instant) and issues the `Describe` render
  off the update loop as a Cmd, seeding content when the typed `describeLoadedMsg`
  lands. A render error degrades to a status-bar toast + closes the viewer (D74). The
  input-capture / `overlayCenter` compositing / `overlayActive` machinery is already
  shared (the viewer is one component), so nothing there changed — only the new open
  path + seam were added. Two notable deltas from M3-03's YAML wiring: (1) `Describe`
  takes **no context** (kubectl's describe package has no context-aware entry point,
  D2 describe note), so the seam matches that shape and the `viewerGen` guard is the
  sole staleness defence — there is no cancellation to thread; (2) both viewer
  fetches share the single `viewerGen`, so opening a describe view supersedes any
  in-flight YAML fetch and vice-versa (one shared viewer ⇒ one generation counter).
  Launcher wires `WithDescriber(clients)`. Remaining `rowAction`s (logs/edit/delete/…)
  still fall through to the "not yet available" toast until their own legs.
- Decisions: none — a faithful application of D108 (the M3 viewer seam/async-fetch/
  gen-guard/degrade/capture pattern, which already names describe as a follower);
  the no-context Describe seam and the shared-viewerGen invariant are implementation
  detail (journal, not decisions — D67).
- Files: internal/tui/app.go (Describer seam + WithDescriber, describer field,
  handleRowAction rowActionDescribe branch, describeLoadedMsg + openDescribeViewer +
  handleDescribeLoaded, describeLoadedMsg Update case), cmd/kubecom/run.go
  (WithDescriber wiring), internal/tui/tui_test.go (fakeDescriber + 5 tests:
  open+content, close, render-error degrade, inert-without-describer, stale-gen drop),
  vault/tasks/board.md (M3-04 → Done; M3-05 next)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests drive
  the full path (`d` → intent → viewer open + Describe render → content composited
  over the base menu), esc-close, an error toast + close on Describe failure, the
  no-describer inert case, and the generation guard dropping a stale render. Runtime
  surface: `d` (or the actions menu) over a live table now opens a scrollable describe
  pager for the selected object against the real cluster; a human live-cluster visual
  confirm (real describe output, scroll feel) is a dogfood follow-up (D68), not
  reproducible in the sandbox — no bare-launch regression (viewer opens only on the
  action, the browse view is untouched). M3 milestone Status unchanged — the
  logs/describe/YAML exit criterion is compound and logs (M3-05..07) are not yet wired.
- Next: M3-05 — Logs viewer (initial, no follow): `res.logs`/`L` → `kube.Logs`
  (M1-07c) streamed into the shared viewer via a channel→msg pump (M2-02/D53), a
  generation guard dropping a superseded stream's lines. Pods first. This is the first
  *streaming* viewer, so it extends D108's one-shot-fetch shape with the pump rhythm.
