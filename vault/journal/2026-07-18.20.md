# 2026-07-18 — M1-04: on-disk discovery cache + invalidation

- Agent: claude-opus (Opus 4.8)
- Milestone: M1
- Did: Rewired `NewClients` to back discovery with kubectl's on-disk
  `CachedDiscoveryClient` (`discovery/cached/disk`) instead of the M1-02
  in-memory memcache — completing the "cache discovery on disk" half of D8.
  New `internal/kube/cache.go` computes a per-host cache dir under
  `os.UserCacheDir()/kubecom` (disposable, separate from the D20 config dir),
  TTL 6h; falls back to in-memory cache when no cache dir resolves (degrade,
  don't crash). Added `Clients.Invalidate()` to force a refetch (clears the disk
  cache + Resets the retained deferred RESTMapper). Still zero network I/O at
  construction. Split "lazy group detail on first open" out to M1-04b (M2-coupled).
- Decisions: D32 (diskcached client, per-host dir, TTL 6h + explicit Invalidate,
  memory fallback, Discovery field widened to CachedDiscoveryInterface).
- Files: `internal/kube/cache.go` (new), `internal/kube/cache_test.go` (new),
  `internal/kube/client.go`, `go.mod`, `go.sum`, `vault/knowledge/{decisions,stack}.md`,
  `vault/tasks/board.md`, `vault/milestones/M1-kube-layer.md`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues; hermetic —
  disk-write path is envtest/runtime, not in the gate)
- Next: **M1-05** — server-side Table List+Watch → event channel; reconnect/resync.
  (M1-04b — lazy group detail — parked until the M2 menu exists.)
