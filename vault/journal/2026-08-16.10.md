# 2026-08-16 — STORY-06j-1: the painted follow indicator (`styles.Follow`)

- Agent: opencode (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: Folded in `2026-08-15-logs-follow-visual-signal.md`, the S03 high-priority
  finding that live output is indistinguishable from a frozen snapshot — the only
  cue was the header's `[following]`/`[paused]` word, "a word in a status line
  that the eye skips". First slice of STORY-06j (06j's two fixes smallest first),
  split on pickup with 06j-2 (scroll-past-end re-arms follow) and 06j-3
  (newest-first, weighed) written back to Backlog. **`styles.Follow` is a new
  role** on the Styles set: bold everywhere, and on a dark canvas it paints canvas
  ink on the palette's Success green — the exact Match rule (D252 pt 3), measured
  4.69–11.03:1 over the dark built-ins and gated by the palette's own polarity
  (`IsDark`), so on a light canvas it degrades to bold alone (no palette-native
  shade on Success clears the floor, 2.96–4.29:1). **The logs header now renders
  `[following]` through that badge** and `[paused]` through `Header` as today: the
  token is looked up in the already-clipped text (intact or gone with the tail —
  a narrow header degrades to plain rather than paint a broken span), and the
  prefix/suffix each get their own `Header` render so the badge's reset does not
  leave the trailing text in the terminal default. `TestFollowBadgeIsDistinguishable`
  (themes_test) holds every built-in to the paint-on-dark / weight-on-light rule,
  and `TestFollowStateBadgeAndPausedPlain` (logsview_test) pins that live paints
  `[following]` and paused does not. README's logs paragraph names the badge. The
  feedback file is deleted (D69).
- Decisions: **D280** — the follow state is a `styles.Follow` role (not a per-view
  colour), the paint rule is Match's polarity-gated one (canvas-on-Success on
  dark, bold alone on light), and the header composes the badge mid-line by
  clipping first and re-rendering the neighbours.
- Files: internal/tui/styles/styles.go + styles_test.go (Follow role, followStyle),
  internal/tui/styles/themes_test.go (TestFollowBadgeIsDistinguishable),
  internal/tui/components/logsview/logsview.go (header badge composition),
  internal/tui/components/logsview/logsview_test.go (followSpan helper,
  TestFollowStateBadgeAndPausedPlain), README.md, vault/tasks/board.md,
  vault/milestones/M5-release.md, vault/knowledge/decisions.md,
  vault/feedback/2026-08-15-logs-follow-visual-signal.md (deleted), this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. New hermetic
  tests pin the badge's shape across every built-in theme (dark canvases paint
  canvas-on-Success and clear 4.5:1; light canvases paint nothing and keep bold)
  and the header's behaviour (live renders `[following]` through the Follow badge,
  paused renders plain with no badge anywhere; the follow-state tests that assert
  on plain text still pass). **Live sanity**: the binary builds and launches
  (`kubecom --help`). The badge's real-cluster appearance — that a live stream
  reads as live at a glance — is a human's dogfood, noted here rather than
  claimed (D79).
- Next: **STORY-06j-2** — scrolling down past the last log line re-arms follow
  (`2026-08-15-logs-scroll-past-end-resumes-follow.md`, Priority high): the same
  re-arm `G` gives, replacing the behaviour `TestDownwardScrollDoesNotResumeFollowing`
  pins; or **STORY-06h** (the rich describe panel, Priority high). Either is the
  top-unblocked high-priority fold-in after this slice.
