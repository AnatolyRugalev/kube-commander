# 2026-08-10 — RC-PRERELEASE: pre-release tags must publish as pre-releases

- Agent: orchestrator (do-rewrite-run), on the maintainer's "let's tag release candidate"
- Milestone: M5
- Did: The first release tag `v1.0.0-rc.1` was pushed (maintainer-approved act; the tag
  itself is a human's, D173 pt 1). The release workflow ran clean — `make check` on both
  platforms, then `goreleaser release` uploaded four archives, four bare binaries and
  `checksums.txt`, with release notes generated from the 0.7.6→v1.0.0-rc.1 span (211
  lines, Features/Fixes/Other groups, zero `chore(board): claim` lines, D185 filters
  working). Verification found one gap: the release was published with
  `isPrerelease: false` because `.goreleaser.yml` never set `release.prerelease` (the
  default is `false`, not `auto`). Fixed two ways: (1) the live release was re-marked
  pre-release via `gh release edit` — a release flag is editable, unlike the tag; (2)
  the config now sets `prerelease: auto` so any future `-rc`/`-beta`/etc. tag
  self-marks and a stable `v1.0.0` does not. Other checks passed: the downloaded binary
  reports the real version/commit/date, and `go install
  github.com/neuroplastio/kubecom/cmd/kubecom@v1.0.0-rc.1` works from a clean GOPATH
  (module path installable remotely; the "dev" report from a module-built binary is
  by-design — only goreleaser's ldflags stamp real values, version.go).
- Decisions: none new — this is a config correction under existing D173/D185; no Dn
  needed, the behavior it locks is stated in the `.goreleaser.yml` comment.
- Files: .goreleaser.yml
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`); `goreleaser check` ✓
- Next: M5-11 (`v1` → `main`) — the tag human task stays open until `v1.0.0` lands;
  the rc stands as evidence a release now exists.
