# 2026-07-24 — FB-delete-key-d: default delete key → `d`, describe → `D`

- Agent: claude-opus (Opus 4.8)
- Milestone: M3
- Did: Addressed feedback `2026-07-24-delete-default-key-d`. Changed the shipped
  default keymap so `res.delete` binds `d` (vim `dd` muscle memory, was `x`) and
  moved `res.describe` off `d` to `D` (capital, read-only, not a reserved nav
  chord) to resolve the collision. Registry-driven and rebindable (D11) — only
  `defaultBindings`, the generated doc, and the design-intent table changed; no
  view matches a raw key. `x` is now unbound by default. Recorded D133, which also
  pins `d`/`D` as settled so the coupled `unify-yaml-view-and-edit` feedback lays
  its freed key out against this surface (no conflicting one-offs). Deleted the
  feedback file.
- Decisions: D133 — delete=`d`, describe=`D`; `d`/`D` settled for the coming
  yaml/edit-unify leg.
- Files: internal/tui/keymap/keymap.go (defaultBindings), docs/keybindings.md
  (regenerated via `make keys-doc`), internal/tui/tui_test.go (describeKey/
  deleteKey helper vars), vault/knowledge/keybindings.md, vault/knowledge/decisions.md,
  vault/tasks/board.md, vault/feedback/2026-07-24-delete-default-key-d.md (deleted).
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (make check green). Keymap-default
  change; runtime behavior is a key relabel, not a logic change — covered by the
  existing delete/describe flow tests via the updated helper vars.
- Next: FB-unify-yaml-view-and-edit — collapse `res.yaml` (`y`) + `res.edit` (`e`)
  into one editable-object action (open YAML in `$EDITOR`, apply on save via the
  M3-15a `Clients.Update` path), laying the freed key out against D133; describe/
  logs stay read-only. Needs M3-15b (edit suspend flow) first or folded in.
