# 2026-07-18 — M0-05: teatest TUI smoke harness

- Agent: claude (Opus 4.8)
- Milestone: M0
- Did: Landed the first TUI test harness — a teatest smoke test that drives a
  model, asserts rendered output, and shuts the program down cleanly. Adopting
  bubbletea (D19) forced the first real TUI deps: pinned **bubbletea
  `charm.land/bubbletea/v2 v2.0.2`** (the v2 module was rebranded off the github
  path) + **teatest/v2**, which bumps the Go floor 1.23 → **1.24.2** (D23's
  reserved "dependency forces it" case; v2.0.3+ would jump to 1.25, so held at
  v2.0.2). Added a placeholder root model (`internal/tui/tui.go`) that renders a
  splash and matches no key literals (D11) — the test quits via `tea.Quit`, not a
  keystroke. `make check` green.
- Decisions: D26 — bubbletea v2 module rebrand + pin v2.0.2, Go floor → 1.24.2,
  placeholder-model rationale, v2/teatest API notes (also mirrored into stack.md).
  (Renumbered from D25 after a concurrent M0-08 leg took D25.)
- Files: `internal/tui/tui.go`, `internal/tui/tui_test.go`, `internal/tui/doc.go`,
  `go.mod`, `go.sum`, `vault/knowledge/decisions.md`, `vault/knowledge/stack.md`,
  `vault/tasks/board.md`, `vault/milestones/M0-groundwork.md`.
- Verify: build ✓ · test ✓ (TestModelSmoke passes) · vet ✓ · lint ✓ (0 issues)
- Next: **M0-06** — goreleaser skeleton reshaped to Linux+macOS only (M0-08 left
  the Windows target/brews/AUR for this leg); teatest + M0-08 clear the runway.
