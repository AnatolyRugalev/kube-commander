# 2026-07-20 — REVIEW-03: progress review + vault-hygiene cleanup

- Agent: claude (Opus 4.8), interactive review with the maintainer
- Milestone: cross-cutting (process; no product code)
- Did: Reviewed ~2 days of autonomous loops (54 legs, M0 done, M1 feature-complete,
  M2 ~60%). Verdict: healthy — tree green, binary runs, `internal/kube` has zero
  TUI imports (verified), ~1:1 test:prod LOC. Applied three course corrections:
  (1) **Decision-log inflation** (65 D's for 54 legs, recent ones were
  implementation notes) — added the D67 guardrail to CLAUDE.md + the leg skill:
  a `Dn` is a constraint, not a changelog; implementation detail goes in the
  journal. (2) **Status-line bloat** — the M1 `Status:` line and the board's
  `Last updated:` line had become multi-KB per-leg narratives; compressed both to
  one sentence and added the D67 rule to keep them terse. (3) **M1 closure** —
  declared M1 feature-complete (D66): ticked the two fake-covered / zero-import
  criteria, marked the restricted-RBAC criterion `[~]` deferred, and moved the
  live-apiserver integration tests to a tracked backlog item **M1-INT** so M1
  stops sitting perpetually "in-progress". Also **collapsed the board's Done
  section** — every completed leg had a full journal-length paragraph (the board
  had grown to ~100KB, read on every orient); each is now a one-liner (id ·
  title · done-date · decision refs), full detail staying in the journal. Board:
  ~100KB → ~14KB, 53 items preserved.
- Decisions: D66 (M1 completion bar; envtest tests deferred) · D67 (vault hygiene).
- Files: `CLAUDE.md`, `.claude/skills/do-rewrite-leg/SKILL.md`,
  `vault/milestones/M1-kube-layer.md`, `vault/tasks/board.md`,
  `vault/knowledge/decisions.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/vault only; no code touched)
- Next: **M2-08b** — picker filtering (the top-unblocked M2 slice).
