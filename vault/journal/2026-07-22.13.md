# 2026-07-22 — FB-nav-menu-toggle: hide/show the left menu pane

- Agent: claude-opus-4-8
- Milestone: M2
- Did: First slice of the D96 navigation direction (the left pane is not a permanent
  fixture). Added a `menu.toggle` registry action (default `m`, rebindable — D11) that
  hides/shows the left resource-menu pane at runtime, handled in the app-global branch
  of `handleAction` (alongside `mouse.toggle`) so it works whichever pane is focused. A
  new `menuHidden` flag on the root model (session-only, touched only from the update
  loop — principle 1) gates the layout: `resize()` hands the full width to the table,
  `browseBody()` renders the table alone (no `JoinHorizontal` with the menu), and
  `inMenu()` returns false so mouse routing sends every click/wheel to the table.
  Focus follows visibility — hiding moves focus to the table (a hidden pane can't hold
  focus), showing returns it to the menu; the same key re-shows the menu, so it is
  never a one-way door even before the pane-free command-palette switch lands. Known
  gap by design: with the menu hidden there is no pane-free way to change the browsed
  resource yet — FB-nav-resource-palette closes that. `docs/keybindings.md` regenerated
  (new `menu` group). README Usage documents the `m` toggle. Kept `menu.toggle` out of
  the curated short-help hint (it appears in the `?` full-help overlay + docs), matching
  the mouse.toggle precedent and keeping the hint line lean. → D99.
- Decisions: D99 — the left menu is toggleable (`menu.toggle`); a hidden menu is
  zero-width and cannot hold focus; focus follows visibility; the fixed-pane half may
  later be superseded by FB-nav-menu-popup's overlay, keeping the toggle + focus
  contract.
- Files: internal/tui/keymap/keymap.go (+ActionToggleMenu/meta/binding),
  internal/tui/keymap/{keymap,help}_test.go, internal/tui/app.go (menuHidden field,
  toggleMenu, handleAction case, resize/inMenu/browseBody gating),
  internal/tui/tui_test.go (TestMenuToggle), docs/keybindings.md, README.md,
  vault/knowledge/decisions.md (D99), vault/tasks/board.md,
  vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New tests:
  keymap `m`→menu.toggle resolution; FullHelp now 6 groups (5 with nav disabled);
  app TestMenuToggle drives `m` to hide (table focused, menu gone from the view,
  `inMenu(0)` false) then re-show (menu focused, sections render again). Runtime
  surface: `m` hides/shows the left pane against a real cluster; a human visual/dogfood
  confirm of the wide-table look is a D68 follow-up — no bare-launch regression (menu
  starts shown, default behaviour unchanged).
- Next: FB-nav-resource-palette — the command-palette (`:`-style) resource switch that
  makes the hidden-menu state fully usable (pick/filter a kind and switch the table
  without the left pane); reuses the M2-08a picker + `overlayCenter` (D95). Then
  FB-nav-menu-popup folds the toggled menu into an overlay. (M2-14b still gated on M3
  confirm wiring, D88.)
