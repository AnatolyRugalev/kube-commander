# 2026-07-24 — FB-confirm-yn-keys: confirm modal accepts y/n as registered actions

- Agent: claude-opus
- Milestone: M3
- Did: Addressed feedback `2026-07-24-confirm-modal-yn-keys` — the confirm modal now
  accepts `y` to confirm and `n` to decline (the universal yes/no muscle memory),
  alongside the existing `enter`/`esc`. Reconciled with D11 (no hard-coded keys) by
  introducing **key contexts** in the keymap: two registered, rebindable actions
  `confirm.accept` (`y`/`enter`) and `confirm.decline` (`n`/`esc`) resolve in a
  dedicated **confirm** context so they don't collide with `y`/`n`'s browse meaning
  (res.yaml / app.searchNext). `build` now partitions bindings per context
  (`bySeq` browse + sequencer, `confirmBySeq` for the modal via a new `ConfirmAction`).
  The app routes confirm-modal keys through `routeModalConfirmKey` (ConfirmAction
  first, browse-keymap fallback for `app.quit`); `modal.Update` accepts/declines on
  the new actions or the prompt-mode nav keys. Supersedes the "no y/n" part of D88/D115.
- Decisions: D132 (key contexts; confirm modal `y`/`n`; supersedes the no-y/n of D88/D115).
- Files: internal/tui/keymap/keymap.go (actions, defaults, context partitioning,
  ConfirmAction), internal/tui/components/modal/modal.go (accept/decline on the new
  actions + doc), internal/tui/app.go (routeModalConfirmKey + dispatch),
  docs/keybindings.md (regenerated `confirm` group), keymap/keymap_test.go &
  keymap/help_test.go & tui_test.go (tests), vault (D132, board, feedback deleted).
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓  (prompt-mode paths unchanged; delete-flow
  teatest still green; new unit tests for `y`/`n` accept/decline + context resolution).
- Next: **FB-delete-default-key-d** — make the default delete key `d` (needs describe
  moved off `d`); coordinate with FB-unify-yaml-view-and-edit per that feedback so the
  action-key layout lands coherently. Or continue draining the feedback inbox (2 items).
