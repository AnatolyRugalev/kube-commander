# 2026-08-15 — Feedback: the story instrument resets the cluster but not kubecom's state — planted `stories/cluster/run.sh`

- Agent: opencode (interactive, STORY-05 walk prep)
- Milestone: M5 (UX-validation line, D268)
- Feedback: `2026-08-15-stories-clean-kubecom-state` — the maintainer, mid-S01, pointed out
  that `up.sh` only makes the *cluster* identical: kubecom's own user state
  (`~/.config/kubecom`: config + per-context menus/state — pins, previous container, the
  welcome-screen flag; `~/.cache/kubecom`: discovery cache + log) carries over between runs,
  so a story walked after a story measures leaked state, and a walk pollutes the
  maintainer's real config. The tape already wipes a throwaway XDG dir for the same reason
  (D261).
- Did: planted `stories/cluster/run.sh`, the story-run wrapper: it launches kubecom against
  the story cluster with a **throwaway user dir** — `XDG_CONFIG_HOME`/`XDG_CACHE_HOME`
  under `$TMPDIR/kubecom-stories/<id>`, wiped before every launch — so a walk starts with no
  config, menus/state, discovery cache or log history and never touches the real
  `~/.config/kubecom`. Trace defaults to `~/traces/<id>.jsonl` (outside the wipe, so it
  survives and `keys analyze` can read it back); a caller-passed `--keylog` wins. Resolves
  the binary from the repo's `bin/kubecom` or PATH, warns when the current kube-context
  isn't the story cluster, and echoes the analyze command on exit. Wired into the walk:
  `stories/README.md`'s "Running one" now leads with `run.sh sXX`, `up.sh`'s footer suggests
  it, and the STORY-05 human task's walk block uses it. `make check`-irrelevant (no Go change);
  smoke-tested via `bash -n` and a `--help` launch (throwaway dir created, trace path
  resolved, binary found). Feedback file deleted — addressed.
- Decisions: none — this follows D268 pt 3 (identical starts) and D261 (the tape's XDG trick);
  the wrapper is the home for the clean-dir guarantee, matching the walk's shape.
- Files: stories/cluster/run.sh (new), stories/cluster/up.sh, stories/README.md,
  vault/human-tasks/2026-08-15-walk-the-stories.md, deleted vault/feedback/2026-08-15-stories-clean-kubecom-state.md,
  this entry.
- Verify: `bash -n` ✓ · `--help` smoke run ✓ (throwaway dir at `/tmp/kubecom-stories/s01`,
  trace resolved to `~/traces/s01.jsonl`, repo `bin/kubecom` picked). No Go surface changed,
  so the `make check` gate is unaffected.
- Next: the STORY-05 walk itself — the maintainer now walks S01…S05 with `./stories/cluster/run.sh sXX`.
  The next *agent* leg after the walk returns is STORY-06 (fold the findings in).
