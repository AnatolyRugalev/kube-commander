# 2026-07-18 — PROC-02: make scheduled runs work from fresh cloud checkouts

- Agent: claude (Fable 5), interactive with the maintainer
- Milestone: M0 (process, no product code)
- Did: First routine run failed with "Unknown command: /do-rewrite-run" — cloud
  environments clone the **default branch (`master`)**, which has none of the
  process files, so no skills exist at session init. Fixes: `/do-rewrite-run`
  gained a priming step 0 (checkout `v1`, ensure golangci-lint), and its
  subagent prompt now tells legs to read
  `.claude/skills/do-rewrite-leg/SKILL.md` by path instead of relying on
  slash-command registration. The routine's Instructions field is prose that
  bootstraps `v1` before following the skill (not a bare slash command).
- Decisions: none new (operationalizes D21).
- Files: `.claude/skills/do-rewrite-run/SKILL.md`, this entry, board.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs only; unchanged code)
- Next: **M0-07** — delete the legacy trees, prune `go.mod`, drop the
  `.golangci.yml` path excludes (D14).
