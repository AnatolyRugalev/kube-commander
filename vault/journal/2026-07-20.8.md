# 2026-07-20 — M2-08a: generic modal picker component over bubbles/list

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Split M2-08 (namespace picker) into 08a/08b/08c and landed **08a**: a new
  generic modal picker component (`internal/tui/components/picker`) — the fourth
  `components/*` package. It chooses one string value from a set, wrapping
  `bubbles/list` for cursor + pagination management but **driven entirely through
  keymap actions, never raw keys** (D11): `Update(keymap.Action)` calls the list's
  public cursor methods (up/down/top/bottom/page), and the list's own key bindings +
  chrome (title/help/status/pagination/filter/quit) are disabled so no hard-coded key
  leaks — the picker frames + titles the list itself through the shared styles (Header
  title, PaneFocus border, Selection cursor row via a one-line `itemDelegate`). It is
  kind-stamped ("namespace" first) for reuse as the later context/container/port
  pickers. Drill-in emits `picker.SelectedMsg{Kind,Value}`, back emits
  `picker.CancelledMsg{Kind}` — owned here (D56); the picker does not hide itself
  (the root model will, in 08c). `SetSize` takes the full screen size; `View` centers
  a clamped modal box with `lipgloss.Place`; hidden/unsized → `""`.
- Decisions: D65 — generic kind-stamped picker over bubbles/list; keymap-action
  driven (list keys/chrome disabled, D11); emitter owns SelectedMsg/CancelledMsg
  (D56); modal centered via lipgloss.Place; component doesn't self-hide. Two new
  indirect deps (atotto/clipboard, sahilm/fuzzy) via list/textinput, no version drift.
- Files: `internal/tui/components/picker/picker.go` (new), `internal/tui/components/
  picker/picker_test.go` (new), `go.mod`/`go.sum` (+2 indirect), `vault/knowledge/
  decisions.md` (D65), `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`,
  this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); `-race` clean
  on the picker package. Rendered the sized+seeded picker end-to-end: a centered,
  accent-bordered modal titled "Namespace" with the first item highlighted
  (Selection background) and the rest plain. Tests: hidden-empty-view, show renders
  title+items, first item selected after SetItems, navigation moves the cursor
  (down/bottom/top/up-clamped), drill-in emits SelectedMsg{namespace,value}, back
  emits CancelledMsg, inactive picker ignores actions, empty picker drill-in is a
  no-op.
- Next: **M2-08b** — add incremental filtering to the picker (bubbles/list native
  filter or a `bubbles/textinput`), driven so no raw key leaks past the text field
  (`/` opens, matches narrow, esc clears). Then **M2-08c** wires the namespace picker
  into the app shell (a `ns.switch` keymap action + a `NamespaceLister` seam; select
  re-scopes the live watch via `m.namespace`; regenerate `docs/keybindings.md`).
