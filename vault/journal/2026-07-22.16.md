# 2026-07-22 — REVIEW-06: progress check + board Done re-collapse (D102)

- Agent: claude (Opus 4.8), interactive progress review
- Milestone: cross-cutting (process; no product code)
- Did: Reviewed the loop's work since the four dogfood feedback items. All four
  landed correctly and verified in-tree: popups now composite over the base browse
  via `overlayCenter` (FB-popups-overlay); mouse capture is off by default, opt-in
  via `mouse.toggle`=`M` (FB-mouse-optin, D97); the menu pane width is capped
  (FB-left-pane-width-smaller); the status bar moved to the top and shows the
  browsed resource type (FB-status-bar-top). The status-bar item's nav direction
  was triaged into real work — `menu.toggle` (`m`), a `resources.switch` (`:`)
  command palette component, and the menu-as-popup idea retired/folded into the
  palette (D99/D100/D101) — plus M2-13b column sort wired. Tree green, binary
  builds, both inboxes empty. One drift found + fixed: the board's Done list had
  reswelled to ~50KB (35/93 entries were paragraphs) because D67 named only the
  Status/Last-updated lines; re-collapsed the Done section to one line per item
  (93 preserved, ~50KB → ~17KB) and extended the rule (D102) to cover Done entries
  in CLAUDE.md + the leg skill.
- Decisions: D102 (board Done entries are one line; extends D67).
- Files: `vault/tasks/board.md`, `CLAUDE.md`,
  `.claude/skills/do-rewrite-leg/SKILL.md`, `vault/knowledge/decisions.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/vault only; no code touched)
- Next: loop continues M2 (M2-14 teatest, remaining slices). A second human
  dogfood pass is worth doing — a batch of UI changed since the last one.
