# 2026-07-24 — M3-13b: port-forward panel

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Completed M3-13b, the port-forward listing panel, finishing the M3-13 manager
  (13a landed start + background lifecycle + stop-on-exit). The prior run had pushed the
  **claim commit only** (912f475) — it bundled the `forwards.panel`/`forwards.stopAll`
  keymap entries + a help_test group-count bump but **left HEAD red** (the generated
  `docs/keybindings.md` was never regenerated, so `TestKeybindingsDoc` failed) and never
  implemented the panel. This leg implemented the panel and got the tree green.
  The panel is **inline overlay state** (not a new component) — `forwardsPanel bool` +
  `forwardsSel int` on the root model, mirroring the M3-08b secret entry-cursor precedent
  (D114) since it reads `m.forwards` directly. `forwards.panel` (`F`) opens it from the
  browse view via a new `openForwardsPanel` (app-global, not row-scoped — forwards outlive
  the row they started on); while it is up `handleForwardsPanelAction` captures input like
  the help/viewer overlays: `nav.up`/`nav.down` move the cursor (`clampForwardsSel` keeps
  it in range), `nav.drillIn` stops the selected forward (calls its `cancel()`; the
  **existing M3-13a `forwardDoneMsg` flow** removes the entry, flashes `stopped …`, and
  re-clamps the cursor), `forwards.stopAll` (`X`) calls `stopForwards()` (cancel-all +
  clear) plus a `stopped all port-forwards` notice, and `forwards.panel`/`nav.back`/
  `app.quit` close it; everything else is swallowed. `overlayActive()` now includes the
  panel (mouse inert while up); `View` composites `forwardsPanelView` centered
  (`overlayCenter`, D95). The panel renders one line per forward — label · bound
  (`forwardPortsLabel`) or requested ports · `ready`/`starting…` — the cursor row in
  Selection style, an empty-state line when none, and a footer of its keys. Regenerated
  `docs/keybindings.md` (`make keys-doc`) to un-red the tree.
- Decisions: none — applies existing patterns (overlay capture like help/viewer, inline
  cursor state like the secret viewer D114, `overlayCenter` compositing D95, the M3-13a
  D122 lifecycle for stop). No new binding constraint (D67).
- Files: internal/tui/app.go (forwardsPanel/forwardsSel fields, openForwardsPanel,
  clampForwardsSel, handleForwardsPanelAction, forwardsPanelView, ActionForwards case in
  handleAction + panel-capture branch, ActionForwards View case, overlayActive, clamp call
  in handleForwardDone), internal/tui/tui_test.go (startForwardOnPod/startForwardOnPod2
  helpers + TestForwardsPanelToggle/Lists/StopSelected/StopAll/CursorMoves),
  docs/keybindings.md (regenerated), vault/knowledge/keybindings.md (forwards rows),
  vault/tasks/board.md, vault/milestones/M3-actions-viewers.md (port-forward exit criterion
  ticked), vault/journal/2026-07-24.1.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues; the pre-existing
  stale-doc failure is fixed). New tui tests drive the full path: `F` opens the panel (an
  active overlay) and `F`/esc close it; the panel shows the empty-state line with none
  active and lists a ready forward's label + bound `localhost:8080 → 80` + `ready`;
  `nav.drillIn` cancels the selected forward's context (entry stays until its Done lands,
  then is dropped); `forwards.stopAll` cancels every context, clears the set immediately,
  and reports the sweep, and is inert (no notice) with nothing active; `nav.up`/`down`
  move the cursor over two forwards and clamp at both ends. Runtime surface: `F` from the
  browse view now opens a panel listing the background forwards started via the M3-13a
  action; `enter` stops the selected one, `X` stops all. A human live-cluster confirm (real
  forwards carrying traffic, the panel updating as one dies) is a dogfood follow-up (D68),
  not reproducible in the sandbox — no bare-launch regression (the panel opens only on `F`,
  and is empty until a forward is started). M3 milestone: the **port-forward exit criterion
  is now met and ticked** (background · listed · stop cleanly on exit); Status stays
  in-progress (M3-13c Service→pod, exec M3-14, edit M3-15 remain).
- Next: M3-13c — Port-forward for Services: resolve a Service to a backing endpoint pod
  before forwarding (mirrors M3-07b's `PodForOwner`), then re-add Service to the
  Port-forward action's applicability (M3-13a narrowed it to Pod-only).
