# 2026-07-20 — M2-07a: root app model / shell (keymap-routed skeleton)

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Replaced the M0 `internal/tui/tui.go` placeholder with the real root model
  in `internal/tui/app.go` — the M2 app shell's first slice. The root `tea.Model`
  owns the resolved `*keymap.Keymap` and the single `*keymap.Sequencer` (only
  mutable input state; pointer so its buffered prefix survives Bubble Tea's
  value-model copy, touched only from the single-threaded `Update` — principle 1).
  Every `tea.KeyPressMsg` is resolved through `seq.Input(msg.Key())` to an `Action`
  — the model matches no raw key (D11). `ResultPending` schedules
  `tea.Tick(keymap.SequenceTimeout)` → a private `seqTimeoutMsg` → `seq.Timeout()`
  (the keymap package runs no timer, D48); each tick is **generation-tagged**
  (`seqGen`, bumped on every new pending) so a stale timer superseded by a newer
  pending is dropped instead of firing the wrong action's short form early.
  Actions serviced: `app.quit`→`tea.Quit`, `app.help`→toggle the M2-01d help
  overlay, `nav.back`→close it when open; nav/filter/search inert until the panes
  land. `New()` uses `DefaultKeymap`; `NewWithKeymap(*Keymap)` accepts a
  config-merged keymap so `tui` needn't import `config`. `View` is empty until the
  first `WindowSizeMsg`, then a placeholder body (or the help overlay) + a
  registry-generated short-help hint.
- Decisions: D61 — root model owns the keymap + Sequencer (pointer, single-threaded);
  model schedules the sequence-timeout tick, generation-tagged to drop stale ticks;
  services quit/help/back, nav/filter inert until panes land; `New`/`NewWithKeymap`
  keep `tui` config-free.
- Files: `internal/tui/app.go` (new root model), `internal/tui/tui.go` (removed),
  `internal/tui/doc.go` (package doc updated), `internal/tui/tui_test.go` (M0-05
  smoke carried onto the real model + white-box key-routing/help/sequence/stale-tick
  tests), `vault/knowledge/decisions.md` (D61), `vault/tasks/board.md`,
  `vault/milestones/M2-core-tui.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). teatest smoke
  drives the real model end-to-end (renders `kubecom`, quits cleanly); white-box
  tests cover quit-key→`tea.Quit`, help toggle + esc-close, `g` buffers-and-ticks
  with a matching gen, `gg` resolve then stale first-tick ignored, empty-until-sized.
- Next: **M2-07b** — compose the two-pane browse layout (menu | table) in the root
  model, with focus switching between panes; then 07c wires the live watch (M2-02
  pump feeding `table.SetTable`/`ApplyEvent`) and 07d the discovery reconcile +
  status-bar spinner.
