# 2026-07-22 — M2-11b-1: per-context state store

- Agent: claude-opus-4-8
- Milestone: M2
- Did: Split M2-11b (mirroring the M2-11a primitive-first rhythm) and took the
  first slice — the persistence store, no app wiring. Added `internal/config/state.go`:
  `State{LastNamespace}`, `StateDir()` (`…/kubecom/state`), `StatePath(context)`
  (reuses `menuFileName` sanitization so a context name can't escape the dir; empty
  context errors), `LoadState`/`LoadStateFile` (missing → zero, strict unknown-field),
  and `Save`/`SaveFile` (atomic 0o600). Extracted the atomic-write body from
  `Config.SaveFile` into a shared package-private `atomicWriteFile` so both writers
  are identical by construction. Decided last-namespace lives in a **dedicated
  per-context state file**, not config.yaml (not per-context) and not the menu file
  (user-authored — kubecom must not clobber a hand-edited file) → D90.
- Decisions: D90 — per-context runtime state gets its own `state/<context>.yaml`
  store; kubecom-written runtime values never go into config.yaml or a menu file.
- Files: internal/config/state.go, internal/config/state_test.go,
  internal/config/config.go (extracted `atomicWriteFile`), vault/knowledge/decisions.md,
  vault/tasks/board.md, vault/milestones/M2-core-tui.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic —
  new tests round-trip Save→LoadState and SaveFile→LoadStateFile (empty→zero,
  parent-dir creation + 0o600 perm, atomic overwrite with no leftover temp),
  context-name sanitization stays inside StateDir, unknown-field + empty-context
  rejection. No runtime surface changed (config-package only), so the running binary
  is unaffected.
- Next: M2-11b-2 — wire the launcher to load the context's state on start and use
  `LastNamespace` as the initial watch scope when `-n` is not given, and add a
  namespace-persister seam to the app so `handleNamespaceSelected` writes through
  `State.SaveFile` (explicit `-n` overrides stored state for that run; record it).
