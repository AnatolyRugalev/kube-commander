# 2026-07-18 — M1-02: static seed RESTMapper for instant start

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Added `internal/kube/seed.go` — a static `meta.DefaultRESTMapper` seeded
  with ~28 core GVKs (core/v1, apps/v1, batch/v1, networking/v1, rbac/v1,
  storage/v1), each with its exact plural/singular resource and scope
  (`AddSpecific`, so irregular plurals like NetworkPolicy→networkpolicies and
  Endpoints→endpoints stay kubectl-identical). `NewClients` now composes it
  **ahead of** the D29 deferred discovery mapper via
  `meta.FirstHitRESTMapper{MultiRESTMapper:{seed, deferred}}`: seeded kinds
  resolve instantly with zero network I/O; unknown kinds (CRDs, uncommon groups)
  fall through to discovery. This is the "instant start" half of D8 — M1-03 adds
  the async full-discovery reconcile signal.
- Decisions: D30 (seed mapper shape; exact GVRs via AddSpecific; FirstHitRESTMapper
  seed-first composition; curated not exhaustive; permanent layer).
- Files: `internal/kube/seed.go`, `internal/kube/seed_test.go`,
  `internal/kube/client.go`, `vault/knowledge/decisions.md`, board, milestone,
  this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓  (envtest N/A — gate off)
- Next: **M1-03** — async full discovery → reconcile signal; per-group fault
  isolation (#87, #76). Background discovery that reconciles the menu beyond the
  seed set and isolates a failing/denied API group to itself.
