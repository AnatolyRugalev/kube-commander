# 2026-07-24 — M3-15b: Edit — $EDITOR suspend + apply machinery (unify-yaml-view-and-edit, slice 1)

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Drained the feedback inbox (D69): `2026-07-24-unify-yaml-view-and-edit` ("don't
  separate view YAML and edit — one editable object view") is larger than one leg, so I
  converted it into board tasks and did the first slice. This leg lands **M3-15b** — the
  Edit → `$EDITOR` **suspend + apply machinery** (`internal/tui/edit.go`, mirroring the
  exec suspend rhythm): `res.edit`/`e` fetches the object's YAML off the update loop
  (reusing the existing M1-07a `YAMLGetter`) → `editFetchedMsg`; `handleEditFetched`
  suspends via `tea.Exec` into `$EDITOR` over a temp file (`editCommand`, off the loop),
  reads the buffer back, and — only if it changed — applies it through the new `Editor`
  seam (`kube.Update`, M3-15a/D129). No-change (edited bytes == fetched), editor abort,
  and apply rejection all degrade to a status-bar toast without mutating (principle 3);
  kube.Update carries the resourceVersion so a stale edit is a Conflict, not a clobber.
  `$EDITOR` resolves `KUBE_EDITOR`→`EDITOR`→`vi`, space-split so `code -w` works. Wired
  `WithEditor(clients)` in `run.go`, the `editor` model field, the two message cases, and
  `rowActionEdit`→`openEdit`. The unification proper (retire the read-only YAML viewer,
  collapse to one key against the D133 `d`/`D` layout) is filed as **M3-15c**. Deleted the
  feedback file.
- Decisions: D135 — View YAML + Edit unify into one object-YAML action (`$EDITOR`
  edit-in-place); the standalone read-only YAML viewer is retired (M3-15c); describe/logs
  stay read-only. Supersedes the M3-03-vs-M3-15 split.
- Files: internal/tui/edit.go (new: Editor seam, WithEditor, lookup/resolveEditorArgv,
  runEditor seam, openEdit/handleEditFetched/handleEditDone, editCommand),
  internal/tui/edit_test.go (new: editor-argv precedence, inert guards, fetch→suspend,
  fetch-error degrade, apply-on-change, no-change skip, editor-error/apply-error, done
  branches), internal/tui/app.go (editor field, editFetchedMsg/editDoneMsg cases,
  rowActionEdit dispatch), cmd/kubecom/run.go (WithEditor wiring),
  vault/knowledge/decisions.md (D135), vault/tasks/board.md (M3-15b done, M3-15c added,
  feedback triaged), vault/human-tasks/2026-07-24-edit-live-cluster-dogfood.md (new,
  advisory), vault/feedback/2026-07-24-unify-yaml-view-and-edit.md (deleted)
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic coverage,
  no cluster/editor/TTY: a `runEditor` seam simulates the editor (writes the buffer / a
  no-op / an abort) so the temp-file round-trip, no-change detection, apply-on-change with
  the round-tripped bytes, editor-abort-no-apply, and apply-error-surfaces all run without
  a process; `resolveEditorArgv` precedence is pure-tested; the fetch→suspend routing and
  the three result branches are driven through the update loop. The **live interactive
  `$EDITOR` suspend + real apply** (a full-screen editor owning the TTY, TUI restore,
  actual cluster mutation) is a real-terminal/real-cluster check the sandbox can't do —
  the advisory dogfood human-task tracks it and the M3 "Edit round-trips through `$EDITOR`"
  exit criterion stays **unticked**. No bare-launch regression: the branch only runs inside
  an Edit suspend, inert otherwise (and inert entirely with no editor/getter wired).
- Next: M3-15c — retire the read-only YAML viewer path (`openYAMLViewer`/`yamlLoadedMsg`/
  `handleYAMLLoaded`/`viewerKindYAML` + `rowActionYAML`/`ActionYAML`) and collapse the
  surface to one object-YAML key (keep the `YAMLGetter` seam; regenerate
  `docs/keybindings.md`; update the YAML-viewer tests to the edit flow). Completes D135.
