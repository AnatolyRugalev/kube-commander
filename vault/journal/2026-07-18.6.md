# 2026-07-18 — M0-07: delete legacy trees, prune go.mod, drop lint excludes

- Agent: claude (Sonnet 5), autonomous leg
- Milestone: M0
- Did: Deleted `app/`, `cli/`, `commander/`, `config/`, `pb/`, `cmd/kube-commander/`
  (incl. the Windows-only `app/executor/cmd_windows.go`), `.travis.yml`, and the
  snap CI files (`ci/snap-deps.sh`, `ci/snap.login.enc`) per D14. Ran `go mod
  tidy` — no code under `cmd/kubecom`/`internal/` imports anything external yet,
  so `go.mod`/`go.sum` are now empty of requires (cobra/client-go come back
  fresh in M0-02/M1); dropped the now-empty `replace vbom.ml/util` line.
  Removed the legacy-tree path excludes from `.golangci.yml` (D12 has no targets
  left). Fixed `.goreleaser.yml`, which the deletion broke: ldflags pointed at
  the deleted `cli.version` symbol (now `internal/version.Version`, the var
  already built for this) and a build id + `snapcrafts:` block existed only to
  package the now-gone `cmd/kube-commander` binary (removed both). Left the
  `kubecom-windows` build target and `aur`/`brews` publishers alone — the
  Linux+macOS-only release redesign is M0-06's job, not this leg's.
- Decisions: D22 — scope boundary for this leg's `.goreleaser.yml` touch-up
  (unblock what deletion broke; don't redesign the release matrix here).
- Files: deleted trees above; `go.mod`, `go.sum`, `.golangci.yml`,
  `.goreleaser.yml`, `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M0-groundwork.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` green; also confirmed
  `.golangci.yml`/`.goreleaser.yml` still parse as valid YAML)
- Next: **M0-02** — bump to Go 1.23+, adopt latest cobra (wire `cmd/kubecom`
  onto it), drop `ioutil` usage (now trivial with the legacy trees gone).
