# 2026-07-24 — M3-14b-2: exec multi-container picker reuse

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Generalised the M3-07a container-resolution path so the **exec** flow reuses the
  shared container picker (`ctrPicker`) instead of always execing the pod's default
  container (M3-14b-1). Introduced a `ctrPurpose` (`ctrPurposeLogs`/`ctrPurposeExec`)
  threaded through `resolveContainersFor(res, podRef, purpose)`, the async
  `containersLoadedMsg`, and the picker stash (renamed `logStreamRes/Ref` →
  `ctrStreamRes/ctrStreamRef`, plus a new `ctrPurpose` field). A new **`streamOrExec`**
  is the single terminal that routes a resolved container to `streamLogsInto` (logs) or
  the new **`execInto`** (exec). `openExec` no longer suspends directly — it calls
  `resolveContainersFor(..., ctrPurposeExec)`: a single-container Pod (or no
  `ContainerLister` wired → empty container, the 14b-1 fallback) execs directly; a
  multi-container Pod prompts via the reused picker (titled "Exec container" to
  disambiguate from "Logs container"), and the pick suspends into a shell on the chosen
  container. `newExecCommand` now takes the chosen container. No new seam — the existing
  `ContainerLister` (M3-07a) is reused. → D126.
- Decisions: D126 — container resolution is purpose-tagged (logs↔exec), routes via
  `streamOrExec`, and the shared `ctrPicker` is disambiguated by `ctrPurpose` (one picker,
  never a second; future container-picking actions add a purpose arm, not a Kind branch).
- Files: internal/tui/exec.go (openExec → resolveContainersFor(exec), new execInto,
  newExecCommand gains a container param, header comment), internal/tui/app.go (ctrPurpose
  type + label/pickerTitle, containersLoadedMsg.purpose, resolveContainersFor purpose arg,
  new streamOrExec, handleContainersLoaded/handleContainerSelected purpose-routed,
  logStreamRes/Ref → ctrStreamRes/ctrStreamRef + ctrPurpose field, openLogsViewer/
  handlePodResolved pass ctrPurposeLogs), internal/tui/exec_test.go (newExecCommand call
  sites fixed; TestExec{SingleContainerExecsDirectly,MultiContainerOpensPicker,
  ContainerPickSuspendsIntoSession,ContainerResolveErrorDegrades},
  TestExecCommandRunUsesChosenContainer; tea + picker imports), vault/knowledge/decisions.md
  (D126), vault/tasks/board.md, vault/journal/2026-07-24.5.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). New hermetic tests
  drive the exec picker-reuse without a cluster/TTY: a single-container Pod resolves then
  suspends directly (non-nil tea.Exec cmd, no picker); a multi-container Pod opens the
  picker stamped `ctrPurposeExec` and starts no exec until a pick; a pick closes the picker
  and suspends into a session (never opening the logs viewer — proves the purpose route);
  a `PodContainers` error degrades to a status-bar toast; `execCommand.Run` passes the
  chosen container through to `kube.Exec`. Existing logs container-picker tests still pass
  unchanged (the shared path is behaviour-preserving for logs). The **live interactive
  exec** — real shell drop-in, raw-mode passthrough, TUI restore, and now the
  multi-container prompt against a real pod — is still a real-terminal/real-cluster check
  the sandbox can't do: the M3-14b-1 dogfood human-task
  (`vault/human-tasks/2026-07-24-exec-live-cluster-dogfood.md`, Blocks: none — advisory)
  covers it; the M3 exec exit criterion stays **unticked** pending that run. No bare-launch
  regression: exec only fires from a Pod's actions menu and is inert until picked.
- Next: M3-14b-3 — Exec live terminal resize: feed SIGWINCH into the exec size queue so
  the remote PTY tracks the local window mid-session (14b-1 seeds only the initial size).
