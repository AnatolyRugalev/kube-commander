# 2026-07-23 — FB-k9s-not-prior-art: reword README k9s "prior art" line

- Agent: claude-opus-4-8
- Milestone: M3 (feedback, preempts the board — D69)
- Did: Drained the human feedback `vault/feedback/2026-07-23-k9s-not-prior-art.md`,
  which lands mid-leg (arrived while I rebased my M3-11a claim, so per D69 it preempts
  the board — I released the M3-11a claim back to Backlog and addressed this instead).
  The README "Special thanks" list called k9s "prior art in the Kubernetes-TUI space";
  since kube-commander and k9s both emerged ~2019–2020, k9s is a **contemporary / peer**,
  not a predecessor. Reworded that one line to "a contemporary Kubernetes TUI in the same
  space." Left the other four k9s references untouched (the feedback confirms they are
  accurate and non-chronological): the "simpler and more discoverable than k9s" comparison,
  the "not cloning k9s" non-goal, the "tview (what k9s uses)" note, and the k9s-`:`-style
  palette references. Recorded the framing as D118 so a future docs leg does not
  reintroduce "prior art". Also folded in a piece of board hygiene that this session had
  already started: M3-11 (cordon/uncordon + drain) was oversized for one leg, so it is now
  split into **M3-11a** (cordon/uncordon — idempotent, no confirm, mirrors the D117 seam
  pattern) and **M3-11b** (drain — confirm + eviction progress stream + cancel-on-quit),
  both in Backlog; M3-11a is the top-unblocked next pick.
- Decisions: D118 — k9s is framed as a contemporary/peer of kube-commander, not "prior
  art"; do not describe a peer TUI as a predecessor in docs/README.
- Files: README.md (Special thanks k9s line), vault/knowledge/decisions.md (D118),
  vault/feedback/2026-07-23-k9s-not-prior-art.md (deleted), vault/tasks/board.md
  (Done entry, M3-11 split into M3-11a/M3-11b, Last updated), vault/journal/2026-07-23.12.md
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues) — a docs-only
  change, no code touched, so nothing to exercise at runtime; the binary is unchanged and
  still launchable (no bare-launch regression, D68). The install/usage sections of the
  README are untouched — only the thanks wording changed — so they still match the binary.
- Next: M3-11a — Cordon/uncordon wired: a `Cordoner` seam (`kube.Cordon`/`Uncordon`,
  M1-06c) on Node rows, dispatched directly (idempotent → no confirm, unlike scale/delete),
  result to the status bar via a `cordonDoneMsg` → notice/toast; wire `WithCordoner` in
  run.go. Then M3-11b (drain, the confirm + progress-stream piece).
