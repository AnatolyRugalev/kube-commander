## DOC-01b: Prose pass over the restructured README

> **Renamed 2026-08-12 by BOARD-03 (D267).** This leg was written, committed and indexed as
> `DOC-02`, and the agent-found install-docs leg took the same id three days later. `DOC-01b`
> is the id on the board — this is the prose half of the feedback DOC-01 was the outline half
> of. The commits below still say `DOC-02` and are not rewritten: `9e8ef4d` (claim), `16c39db`
> (`docs: condense README capability sections (DOC-02)`) and `c4e6990`.

I picked up `DOC-02` after determining `CTX-MEM-03` was too complex to handle safely in a single leg without further clarity on the mechanism for persisting cursor view state.

- I ran a full prose pass on the capabilities section of `README.md`.
- Removed repetitive text like "Press X to...".
- Condensed the text to ensure each paragraph states what the capability does and when to use it.
- Kept the failure-mode paragraphs intact as requested.
- Verified the change with `make check` (tree is green).
- Pushed `DOC-02` to `v1`.

Next up should be continuing `THEME-05` (styles.Match legibility).
