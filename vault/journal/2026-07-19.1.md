# 2026-07-19 — M1-06d: cronjob suspend/resume actions

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M1
- Did: Added `Clients.Suspend` / `Clients.Resume` to `internal/kube/actions.go` —
  merge-patch a CronJob's `spec.suspend` (true/false) through the dynamic client,
  exactly as `kubectl patch cronjob` does. A near-exact mirror of M1-06c
  cordon/uncordon (D37): shared `resourceInterface` addressing, shared `setSuspend`
  body, pure `suspendPatch` wire format, `suspendNoun`/`suspendVerb` error verbs.
  CronJobs are namespaced so the ref's namespace is honored; resume writes concrete
  `false` (not null); idempotent → no UID guard; empty name rejected; NotFound
  wrapped (#86). Fourth of the M1-06 action set; only 06e drain remains.
- Decisions: D38 — cronjob suspend/resume as a generic `spec.suspend` merge patch.
- Files: `internal/kube/actions.go`, `internal/kube/actions_test.go`,
  `vault/knowledge/decisions.md`, `vault/tasks/board.md`,
  `vault/milestones/M1-kube-layer.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues)
- Next: **M1-06e** — drain (node; policy/v1 Eviction API — evict pods, skip
  DaemonSet/mirror/completed, PDB-aware 429-retry, wait for deletion); cordons
  first (reuses M1-06c). Its own green leg; then **M1-07** logs/describe/YAML.
