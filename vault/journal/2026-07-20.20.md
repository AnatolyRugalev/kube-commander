# 2026-07-20 — REVIEW-05 / PROC-06: human-task queue (`vault/human-tasks/`)

- Agent: claude (Opus 4.8), interactive with the maintainer
- Milestone: cross-cutting (process; no product code)
- Did: Reviewed progress (M2-RUN landed → binary launches; all 3 feedback items
  drained with quality fixes; M2-08 picker + M2-09a filter done; tree green;
  verified the v2 alt-screen handling D70 and the klog/alt-screen fix D71 are
  correct). Key gap: no human has run kubecom against a real cluster — the loop
  can't. Built the fix the maintainer asked for: **`vault/human-tasks/`**, an
  agent → human queue (inverse of the D69 feedback inbox) whose open items can
  **block** named board tasks or a whole milestone. Every leg's Orient lists it;
  an open `Blocks:` gates Pick; if it blocks all work the agent **stops and
  reports** (never busywork); a `Status: done` task gets its result folded in and
  the file deleted; the agent **raises** tasks here instead of faking a green it
  can't earn. Wired into CLAUDE.md (leg loop + hard rule), the `do-rewrite-leg`
  skill (Orient/Pick/Verify), the `do-rewrite-run` orchestrator (blocked → end
  run + push-notify), and `vault/README.md`. **Seeded** the first task: a human
  dogfood pass of the first-usable build, `Blocks: M2-10, M2-11, M2-12, M2-13`
  (the new feature slices) — but not feedback, the in-flight filter (M2-09b), or
  teatest (M2-14), so the loop stays productive while it waits.
- Decisions: D79 (human-task queue; can block board/milestone).
- Files: `vault/human-tasks/README.md`,
  `vault/human-tasks/2026-07-20-dogfood-first-usable-build.md`, `CLAUDE.md`,
  `.claude/skills/do-rewrite-leg/SKILL.md`, `.claude/skills/do-rewrite-run/SKILL.md`,
  `vault/README.md`, `vault/knowledge/decisions.md`, `vault/tasks/board.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (docs/vault only; no code touched)
- Next: **human** — do the dogfood task. Loop meanwhile: feedback → M2-09b →
  M2-14 (M2-10…13 gated until the dogfood task closes).
