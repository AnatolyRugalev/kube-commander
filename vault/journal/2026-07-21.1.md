# 2026-07-21 — M2-14c: teatest coverage of the filter flow through the real program

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: The dogfood human-task (`vault/human-tasks/2026-07-20-dogfood-first-usable-build.md`)
  gates M2-10…M2-13 but explicitly leaves "teatest coverage of existing behavior"
  unblocked; the feedback inbox is empty. Picked that carve-out. The landed filter
  flow (M2-09b) was covered only by direct-`Update` tests — never driven through the
  real bubbletea program. Added `TestProgramFilterFlow` (teatest/v2, the M0-05
  harness): drills into a resource, then a live `/` → typed runes → enter-commit flow
  through the running program. Asserted on `tm.FinalModel` rather than scanning
  output, because the status-bar filter segment is background-styled (the terminal
  emulator writes those cells via a path a plain byte scan misses — same reason the
  other program tests key on unselected rows); the final model proves the committed
  query, the closed input, and the narrowed row set (2 of 3). Added a small
  `fakeWatcher.preload` field so a program test receives watch rows straight off the
  channel buffer at `Watch` time, avoiding the test goroutine racing to push into a
  channel created inside an async command (nil default → every existing test
  unchanged).
- Decisions: none (test-only; introduces no constraint a future leg must honor, D67).
- Files: `internal/tui/tui_test.go` (new `TestProgramFilterFlow` + `fakeWatcher.preload`),
  `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues); new test
  `-race`-clean over 5 runs.
- Next: **M2-14** stays partial — the remaining slice is M2-14b (modal confirm flow),
  which is blocked by the gated M2-10. With M2-10…M2-13 all gated by the open dogfood
  human-task and the feedback inbox empty, the loop is **blocked pending that human
  dogfood pass**; the next productive leg needs either that task closed (or feedback
  filed) or another existing-behavior coverage gap (e.g. a program-level namespace
  cancel / error-toast path). Suggested next: if still blocked, report blocked; else
  take the oldest feedback item.
