# 2026-08-16 — STORY-06g-2b-1: the cross-kind unhealthy list (`components/unhealthyview`) + the seams it needs

- Agent: opencode (do-rewrite-leg, interactive session)
- Milestone: M5 (UX-validation line, D268)
- Did: The surface half of the S02 miss's fix, in the component-before-wiring
  rhythm (the SEARCH-02a→02b shape, D52): **`components/unhealthyview`** is the
  full-screen, cursor-navigable list of ScanHits the cross-kind sweep will feed —
  each row `Kind  ns/name  <offending cells>`, the reason cells painted with the
  M4-06 role's hue (Warn/Error), a header tracking the sweep (`unhealthy ·
  scope · N found · scanning M/N kinds…` + the cap line), navigation/`drillIn`/
  `back` as keymap actions, emitting its own `SelectedMsg`/`ClosedMsg` (D56), no
  shared mutable state (principle 1). It has **no query field** — the sweep runs
  once on open, not per keystroke — so unlike the search view it is a pure list
  with no focus split. It runs no scan itself and knows nothing about clients,
  exactly like SEARCH-02a's search view. This slice also lands the two seams the
  surface needs: **`kube.ScanHit` now carries `Columns`** (the table the row came
  from), because "which cell is the offending one" is unanswerable from the row
  alone and the hit's whole shape exists so the reason renders without re-listing
  (D276 pt 2); and the table component exports **`UnhealthyRow`/`UnhealthyCells`**
  — the M4-06 classifier (color.go) lifted to a whole server-printed table — the
  concrete seam D276 named: the sweep's filter, the reason a hit shows, and the
  per-kind `H` filter all run the same `classifyCell`, so the cross-kind list and
  the per-kind filter can never disagree about what is broken (D275 pt 1). The
  wiring — the action + key + Scanner seam + the drill-in that switches browse to
  a hit — is **06g-2b-2**, split back to Backlog; the feedback file
  `2026-08-15-pod-first-blinds-non-pod-failures.md` stays with that slice which
  completes the address (D69).
- Decisions: **D278** — the surface is a `components/unhealthyview` sub-model fed
  by `kube.Scan` (no query field, pure list); a scan hit carries the columns so
  the reason is renderable, and the row predicate is the table component's
  exported `UnhealthyRow`/`UnhealthyCells` (the one classification source);
  06g-2b splits into the component+seams (here) and the wiring (06g-2b-2).
- Files: internal/kube/scan.go + scan_test.go (ScanHit.Columns),
  internal/tui/components/table/color.go + color_test.go (UnhealthyRow/UnhealthyCells),
  internal/tui/components/unhealthyview/unhealthyview.go + unhealthyview_test.go (new),
  vault/tasks/board.md, vault/milestones/M5-release.md, vault/knowledge/decisions.md,
  this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ — `make check` green. New hermetic
  tests pin the view's shape (hidden/unsized → empty, inactive ignores input,
  append+grow, drill-in emits the highlighted hit, back emits ClosedMsg,
  navigation moves the cursor, Reset clears hits/progress/cap, the progress/cap
  header states, row rendering with the reason and its role-tagged spans, the
  cluster-scoped bare-name path, the joined multi-reason row, and the delegate
  coloring the reason apart from the plain label) and the seams (`ScanHit`
  carries the row's columns; UnhealthyRow/UnhealthyCells keep the healthy row out,
  return the offending cells in column order with role tags, and ignore columns
  the classifier knows nothing about). No TUI, install, launch or config surface
  changed — the component is not yet reachable — so D68's dogfood clause is
  limited to `make check` and the binary building; the README is unaffected until
  06g-2b-2 adds the gesture.
- Next: **STORY-06g-2b-2** — the wiring: a keymap action that opens
  `unhealthyview` and runs `kube.Scan` over the menu's kinds with
  `table.UnhealthyRow` as the filter, pumping hits in, each drill-in switching
  browse to that resource; README + keybindings doc; delete
  `2026-08-15-pod-first-blinds-non-pod-failures.md` when it lands (D69).
