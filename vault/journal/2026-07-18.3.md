# 2026-07-18 — REVIEW-01: maintainer review of the setup + first leg

- Agent: claude (Fable 5), interactive review with the maintainer
- Milestone: M0 (process/plan leg, no product code)
- Did: Reviewed the vault setup and leg M0-01; the maintainer approved the
  findings. Applied them: legacy trees will be **deleted up-front** instead of
  kept compiling (the k8s.io v0.18 vs client-go v0.31 single-module conflict
  made the old plan unworkable) — new leg **M0-07**, M0-03 absorbed; M0
  reordered so CI follows the toolchain bump; journal split into per-entry
  files under `vault/journal/` with the `Commit:` field dropped; board claims
  now committed + pushed immediately as a lock; `Makefile` with `make check`
  added as the canonical gate; kube tests default to fake clients with envtest
  opt-in and moved to M1 (new M1-00); Bubble Tea v2 preferred; config path set
  to `os.UserConfigDir()/kubecom/config.yaml`; Table-watch gotchas and the
  no-stdout-while-TUI-active logging rule recorded in `stack.md`. Cobra stays
  (maintainer-confirmed); the Opus Co-Authored-By trailer stays.
- Decisions: D14 delete legacy · D15 journal format + milestone upkeep ·
  D16 claim-push · D17 make check + CI-early · D18 fakes default/envtest opt-in ·
  D19 Bubble Tea v2 · D20 XDG config path.
- Files: `vault/**` (journal/, board, milestones M0+M1, decisions, stack, plan,
  README), `CLAUDE.md`, `.claude/skills/do-rewrite-leg/SKILL.md`, `Makefile`.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (no product-code changes; Makefile only)
- Next: **M0-07** — delete the legacy trees, prune `go.mod`, drop the
  `.golangci.yml` path excludes (D14).
