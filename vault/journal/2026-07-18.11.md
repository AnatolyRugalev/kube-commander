# 2026-07-18 — M0-04: GitHub Actions CI runs `make check`

- Agent: claude (Opus 4.8)
- Milestone: M0
- Did: Added `.github/workflows/ci.yml` — one `check` job on a
  `[ubuntu-latest, macos-latest]` matrix that runs `make check` verbatim (the
  D17 canonical gate), so CI green == local green. golangci-lint is installed
  via the official `install.sh` pinned to `GOLANGCI_LINT_VERSION` (v2.5.0,
  matching the local tool) rather than `golangci-lint-action`, keeping CI a
  single end-to-end `make check` instead of splitting lint out. Go floor comes
  from `go-version-file: go.mod` (single source of truth, D23). Triggers on
  push/PR to `v1`/`main`; `concurrency` cancels superseded runs; least-privilege
  `permissions: contents: read`.
- Decisions: D24 (CI runs `make check` verbatim; golangci-lint installed, not action-run).
- Files: `.github/workflows/ci.yml`, `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M0-groundwork.md`, this entry.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check` exit 0, 0 lint issues);
  workflow YAML parsed + matrix/triggers checked. Live GH Actions run fires on this push.
- Next: **M0-08** — sweep remaining legacy files (`Dockerfile`, `get.sh`, `ci/aur/`
  incl. `id_rsa.enc`, `ci/terminalizer/`) so the M0-06 release redesign starts clean.
