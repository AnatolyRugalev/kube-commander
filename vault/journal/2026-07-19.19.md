# 2026-07-19 — M2-05a: static seed resource-menu sidebar

- Agent: claude-opus (Opus 4.8), autonomous leg
- Milestone: M2
- Did: Landed `internal/tui/components/menu` — the browse view's left pane and the
  second `components/*` package. `Model` is a static-seeded vertical list of core
  resource kinds (`seedItems()`: namespaces/nodes/events · pods + apps/batch
  workloads · services/ingresses · configmaps/secrets/serviceaccounts · pvcs/pvs/
  storageclasses), each a real `kube.Resource` (GVK/GVR/scope) so a selection gives
  the kube layer everything List/Watch needs. It consumes **keymap actions, never
  raw keys** (D11): `Update(a keymap.Action)` moves the highlight (up/down/top/
  bottom, clamped, with a scroll `offset` that keeps the cursor visible on a short
  pane) and, on `nav.drillIn`, emits `menu.ResourceSelectedMsg` for the highlighted
  item (unavailable item -> no-op, for the M2-05b reconcile). Pure value-receiver
  `View` frames the list with `Pane`/`PaneFocus` and highlights the cursor via the
  `Selection` style (D54); returns `""` until sized. Owns no shared mutable state
  (principle 1). Also **removed the placeholder `tui.ResourceSelectedMsg`** from
  `msg.go`: a component-emitted message is owned by the emitter's package (the root
  model imports the component, so the component can't import `tui` back without a
  cycle) — `msg.go` now keeps only the kube-boundary messages + `ErrorMsg`, and a
  note records that the table's `RowSelectedMsg` moves to the table package in
  M2-06.
- Decisions: D56 — emitter owns the message type (menu owns `ResourceSelectedMsg`,
  removed the `tui` placeholder); static discovery-independent seed; title =
  `GVK.Kind`; actions-in/no-raw-keys; scroll offset keeps the cursor visible;
  page/half-page deferred.
- Files: `internal/tui/components/menu/menu.go`,
  `internal/tui/components/menu/seed.go`,
  `internal/tui/components/menu/menu_test.go`, `internal/tui/msg.go` (removed the
  placeholder + added the ownership note), `vault/knowledge/decisions.md`,
  `vault/tasks/board.md`, `vault/milestones/M2-core-tui.md`, this entry. No new deps.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  white-box tests: seed non-empty/first-selected/well-formed, navigation clamp +
  top/bottom jumps, unhandled action ignored, drill-in emits `ResourceSelectedMsg`
  for the highlighted resource, drill-in on an unavailable item emits nothing,
  scroll offset keeps the cursor visible on a 2-row window, `View` empty until
  sized + focus changes the frame. Also rendered the menu end-to-end (24x10 pane,
  focused, cursor on Pod) to eyeball the accent border + full-width Selection bar.
- Next: **M2-05b** — reconcile the seed with `DiscoveryReadyMsg` (add CRDs / extra
  groups, mark unavailable) **without disturbing the current selection or scroll**
  (the M2 risk item). Then **M2-06a** (table snapshot render). M2-05a + M2-06a are
  the two panes M2-07b composes into the two-pane browse layout.
