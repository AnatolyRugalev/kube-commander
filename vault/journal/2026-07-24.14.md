# 2026-07-24 — LOGS-01: dedicated full-screen logs-view component with live filter

- Agent: claude-opus-4-8
- Milestone: M3
- Did: Drained the oldest feedback item (`2026-07-24-logs-dedicated-view-live-grep`,
  normal) — "logs should open in a dedicated full-screen logs view with real-time
  grep." It's a multi-leg item, so per D69 I **triaged** it into board tasks LOGS-01…04
  (D134) and did the **first slice**. LOGS-01 is the new `internal/tui/components/logsview`
  component in isolation (D52 bottom-up rhythm, like the M3-01 viewer / M2-08a picker —
  not wired yet): a streaming append buffer (`Append`), a **live case-insensitive
  substring filter** (reusing `app.filter`/`/`) that narrows the shown lines **while
  following**, follow/pause (reusing `logs.follow`/`f`) with auto-scroll-to-bottom on
  append and upward-scroll auto-pauses, a **full-screen** header (`[following]`/`[paused]`
  + active query + matched/total) — full-screen (no border) because logs want every
  column. Keymap-driven (D11), message-only (`ClosedMsg`, principle 1); the only raw-key
  entry is the filter field (`UpdateFilter`), exactly as the picker. Recorded the
  architecture as D134 and deleted the feedback file.
- Decisions: D134 — logs move off the shared M3-01 viewer to a dedicated full-screen
  `logsview` mini-app with a live filter; shared viewer keeps YAML/describe/secret; the
  logs sink changes but the M3-07a/07b container+pod resolution and the D53 log pump stay.
- Files: internal/tui/components/logsview/logsview.go (new component),
  internal/tui/components/logsview/logsview_test.go (new: follow/tail, upward-scroll
  pause + no-yank, follow toggle resumes at bottom, live substring narrowing + header
  count, narrow-while-following, back clears-then-closes, Reset, inactive-ignores),
  vault/knowledge/decisions.md (D134), vault/tasks/board.md (LOGS section + LOGS-01 done),
  vault/feedback/2026-07-24-logs-dedicated-view-live-grep.md (deleted),
  vault/journal/2026-07-24.14.md (new). M3 milestone unchanged — no exit criterion moves
  until the wiring (LOGS-02) makes it the running logs surface.
- Verify: build ✓ · test ✓ · vet ✓ · lint ✓ (`make check`, 0 issues). Hermetic
  component tests, no cluster. The component is **not yet wired** into app.go (that's
  LOGS-02), so the running binary is byte-for-byte unchanged — no bare-launch/dogfood
  regression (D68). No runtime surface touched this leg.
- Next: LOGS-02 — wire `res.logs` to the new logs view: stream into `logsview` instead
  of the shared M3-01 viewer, retire the `viewerKindLogs`/`logFollow`/`logTitle`
  special-casing in app.go, keep the M3-07a container picker + M3-07b pod resolution +
  D53 log pump. Must stay launchable (D68); raise a real-terminal filter/throughput
  dogfood human-task if warranted. (One feedback item remains and still preempts the
  board next leg: `unify-yaml-view-and-edit`.)
