// Package config defines kubecom's plain-YAML configuration, including the
// `keys:` action→bindings section, and the one-shot migration from the legacy
// ~/.kubecom.yaml. It is implemented in a later milestone; this file establishes
// the package so the module layout is in place.
package config
